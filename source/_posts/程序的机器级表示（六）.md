---
title: 程序的机器级表示（六）
tags:
  - 计算机系统基础
categories: CSAPP 学习
toc: true
typora-root-url: 程序的机器级表示（六）
typora-copy-images-to: 程序的机器级表示（六）
abbrlink: 854463682
date: 2026-09-09 17:09:27
summary: 本文讲解变长栈帧与帧指针的使用，以及 x86-64 浮点代码：媒体寄存器、浮点传送转换与运算操作。
description: 本文对应《深入理解计算机系统》第 3 章 3.10.5 节与 3.11 节，讲解支持变长栈帧的帧指针（%rbp）使用与变长数组的栈分配，以及 x86-64 浮点代码：SSE/AVX 媒体寄存器（%xmm）、浮点传送与类型转换操作、浮点数作为参数和返回值的传递规则、浮点运算指令、浮点常数以及位级操作和比较操作。
---

---
## 写在前面

这篇文章对应的是原书的以下章节：

- 第 3 章 程序的机器级表示
  - 3.10 在机器级程序中将控制与数据结合起来（只含 3.10.5）
  - 3.11 浮点代码

部分内容为ai生成，如有错误恳请指出。

---

## 一、支持变长栈帧

到目前为止，我们检查过的各种函数的机器级代码都有一个共同点：编译器能够预先确定需要为栈帧分配多少空间。但有些函数需要的局部存储是变长的。例如，当函数调用 `alloca` 时就会发生这种情况——`alloca` 是一个标准库函数，可以在栈上分配任意字节数量的存储；当代码声明一个局部变长数组时，也会发生这种情况。

虽然本节介绍的内容实际上属于过程实现的一部分，但我们还是把它推迟到现在才讲，因为它需要用到数组和数据对齐的相关知识。

### 1.1 变长数组示例

图 3-43a 的代码给出了一个包含变长数组的例子。函数声明了存有 $n$ 个指针的局部数组 `p`，其中 $n$ 由第一个参数给出，这要求在栈上分配 $8n$ 个字节，而 $n$ 的值每次调用该函数时都可能不同，因此编译器无法预先确定要给该函数的栈帧分配多少空间。此外，该程序还产生了对局部变量 `i` 的地址引用，因此该变量必须存储在栈中。执行过程中，程序必须能够访问局部变量 `i` 和数组 `p` 中的元素；返回时，函数必须释放这个栈帧，并将栈指针设置为存储返回地址的位置。

**a) C 代码**

```c
long vframe(long n, long idx, long *q) {
    long i;
    long *p[n];
    p[0] = &i;
    for (i = 1; i < n; i++)
        p[i] = q;
    return *p[idx];
}
```

*图 3-43：需要使用帧指针的函数。变长数组意味着在编译时无法确定栈帧的大小。*

### 1.2 帧指针

为了管理变长栈帧，x86-64 代码使用寄存器 `%rbp` 作为**帧指针**（frame pointer，有时称为**基指针**，base pointer），这也是 `%rbp` 中 bp 两个字母的由来。当使用帧指针时，栈帧的组织结构与图 3-44 中函数 `vframe` 的情况一样。

可以看到，代码必须先把 `%rbp` 之前的值保存到栈中，因为它是一个被调用者保存寄存器。然后在函数的整个执行过程中，都使 `%rbp` 指向 `%rbp` 被压入栈时栈指针的位置，再用相对于 `%rbp` 的偏移量来引用固定长度的局部变量（例如 `i`）。

**b) 生成的部分汇编代码**

```asm
# long vframe(long n, long idx, long *q)
# n 在 %rdi，idx 在 %rsi，q 在 %rdx
# 仅展示部分代码

 1 vframe:
 2     pushq %rbp                 # 保存原来的 %rbp
 3     movq %rsp, %rbp            # 设置帧指针
 4     subq $16, %rsp             # 为 i 分配空间（此时 %rsp = s1）
 5     leaq 22(,%rdi,8), %rax
 6     andq $-16, %rax
 7     subq %rax, %rsp            # 为数组 p 分配空间（此时 %rsp = s2）
 8     leaq 7(%rsp), %rax
 9     shrq $3, %rax
10     leaq 0(,%rax,8), %r8       # 将 %r8 设置为 &p[0]
11     movq %r8, %rcx             # 将 %rcx 设置为 &p[0]（%rcx = p）

      初始化循环的代码
      i 在 %rax 和栈上，n 在 %rdi，p 在 %rcx，q 在 %rdx

12 .L3:                          # loop：
13     movq %rdx, (%rcx,%rax,8)   # 将 p[i] 设置为 q
14     addq $1, %rax              # i 加 1
15     movq %rax, -8(%rbp)        # 存回栈中
16 .L2:
17     movq -8(%rbp), %rax        # 从栈中取出 i
18     cmpq %rdi, %rax            # 比较 i 与 n
19     jl .L3                     # 若 <，转到 loop

      函数结尾的代码

20     leave                      # 恢复 %rbp 和 %rsp
21     ret                        # 返回
```

图 3-43b 是 GCC 为函数 `vframe` 生成的部分汇编代码。在函数的开始，代码建立栈帧，并为数组 `p` 分配空间：首先把 `%rbp` 的当前值压入栈中，接着将 `%rbp` 设置为指向当前的栈位置（第 $2 \sim 3$ 行）。然后，在栈上分配 16 个字节，其中前 8 个字节用于存储局部变量 `i`，而后 8 个字节是未被使用的（保证栈顶 `%rsp` 在后续为变长数组 `p` 分配空间之前，已经处于 16 字节对齐的状态，这是 x86-64 ABI 的硬性要求）。接着，为数组 `p` 分配空间（第 $5 \sim 11$ 行）。练习题 3.49 探讨了该分配多少空间以及将 `p` 放在这段空间的什么位置。这里先略过分配的具体数学逻辑，总之，当程序执行到第 11 行时，已经：

1. 在栈上分配了至少 $8n$ 字节；
2. 将数组 `p` 设置为指向已分配的区域，至少有 $8n$ 字节可供其使用。

![图 3-44 函数 vframe 的栈组织](1788855770877.png)

初始化循环的代码给出了如何引用局部变量 `i` 和 `p` 的例子。第 13 行表明数组元素 `p[i]` 被设置为 `q`，该指令用寄存器 `%rcx` 中的值作为 `p` 的起始地址。我们还可以看到修改局部变量 `i`（第 15 行）和读取局部变量 `i`（第 17 行）的例子，`i` 的地址是引用 `-8(%rbp)`，也就是相对于帧指针偏移量为 -8 的地方。

在函数的结尾，`leave` 指令将帧指针恢复到它之前的值（第 20 行）。这条指令不需要参数，等价于执行下面两条指令：

```asm
movq %rbp, %rsp    # 将栈指针设置为帧的起始位置
popq %rbp          # 恢复保存的 %rbp，并将栈指针设置到调用者帧的末尾
```

也就是说，先把栈指针设置为保存 `%rbp` 值的位置，再把该值从栈中弹出到 `%rbp`。这个指令组合具有释放整个栈帧的效果。

在较早版本的 x86 代码中，每个函数调用都使用帧指针；而现在，只在栈帧长度可变的情况下才使用，就像函数 `vframe` 一样。历史上，大多数编译器在生成 IA32 代码时会使用帧指针，最近的 GCC 版本则放弃了这个惯例。把使用帧指针的代码和不使用帧指针的代码混在一起是可以的，只要所有函数都把 `%rbp` 当作被调用者保存寄存器来处理即可。

### 1.3 变长数组的分配

**练习题 3.49** 这道题中，我们要探究图 3-43b 第 $5 \sim 11$ 行代码背后的逻辑，它分配了变长大小的数组 `p`。正如代码的注释表明的，$s_1$ 表示执行第 4 行的 `subq` 指令（为局部变量 `i` 分配空间）之后栈指针的值；$s_2$ 表示执行第 7 行的 `subq` 指令（为局部数组 `p` 分配存储）之后栈指针的值；$p$ 表示第 $10 \sim 11$ 行的指令赋给寄存器 `%r8` 和 `%rcx` 的值，这两个寄存器都用来引用数组 `p`。

图 3-44 的右边画出了 $s_1$、$s_2$ 和 $p$ 指示的位置。图中还画出了 $s_2$ 和 `p` 的值之间可能有一个偏移量为 $e_2$ 字节的空间，这部分空间是未被使用的；数组 `p` 的结尾和 $s_1$ 指示的位置之间还可能有一个偏移量为 $e_1$ 字节的空间。

A. 用数学语言解释第 $5 \sim 7$ 行中计算 $s_2$ 的逻辑。提示：想想 -16 的位级表示以及它在第 6 行 `andq` 指令中的作用。

B. 用数学语言解释第 $8 \sim 10$ 行中计算 `p` 的逻辑。提示：复习一下《[整数的表示和运算](/blog/posts/4205707561/)》4.6 节的除以 2 的幂。

C. 对于下面 $n$ 和 $s_1$ 的值，跟踪代码的执行，确定 $s_2$、`p`、$e_1$ 和 $e_2$ 的结果值。

| $n$ | $s_1$ | $s_2$ | `p` | $e_1$ | $e_2$ |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 5 | 2065 |  |  |  |  |
| 6 | 2064 |  |  |  |  |

<div class="tip">
$s_1$ 在真实的 x86-64 程序运行中一定是 16 字节对齐的。此处取 2065 是为了说明汇编代码中一系列计算的意义而做出的教学假设。
</div>

D. 这段代码为 $s_2$ 和 `p` 的值提供了什么样的对齐属性？

**答案：**

这段代码包含许多我们见过的位级运算技巧，需要仔细研究才能看懂。

A. 第 5 行的 `leaq` 指令计算值 $8n+22$，然后第 6 行的 `andq` 指令把它向下舍入到最接近的 16 的倍数：当 $n$ 是奇数时，结果值会是 $8n+8$；当 $n$ 是偶数时，结果值会是 $8n+16$。用 $s_1$ 减去这个值就得到 $s_2$。

B. 该序列中的三条指令将 $s_2$ 向上舍入到最近的 8 的倍数，作为 `p` 的起始地址。它们利用了之前提过的、实现除以 2 的幂时用到的偏移和移位的组合。

C. 这两个例子可以看做最小化和最大化 $e_1$ 和 $e_2$ 的情况。

| $n$ | $s_1$ | $s_2$ | `p` | $e_1$ | $e_2$ |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 5 | 2065 | 2017 | 2024 | 1 | 7 |
| 6 | 2064 | 2000 | 2000 | 16 | 0 |

D. 可以看到 $s_2$ 的计算方式会把到 $s_1$ 的偏移量舍入为最近的大于等于 $8n+8$ 的 16 的倍数（留出 8 个字节的余量供 `p` 对齐）。还可以看到 `p` 会以 8 的倍数对齐，正是对 8 字节元素数组建议使用的对齐方式。

---

## 二、浮点代码

### 2.1 媒体寄存器

简要回顾一下历史，会对理解 x86-64 的浮点体系结构有所帮助。1997 年出现了 Pentium/MMX，此后 Intel 和 AMD 都引入了持续数代的**媒体**（media）指令，用以支持图形和图像处理。这类指令的本意是允许多个操作以并行模式执行，称为**单指令多数据**（SIMD，读作 sim-dee），即对多个不同的数据并行执行同一个操作。近年来，这些扩展有了长足的发展，名字也经过了一系列大的修改：从 MMX 到 SSE（Streaming SIMD Extension，流式 SIMD 扩展），再到最新的 AVX（Advanced Vector Extension，高级向量扩展），每一代中还有一些不同的版本。每个扩展管理的都是一组寄存器中的数据，这些寄存器组在 MMX 中称为 MM 寄存器，SSE 中称为 XMM 寄存器，而在 AVX 中称为 YMM 寄存器；MM 寄存器是 64 位的，XMM 是 128 位的，而 YMM 是 256 位的。所以，每个 YMM 寄存器可以存放 8 个 32 位值，或 4 个 64 位值，这些值可以是整数，也可以是浮点数。

2000 年 Pentium 4 中引入了 SSE2，媒体指令开始包括那些对标量浮点数据进行操作的指令，也就是对单个数据值进行操作，使用 XMM 或 YMM 寄存器的低 32 位或 64 位中的单个值。这种标量模式提供的一组寄存器和指令，更类似于其他处理器支持浮点数的方式。所有能够执行 x86-64 代码的处理器都支持 SSE2 或更高的版本，因此 x86-64 的浮点数是基于 SSE 或 AVX 的，包括传递过程参数和返回值的规则。

我们的讲述基于 AVX2，即 AVX 的第二个版本，它是在 2013 年 Core i7 Haswell 处理器中引入的。当给定命令行参数 `-mavx2` 时，GCC 会生成 AVX2 代码。基于不同版本 SSE 以及第一个版本 AVX 的代码从概念上来说类似，只是指令名和格式有所不同。我们只介绍用 GCC 编译浮点程序时会出现的那些指令，其中大部分是标量 AVX 指令；对整个数据向量进行操作的指令出现时，我们也会说明。

如图 3-45 所示，AVX 浮点体系结构允许把数据存储在 16 个 YMM 寄存器中，它们的名字为 $\%ymm0 \sim \%ymm15$。每个 YMM 寄存器都是 256 位（32 字节）。当对标量数据操作时，这些寄存器只保存浮点数，而且只使用低 32 位（对于 `float`）或 64 位（对于 `double`），汇编代码用寄存器的 SSE XMM 寄存器名字 $\%xmm0 \sim \%xmm15$ 来引用它们，每个 XMM 寄存器都是对应 YMM 寄存器的低 128 位（16 字节）。

![图 3-45 媒体寄存器](1788864876018.png)

### 2.2 浮点传送和转换操作

#### 传送指令

图 3-46 给出了一组在内存和 XMM 寄存器之间以及从一个 XMM 寄存器到另一个 XMM 寄存器传送浮点数的指令：

| 指令 | 源 | 目的 | 描述 |
| :--- | :--- | :--- | :--- |
| `vmovss` | $M_{32}$ | X | 传送单精度数 |
| `vmovss` | X | $M_{32}$ | 传送单精度数 |
| `vmovsd` | $M_{64}$ | X | 传送双精度数 |
| `vmovsd` | X | $M_{64}$ | 传送双精度数 |
| `vmovaps` | X | X | 传送对齐的封装好的单精度数 |
| `vmovapd` | X | X | 传送对齐的封装好的双精度数 |

其中，有一个操作数引用内存的指令是**标量**指令，意味着它们只对单个而不是一组封装好的数据值进行操作。GCC 只用标量传送操作从内存传送数据到 XMM 寄存器，或从 XMM 寄存器传送数据到内存。数据要么保存在内存中（由表中的 $M_{32}$ 和 $M_{64}$ 指明），要么保存在 XMM 寄存器中（在表中用 X 表示）。无论数据对齐与否，这些指令都能正确执行，不过代码优化规则建议 32 位内存数据满足 4 字节对齐，64 位数据满足 8 字节对齐。内存引用的指定方式与整数 `mov` 指令的一样，包括偏移量、基址寄存器、变址寄存器和伸缩因子的所有可能的组合。

对于在两个 XMM 寄存器之间传送数据，GCC 会使用两种指令之一，即用 `vmovaps` 传送单精度数，用 `vmovapd` 传送双精度数。对于这些情况，程序复制整个寄存器还是只复制低位值，既不会影响程序功能，也不会影响执行速度，所以使用这些指令和使用针对标量数据的指令，其实没有实质上的差别。指令名字中的字母 a 表示 aligned（对齐的）。当用于读写内存时，如果地址不满足 16 字节对齐，它们会导致异常；不过在两个寄存器之间传送数据，绝不会出现错误对齐的状况。

#### 代码示例

下面是一个展示不同浮点传送操作的例子，考虑以下 C 函数：

```c
float float_mov(float v1, float *src, float *dst) {
    float v2 = *src;
    *dst = v1;
    return v2;
}
```

与它相关联的 x86-64 汇编代码为：

```asm
# float float_mov(float v1, float *src, float *dst)
# v1 在 %xmm0，src 在 %rdi，dst 在 %rsi

float_mov:
    vmovaps %xmm0, %xmm1     # 复制 v1
    vmovss (%rdi), %xmm0     # 从 src 读出 v2
    vmovss %xmm1, (%rsi)     # 将 v1 写入 dst
    ret                      # 返回 v2（位于 %xmm0 中）
```

在这个例子中可以看到，它使用了 `vmovaps` 指令把数据从一个寄存器复制到另一个，使用了 `vmovss` 指令把数据从内存复制到 XMM 寄存器以及从 XMM 寄存器复制到内存。

#### 转换指令

图 3-47 和图 3-48 给出了在浮点数和整数数据类型之间以及不同浮点格式之间进行转换的指令集合，这些都是对单个数据值进行操作的标量指令。

图 3-47 中的指令把一个从 XMM 寄存器或内存中读出的浮点值进行转换，并将结果写入一个通用寄存器（例如 `%rax`、`%ebx` 等）：

| 指令 | 源 | 目的 | 描述 |
| :--- | :--- | :--- | :--- |
| `vcvttss2si` | X / $M_{32}$ | $R_{32}$ | 用截断的方法把单精度数转换成整数 |
| `vcvttsd2si` | X / $M_{64}$ | $R_{32}$ | 用截断的方法把双精度数转换成整数 |
| `vcvttss2siq` | X / $M_{32}$ | $R_{64}$ | 用截断的方法把单精度数转换成四字整数 |
| `vcvttsd2siq` | X / $M_{64}$ | $R_{64}$ | 用截断的方法把双精度数转换成四字整数 |

把浮点值转换成整数时，指令会执行截断（truncation），把值向 0 进行舍入，这是 C 和大多数其他编程语言的要求。

图 3-48 中的指令把整数转换成浮点数。它们使用的是不太常见的三操作数格式，有两个源和一个目的。第一个操作数读自于内存或一个通用整数寄存器；这里可以忽略第二个操作数，因为它的值只会影响结果的高位字节；而目的操作数必须是 XMM 寄存器。在最常见的使用场景中，第二个源和目的操作数都是一样的：

| 指令 | 源 1 | 源 2 | 目的 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `vcvtsi2ss` | $M_{32}$ / $R_{32}$ | X | X | 把整数转换成单精度数 |
| `vcvtsi2sd` | $M_{32}$ / $R_{32}$ | X | X | 把整数转换成双精度数 |
| `vcvtsi2ssq` | $M_{64}$ / $R_{64}$ | X | X | 把四字整数转换成单精度数 |
| `vcvtsi2sdq` | $M_{64}$ / $R_{64}$ | X | X | 把四字整数转换成双精度数 |

就像下面这条指令：

```asm
vcvtsi2sdq %rax, %xmm1, %xmm1
```

这条指令从寄存器 `%rax` 读出一个长整数，把它转换成数据类型 `double`，并把结果存放进 XMM 寄存器 `%xmm1` 的低 8 字节中。

#### GCC 的神秘浮点格式转换代码

最后，要在两种不同的浮点格式之间转换，GCC 的当前版本生成的代码需要单独说明。假设 `%xmm0` 的低位 4 字节保存着一个单精度值，很容易就想到用下面这条指令

```asm
vcvtss2sd %xmm0, %xmm0, %xmm0
```

把它转换成一个双精度值，并将结果存储在寄存器 `%xmm0` 的低 8 字节。不过我们发现 GCC 生成的代码如下：

```asm
# 从单精度到双精度的转换
vunpcklps %xmm0, %xmm0, %xmm0    # 复制第一个向量元素
vcvtps2pd %xmm0, %xmm0           # 把两个向量元素转换成 double
```

`vunpcklps` 指令通常用来交叉放置来自两个 XMM 寄存器的值，把它们存储到第三个寄存器中。也就是说，如果一个源寄存器的内容为四个单精度值 $[s_3, s_2, s_1, s_0]$，另一个源寄存器为 $[d_3, d_2, d_1, d_0]$，那么目的寄存器的值会是 $[s_1, d_1, s_0, d_0]$。在上面的代码中，三个操作数使用同一个寄存器，所以如果原始寄存器的值为 $[x_3, x_2, x_1, x_0]$，那么该指令会将寄存器的值更新为 $[x_1, x_1, x_0, x_0]$。`vcvtps2pd` 指令把源 XMM 寄存器中的两个低位单精度值扩展成目的 XMM 寄存器中的两个双精度值。对前面 `vunpcklps` 指令的结果应用这条指令，会得到值 $[dx_0, dx_0]$，这里 $dx_0$ 是将 $x_0$ 转换成双精度后的结果。

也就是说，这两条指令的最终效果是将原始 `%xmm0` 低位 4 字节中的单精度值转换成双精度值，再将其两个副本保存到 `%xmm0` 中。我们不太清楚 GCC 为什么会生成这样的代码，这样做既没有好处，也没有必要。

对于把双精度转换为单精度，GCC 会产生类似的代码：

```asm
# 从双精度到单精度的转换
vmovddup %xmm0, %xmm0            # 复制第一个向量元素
vcvtpd2psx %xmm0, %xmm0          # 把两个向量元素转换成 float
```

假设这些指令开始执行前寄存器 `%xmm0` 保存着两个双精度值 $[x_1, x_0]$，然后 `vmovddup` 指令把它设置为 $[x_0, x_0]$，`vcvtpd2psx` 指令把这两个值转换成单精度，再存放到该寄存器的低位一半中，并将高位一半设置为 0，得到结果 $[0.0, 0.0, x_0, x_0]$（回想一下，浮点值 0.0 是由位模式全 0 表示的）。同样，用这种方式把一种精度转换成另一种精度，而不用下面的单条指令，没有明显直接的意义：

```asm
vcvtsd2ss %xmm0, %xmm0, %xmm0
```

#### 代码示例

下面是一个展示不同浮点转换操作的例子，考虑以下 C 函数：

```c
double fcvt(int i, float *fp, double *dp, long *lp) {
    float f = *fp; double d = *dp; long l = *lp;
    *lp = (long) d;
    *fp = (float) i;
    *dp = (double) l;
    return (double) f;
}
```

以及它对应的 x86-64 汇编代码：

```asm
# double fcvt(int i, float *fp, double *dp, long *lp)
# i 在 %edi，fp 在 %rsi，dp 在 %rdx，lp 在 %rcx

fcvt:
    vmovss (%rsi), %xmm0               # 取出 f = *fp
    movq (%rcx), %rax                  # 取出 l = *lp
    vcvttsd2siq (%rdx), %r8            # 取出 d = *dp 并转换成 long
    movq %r8, (%rcx)                   # 存到 lp 所指的位置
    vcvtsi2ss %edi, %xmm1, %xmm1       # 把 i 转换成 float
    vmovss %xmm1, (%rsi)               # 存到 fp 所指的位置
    vcvtsi2sdq %rax, %xmm1, %xmm1      # 把 l 转换成 double
    vmovsd %xmm1, (%rdx)               # 存到 dp 所指的位置

    # 下面两条指令把 f 转换成 double
    vunpcklps %xmm0, %xmm0, %xmm0
    vcvtps2pd %xmm0, %xmm0
    ret                                # 返回 f
```

`fcvt` 的所有参数都是通过通用寄存器传递的，因为它们都是整数或者指针。结果通过寄存器 `%xmm0` 返回——如图 3-45 中描述的，这是 `float` 或 `double` 值指定的返回寄存器。在这段代码中，可以看到图 3-46 至图 3-48 中的许多传送和转换指令，还可以看到 GCC 将单精度转换为双精度的方法。

### 2.3 浮点数做参数和返回值

在 x86-64 中，XMM 寄存器用来向函数传递浮点参数，以及从函数返回浮点值。如图 3-45 所示，可以看到如下规则：

- XMM 寄存器 $\%xmm0 \sim \%xmm7$ 最多可以传递 8 个浮点参数，按照参数列出的顺序使用这些寄存器，可以通过栈传递额外的浮点参数；
- 函数使用寄存器 `%xmm0` 来返回浮点值；
- 所有的 XMM 寄存器都是**调用者保存**的，被调用者可以不用保存就覆盖这些寄存器中的任意一个；
- 当函数包含指针、整数和浮点数混合的参数时，指针和整数通过通用寄存器传递，而浮点值通过 XMM 寄存器传递。也就是说，参数到寄存器的映射取决于它们的类型和排列顺序。

下面是一些例子：

```c
double f1(int x, double y, long z);
```

这个函数会把 `x` 存放在 `%edi` 中，`y` 放在 `%xmm0` 中，而 `z` 放在 `%rsi` 中。

```c
double f2(double y, int x, long z);
```

这个函数的寄存器分配与函数 `f1` 相同。

```c
double f1(float x, double *y, long *z);
```

这个函数会将 `x` 放在 `%xmm0` 中，`y` 放在 `%rdi` 中，而 `z` 放在 `%rsi` 中。

### 2.4 浮点运算操作

#### 标量浮点算术运算指令

图 3-49 描述了一组执行算术运算的标量 AVX2 浮点指令。每条指令有一个（$S_1$）或两个（$S_1$、$S_2$）源操作数，和一个目的操作数 $D$。第一个源操作数 $S_1$ 可以是一个 XMM 寄存器或一个内存位置；第二个源操作数和目的操作数都必须是 XMM 寄存器。每个操作都有一条针对单精度的指令和一条针对双精度的指令，结果存放在目的寄存器中。

| 单精度 | 双精度 | 效果 | 描述 |
| :--- | :--- | :--- | :--- |
| `vaddss` | `vaddsd` | $D \leftarrow S_2 + S_1$ | 浮点数加 |
| `vsubss` | `vsubsd` | $D \leftarrow S_2 - S_1$ | 浮点数减 |
| `vmulss` | `vmulsd` | $D \leftarrow S_2 \times S_1$ | 浮点数乘 |
| `vdivss` | `vdivsd` | $D \leftarrow S_2 / S_1$ | 浮点数除 |
| `vmaxss` | `vmaxsd` | $D \leftarrow \max(S_2, S_1)$ | 浮点数最大值 |
| `vminss` | `vminsd` | $D \leftarrow \min(S_2, S_1)$ | 浮点数最小值 |
| `sqrtss` | `sqrtsd` | $D \leftarrow \sqrt{S_1}$ | 浮点数平方根 |

#### 代码示例

来看一个例子，考虑下面的浮点函数：

```c
double funct(double a, float x, double b, int i) {
    return a*x - b/i;
}
```

x86-64 代码如下：

```asm
# double funct(double a, float x, double b, int i)
# a 在 %xmm0，x 在 %xmm1，b 在 %xmm2，i 在 %edi

 1 funct:
 2     vunpcklps %xmm1, %xmm1, %xmm1   # 下面两条指令把 x 转换成 double
 3     vcvtps2pd %xmm1, %xmm1
 4     vmulsd %xmm0, %xmm1, %xmm0      # 计算 a * x
 5     vcvtsi2sd %edi, %xmm1, %xmm1    # 把 i 转换成 double
 6     vdivsd %xmm1, %xmm2, %xmm2      # 计算 b / i
 7     vsubsd %xmm2, %xmm0, %xmm0      # 从 a * x 中减去 b / i
 8     ret                             # 返回
```

三个浮点参数 `a`、`x` 和 `b` 通过 XMM 寄存器 $\%xmm0 \sim \%xmm2$ 传递，而整数参数 `i` 通过寄存器 `%edi` 传递。标准的双指令序列用来将参数 `x` 转换为双精度类型（第 $2 \sim 3$ 行）；另一条转换指令用来将参数 `i` 转换为双精度类型（第 5 行）。该函数的返回值通过寄存器 `%xmm0` 返回。

### 2.5 浮点常数

和整数运算操作不同，AVX 浮点操作不能以立即数作为操作数。相反，编译器必须为所有的常量值分配和初始化存储空间，然后代码再把这些值从内存读入。下面这个从摄氏度到华氏度转换的函数就说明了这个问题：

```c
double cel2fahr(double temp) {
    return 1.8 * temp + 32.0;
}
```

相应的 x86-64 汇编代码如下：

```asm
# double cel2fahr(double temp)
# temp 在 %xmm0

cel2fahr:
    vmulsd .LC2(%rip), %xmm0, %xmm0    # 乘以 1.8
    vaddsd .LC3(%rip), %xmm0, %xmm0    # 加上 32.0
    ret

.LC2:
    .long 3435973837                   # 1.8 的低位 4 字节
    .long 1073532108                   # 1.8 的高位 4 字节

.LC3:
    .long 0                            # 32.0 的低位 4 字节
    .long 1077936128                   # 32.0 的高位 4 字节
```

<div class="tip">
`.LC2(%rip)` 这种写法，读取的是 RIP 寄存器（程序计数器 PC）的值，加上汇编器/链接器计算出的一个固定偏移量，最终得到 `.LC2` 这个标签所在的运行时虚拟地址。
</div>

可以看到函数从标号为 `.LC2` 的内存位置读出值 1.8，从标号为 `.LC3` 的位置读出值 32.0。观察这些标号对应的值，可以看出每一个都是通过一对 `.long` 声明和十进制表示的值指定的。该怎样把这些数解释为浮点值呢？看看标号为 `.LC2` 的声明，有两个值：3435973837（0xcccccccd）和 1073532108（0x3ffccccc）。因为机器采用的是小端法字节顺序，第一个值给出的是低位 4 字节，第二个给出的是高位 4 字节。从高位字节可以抽取指数字段为 0x3ff（1023），减去偏移 1023 得到指数 0；将两个值的小数位连接起来，得到小数字段 0xccccccccccccd，二进制小数表示为 0.8，加上隐含的 1 得到 1.8。

### 2.6 位级操作和比较操作

#### 位级操作指令

有时，我们会发现 GCC 生成的代码会在 XMM 寄存器上执行位级操作。图 3-50 展示了一些相关的指令，类似于它们在通用寄存器上对应的操作。这些指令对两个源寄存器的所有位都实施指定的位级操作，然后更新整个目的 XMM 寄存器。和前面一样，我们只对标量数据感兴趣，只想了解这些指令对目的寄存器的低 4 或 8 字节的影响。运用这些操作通常可以简单方便地操作浮点数。

| 单精度 | 双精度 | 效果 | 描述 |
| :--- | :--- | :--- | :--- |
| `vxorps` | `vxorpd` | `D ← S2 ^ S1` | 位级异或（EXCLUSIVE-OR） |
| `vandps` | `vandpd` | `D ← S2 & S1` | 位级与（AND） |

#### 比较指令和条件跳转

AVX2 提供了两条用于比较浮点数值的指令：

| 指令 | 基于 | 描述 |
| :--- | :--- | :--- |
| `ucomiss` $S_1$、$S_2$ | $S_2 - S_1$ | 比较单精度值 |
| `ucomisd` $S_1$、$S_2$ | $S_2 - S_1$ | 比较双精度值 |

这些指令类似于 `cmp` 指令（参见 3.6 节），它们都比较操作数 $S_2$ 和 $S_1$，并且设置条件码指示它们的相对值。与 `cmpq` 一样，它们遵循以相反顺序列出操作数的 ATT 格式惯例。参数 $S_2$ 必须在 XMM 寄存器中，而 $S_1$ 可以在 XMM 寄存器中，也可以在内存中。

浮点比较指令会设置三个条件码：零标志位 ZF、进位标志位 CF 和奇偶标志位 PF。3.6.1 节中我们没有讲奇偶标志位，因为它在 GCC 产生的 x86 代码中不太常见。对于整数操作，当最近的一次算术或逻辑运算产生的值的最低位字节是偶校验的（即这个字节中有偶数个 1），那么就会设置这个标志位；不过对于浮点比较，当两个操作数中任一个是 NaN 时，会设置该位。

条件码的设置条件如下：

| 顺序 $S_2 : S_1$ | CF | ZF | PF |
| :--- | :--- | :--- | :--- |
| 无序的 | 1 | 1 | 1 |
| $S_2 < S_1$ | 1 | 0 | 0 |
| $S_2 = S_1$ | 0 | 1 | 0 |
| $S_2 > S_1$ | 0 | 0 | 0 |

当任一操作数为 NaN 时，就会出现**无序**的情况，可以通过奇偶标志位发现这种情况。通常用 `jp`（jump on parity）指令来做条件跳转，其条件就是浮点比较得到了一个无序的结果。除了这种情况以外，进位和零标志位的值都和对应的无符号比较一样：当两个操作数相等时，设置 ZF；当 $S_2 < S_1$ 时，设置 CF；像 `ja` 和 `jb` 这样的指令可以根据标志位的各种组合进行条件跳转。

#### 代码示例

来看一个浮点比较的例子，图 3-51a 中的 C 函数会根据参数 `x` 与 0.0 的相对关系进行分类，返回一个枚举类型作为结果。C 中的枚举类型是编码为整数的，所以函数可能的值为：0（NEG）、1（ZERO）、2（POS）和 3（OTHER）。当 `x` 的值为 NaN 时，会出现最后一种结果。

**a) C 代码**

```c
typedef enum {NEG, ZERO, POS, OTHER} range_t;

range_t find_range(float x) {
    int result;
    if (x < 0)
        result = NEG;
    else if (x == 0)
        result = ZERO;
    else if (x > 0)
        result = POS;
    else
        result = OTHER;
    return result;
}
```

**b) 生成的汇编代码**

```asm
# range_t find_range(float x)
# x 在 %xmm0

 1 find_range:
 2     vxorps %xmm1, %xmm1, %xmm1       # 将 %xmm1 设置为 0
 3     vucomiss %xmm0, %xmm1            # 比较 0:x
 4     ja .L5                           # 若 >，转到 neg
 5     vucomiss %xmm1, %xmm0            # 比较 x:0
 6     jp .L8                           # 若为 NaN，转到 posornan
 7     movl $1, %eax                    # result = ZERO
 8     je .L3                           # 若 =，转到 done
 9 .L8:                                # posornan:
10     vucomiss .LC0(%rip), %xmm0       # 比较 x:0
11     setbe %al                        # 设置 result = NaN ? 1 : 0
12     movzbl %al, %eax                 # 零扩展
13     addl $2, %eax                    # result += 2（> 0 时为 POS，NaN 时为 OTHER）
14     ret                              # 返回
15 .L5:                               # neg:
16     movl $0, %eax                    # result = NEG
17 .L3:                               # done:
18     rep
19     ret
```

*图 3-51：浮点代码中的条件分支说明。*

GCC 为 `find_range` 生成的这段代码效率不是很高：它比较了 `x` 和 0.0 三次，即使一次比较就能获得所需的信息；它还两次生成了浮点常数 0.0，一次使用 `vxorps`，另一次从内存读出这个值。让我们追踪这个函数，看看四种可能的比较结果：

- `x < 0.0`：第 4 行的 `ja` 分支指令会选择跳转，跳转到结尾，返回值为 0。
- `x = 0.0`：`ja`（第 4 行）和 `jp`（第 6 行）两个分支语句都会选择不跳转，但 `je` 分支（第 8 行）会选择跳转，以 `%eax` 等于 1 返回。
- `x > 0.0`：这三个分支都不会选择跳转。`setbe`（第 11 行）会得到 0，`addl` 指令（第 13 行）会把它的值加上 2，得到返回值 2。
- `x` 为 NaN：`jp` 分支（第 6 行）会选择跳转。第三个 `vucomiss` 指令（第 10 行）会设置进位和零标志位，因此 `setbe` 指令（第 11 行）和后面的指令会把 `%eax` 设置为 1，`addl`（第 13 行）会把它的值加上 2，得到返回值 3。

---

## 参考资料

- [深入理解计算机系统（原书第3版） | GoClub](https://goclub.space/docs/resources/pdf-books/csapp-3e/)
