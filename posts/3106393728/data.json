{"title":"程序逻辑优化技巧","date":"2026-07-23T04:58:14.000Z","toc":true,"summary":"深入解析 C# 集合、Struct、对象池与字符串的底层优化技巧，涵盖 CPU 缓存命中与 GC 避免。","source":"_posts/程序逻辑优化技巧.md","raw":"---\ntitle: 程序逻辑优化技巧\ncategories: Unity 基础学习\ntoc: true\ndate: 2026-07-23 12:58:14\ntags:\n  - C#基础\nsummary: 深入解析 C# 集合、Struct、对象池与字符串的底层优化技巧，涵盖 CPU 缓存命中与 GC 避免。\ndescription: 深入解析 C# 集合、Struct、对象池与字符串的底层优化技巧，涵盖 CPU 缓存命中与 GC 避免。\n---\n\n---\n## 写在前面\n\n这个分类下的文章是我对 《Unity3D高级编程之进阶主程》一书的学习笔记。\n\n这个博客对应的章节是 \n\n- 第一章，程序逻辑优化技巧\n\n部分内容为ai生成，如有错误恳请指出。\n\n---\n\n## 一、使用 List 和 Dictionary\n\n`List` 和 `Dictionary` 是 C# 中最常用的集合类型，它们的底层都是由数组实现的。理解它们的内部机制，才能避免在性能敏感的场景中踩坑。\n\n### List\n\n`List` 在插入元素时，需要将插入位置之后的所有元素依次向后移动，因此 `Insert` 的时间复杂度是 **O(n)**。\n\n`Contains` 通过遍历数组来查找目标元素，时间复杂度也是 **O(n)**。\n\n`Remove` 同样需要先找到元素再移动后续元素，复杂度同样是 **O(n)**。\n\n> 因此，如果频繁调用 `Insert`、`Contains`、`Remove` 这些 O(n) 操作，尤其是在 List 容量较大的情况下，会带来很多不必要的性能消耗。在合适的场景下，可以考虑使用 `HashSet`、`Dictionary` 等 O(1) 查找的数据结构来替代。\n\n### Dictionary\n\n在编写程序时应注意给 `Dictionary` 设置一个合理的初始容量，而不是任由它自行扩容。扩容时的数组重新分配不仅会加重 **GC** 的负担，过小的容量还会让哈希冲突变得更加频繁，进一步导致性能损耗。\n\n此外，如果使用**类实例**作为 `Dictionary` 的 Key，`Dictionary` 会使用 `Object` 的 `GetHashCode()` 方法来获取类实例的哈希值。而 `GetHashCode()` 是通过算法将内存地址转化为哈希值的，它并不会缓存计算结果——每次调用都会重新计算一次，这是一种比较隐蔽的性能损耗。如果频繁通过 `GetHashCode` 来提取 Value，我们应该关注一下 `GetHashCode` 的计算开销，考虑是否可以用唯一 ID（如 `int` 或 `string`）的方式来代替 `GetHashCode` 算法。\n\n---\n\n## 二、使用 Struct\n\n`Struct` 是值类型。当作为局部变量使用时，`Struct` 分配的内存在**栈**上，生命周期结束后回收也十分简单——只需将栈的尾指针回退即可。与分配在堆上的类实例不同，值类型的分配和回收不会产生内存碎片，也不需要 GC 介入。\n\n另外，CPU 从栈上读取数据比从堆上读取数据更高效，因为栈内存具有更好的访问局部性。\n\n### CPU 缓存命中和 Struct 数组\n\nCPU 在读取内存时，会把连续的一大块内容加载到缓存中。从缓存读取数据比直接从内存读取快得多（可达 100 倍以上）。下次读取数据时，CPU 会先在缓存中查找，如果命中则无需再次访问内存。\n\n> 连续内存在 CPU 读取时，缓存的命中率高；而非连续内存则命中率较低。我们应该尽可能让 CPU 读取数据时命中缓存，从而提升访问效率。\n\n正因为如此，`Struct` 数组对提高内存访问速度很有帮助。由于 `Struct` 是值类型，`Struct` 数组中的所有元素都是连续存储的——每个元素的数据实实在在\"嵌\"在数组的连续内存空间中。而 Class 数组则不同：虽然数组中的引用（指针）变量是连续的，但每个引用所指向的、位于堆上的实际数据则像碎片一样分散在各处。\n\n### 使用值类型数组\n\n不过，并非所有 `Struct` 都能提高缓存命中率。如果一个 `Struct` 太大，超过了 CPU 一次缓存加载的数据块大小，那么缓存的效果就会大打折扣——因为一次加载的数据可能只包含一个甚至半个 Struct。因此，很多高性能架构彻底抛弃了在对象上直接存放数据的方式，转而使用**值类型连续数组**来提高 CPU 缓存命中率：将同一类数据集中到基本类型的数组中，而具体对象上只存放一个索引值，通过索引来间接访问数组中的数据。我们来看一个例子：\n\n```csharp\n// 传统 OOP 方式：每个对象持有自己的数据\nclass A\n{\n    public int a;\n    public float b;\n    public bool c;\n}\n\n// 改进方式：对象只持有索引，实际数据集中存储在数组中\nclass B\n{\n    public int index;\n}\n\n// 数据中心：将所有同类型数据以值类型数组的形式集中存放\nclass C\n{\n    private static C _instance;\n    public static C instance\n    {\n        get\n        {\n            if (null == _instance)\n            {\n                _instance = new C();\n            }\n            return _instance;\n        }\n    }\n\n    // 所有数据集中存放在值类型数组中，内存连续\n    public int[] a = new int[] { 2, 3, 5, 6 };\n    public float[] b = new float[] { 2.1f, 3.4f, 1.5f, 5.4f };\n    public bool[] c = new bool[] { false, true, false, true };\n}\n\npublic void Main()\n{\n    // A 类方式：引用数组连续，但实际对象数据分散在堆上\n    A[] arrayA = new A[3] { new A(), new A(), new A() };\n\n    Console.WriteLine(\"A class: a={0} b={1} c={2}\", arrayA[0].a, arrayA[0].b, arrayA[0].c);\n    \n    // B 类方式：对象只持有一个索引，通过索引访问集中存储的数据\n    B b = new B();\n    b.index = 2;\n\n    C c = C.instance;\n\n    // 数据访问时，通过索引去值类型数组中读取，所有数据在内存中连续\n    Console.WriteLine(\"B class: a={0} b={1} c={2}\", c.a[b.index], c.b[b.index], c.c[b.index]);\n}\n```\n\n上述代码中，`A` 类使用了我们非常熟悉的面向对象编程方式，把所有属性变量都放在自己身上，数据集合以引用的方式存储在数组中；而 `B` 类则将数据集中存放在了 `C` 类中，自己只存放一个索引值。\n\n当两者都对数据进行存取时，`A` 类的数据在内存中是分散的——因为每次 `new A()` 时，都是从堆内存中找一块空地来分配，并不保证相邻实例的内存连续，`arrayA` 中只是引用连续，而非数据本身连续。而 `B` 类的数据则存储在值类型的数组中，值类型数组在分配时一定是内存连续的，这样就能更多地利用缓存，提高 CPU 读取数据时的命中率。\n\n---\n\n## 三、对象池\n\n类实例在堆上分配内存后，当引用变量被置空或丢弃，这些内存就会交由**垃圾回收器（GC）**来回收。然而，内存分配本身是耗时的，垃圾回收的过程则更加昂贵。\n\n简单来说，垃圾回收的过程是：当内存不够用时，GC 会遍历所有已分配的内存块，检查哪些类实例已经没有任何变量引用，然后将其回收。如果回收后依然内存不足，就会向操作系统申请更多内存。很明显，业务逻辑越庞大、数据量越多，GC 需要检查的内容就越多。\n\n垃圾回收过程如此繁重，每次回收都会消耗大量 CPU 算力，因此我们不应该在 C# 中随意 `new` 创建类实例，然后又不管它们的死活随意丢弃或置空引用，让类实例不断地被创建和抛弃。\n\n> **销毁实例 vs 置空引用：它们对 GC 的影响有何不同？**  \n> 无论是将引用变量设为 `null`，还是让变量离开作用域被自然销毁，最终的效果是相同的——被引用的对象变得\"不可达\"。GC 会在下一次回收时标记这些不可达对象，并释放它们的内存。两者的区别在于时机：显式置空（`obj = null`）可以提前告诉 GC 该对象已不需要，让 GC 在下次回收时就能处理它；而离开作用域则是隐式的，在栈帧弹出后引用自然消失。对于**长生命周期**的对象（如静态字段、单例引用），如果某个被引用的对象不再需要了，应当显式地将引用置空——否则这个引用永远不会离开作用域，相关对象也会一直无法被回收。\n\n### 小心 List 的使用\n\n除了 `new` 某个类导致 GC 耗时增加外，以我的经验来看，很容易被我们忽略的是 `new List` 这种操作。我们在日常编程中大量使用动态数组，并且随时将它丢弃——因为它实在太容易创建和抛弃了。类似的，`Dictionary<int, List<T>>` 也是常被忽视的内存分配消耗来源之一：被装进 Dictionary 中的 List 经常被随意移除（Remove）并丢弃，却没有考虑过它是否能被再次利用。\n\n### Unity 提供的通用对象池工具\n\n我们应该尽可能使用**对象池**来重复利用已经创建的对象，而不是反复创建和销毁，这有助于减少内存分配的耗时，减少堆内存中的内存块数量，最终降低垃圾回收带来的 CPU 损耗。\n\n下面两个对象池的类是从 Unity 的 UI 库中提取出来的，都是非常实用的对象池工具，我们应该尽可能使用它们。\n\n```csharp\ninternal class ObjectPool<T> where T : new() // T 必须要有一个无参构造函数\n{\n    // 使用栈结构存储闲置对象，Push/Pop 操作均为 O(1)\n    private readonly Stack<T> m_Stack = new Stack<T>();\n    \n    // 从池中取出对象时的回调，可用于初始化对象状态\n    private readonly UnityAction<T> m_ActionOnGet;\n    \n    // 将对象归还池中时的回调，可用于重置对象状态\n    private readonly UnityAction<T> m_ActionOnRelease;\n\n    // 池总共创建过的对象数量\n    public int countAll { get; private set; }\n    // 当前正在使用的对象数量\n    public int countActive { get { return countAll - countInactive; } }\n    // 当前闲置在池中、可供取用的对象数量\n    public int countInactive { get { return m_Stack.Count; } }\n\n    public ObjectPool(UnityAction<T> actionOnGet, UnityAction<T> actionOnRelease)\n    {\n        m_ActionOnGet = actionOnGet;\n        m_ActionOnRelease = actionOnRelease;\n    }\n\n    // 从池中获取一个对象（优先复用闲置对象，没有则新建）\n    public T Get()\n    {\n        T element;\n        if (m_Stack.Count == 0)\n        {\n            // 池中无闲置对象，创建新实例并增加总数计数\n            element = new T();\n            countAll++;\n        }\n        else\n        {\n            // 从栈顶弹出一个闲置对象复用\n            element = m_Stack.Pop();\n        }\n        // 调用 Get 回调（如果有的话），例如重新初始化对象状态\n        if (m_ActionOnGet != null)\n            m_ActionOnGet(element);\n        return element;\n    }\n\n    // 将对象归还到池中\n    public void Release(T element)\n    {\n        // 简单的安全检查：防止连续重复归还同一个对象\n        if (m_Stack.Count > 0 && ReferenceEquals(m_Stack.Peek(), element))\n            Debug.LogError(\"Internal error. Trying to destroy object that is already released to pool.\");\n        // 调用 Release 回调（如果有的话），例如清空对象的状态\n        if (m_ActionOnRelease != null)\n            m_ActionOnRelease(element);\n        // 将对象压入栈中以供后续复用\n        m_Stack.Push(element);\n    }\n}\n\ninternal static class ListPool<T>\n{\n    // 为 List<T> 创建专用的对象池\n    // Get 时不需要额外操作，Release 时自动清空列表内容\n    private static readonly ObjectPool<List<T>> s_ListPool = new ObjectPool<List<T>>(null, l => l.Clear());\n\n    public static List<T> Get()\n    {\n        return s_ListPool.Get();\n    }\n\n    public static void Release(List<T> toRelease)\n    {\n        s_ListPool.Release(toRelease);\n    }\n}\n```\n\n上述对象池使用栈（`Stack`）作为存储容器，将废弃的对象存储起来，在需要时从栈顶推出实例交给使用者。栈的出栈和入栈都是 O(1) 操作，非常高效。\n\n对象池本身并不复杂，复杂的是使用——程序中所有需要创建对象实例、销毁对象实例、移除对象实例的地方，都需要用对象池来管理。我们来看几个使用上述 `ObjectPool` 和 `ListPool` 的例子：\n\n```csharp\npublic class A\n{\n    public int a;\n    public float b;\n}\n\n// 为 A 类型创建专用的对象池实例\ninternal static class PoolA\n{\n    private static readonly ObjectPool<A> s_Pool = new ObjectPool<A>(null, null);\n\n    public static A Get() => s_Pool.Get();\n    public static void Release(A obj) => s_Pool.Release(obj);\n}\n\npublic void Main()\n{\n    // 示例 1：在 Dictionary 中管理对象池中的对象\n    Dictionary<int, A> dic = new Dictionary<int, A>(16);\n    for (int i = 0; i < 1000; i++)\n    {\n        // 从对象池中获取 A 实例，避免 new A() 导致的堆内存分配\n        A a = PoolA.Get();\n        a.a = i % 10;   // 键在 0-9 之间循环，因此会重复命中同一个键\n        a.b = 3.5f;\n\n        A item = null;\n        if (dic.TryGetValue(a.a, out item))\n        {\n            // 键已存在（键会循环重复），覆盖前将旧对象归还对象池\n            PoolA.Release(item);\n        }\n\n        dic[a.a] = a;\n\n        // 随机移除某个键，被移除的对象归还对象池\n        int removeKey = Random.RangeInt(0, 10);\n        if (dic.TryGetValue(removeKey, out item))\n        {\n            PoolA.Release(item);\n            dic.Remove(removeKey);\n        }\n    }\n\n    // 示例 2：使用 ListPool 管理 List 的生命周期\n    Dictionary<int, List<A>> dic2 = new Dictionary<int, List<A>>(1000);\n    for (int i = 0; i < 1000; i++)\n    {\n        // 从 ListPool 中获取 List 实例，避免 new List<A>() 导致的堆内存分配\n        List<A> arrayA = ListPool<A>.Get();\n\n        dic2.Add(i, arrayA);\n\n        List<A> item = null;\n        int removeKey = Random.RangeInt(0, 1000);\n        if (dic2.TryGetValue(removeKey, out item))\n        {\n            // 移除时将 List 归还对象池，Release 时 ListPool 会自动调用 Clear() 清空列表\n            ListPool<A>.Release(item);\n            dic2.Remove(removeKey);\n        }\n    }\n}\n```\n\n上述代码中，`A` 类和 `List<A>` 各有 1000 次创建需求，每次创建都使用对象池，并在 Dictionary 移除时将对象送还对象池。这样一来，我们就能不断复用被\"废弃\"的对象，而不需要总是创建新的。所有被归还的对象都会被存储在池中，不会被垃圾回收器回收，最终内存被不断重复利用，从而减少了内存分配和释放带来的消耗。\n\n---\n\n## 四、字符串 String\n\n字符串的性能问题在大部分语言中都是比较棘手的问题，C# 中尤其如此。在 C# 中 `string` 是引用类型，每次动态创建一个字符串时，C# 都会在堆内存中分配一块内存来存放字符串内容。我们来看看它到底有多\"恐怖\"。\n\n```csharp\nstring strA = \"test\";\nfor (int i = 0; i < 100; i++)\n{\n    string strB = strA + i.ToString();        // 第 1 次内存分配：连接产生新字符串\n\n    string[] strC = strB.Split('e');           // 第 2、3 次内存分配：分割产生两个新字符串\n\n    strB = strB + strC[0];                     // 第 4 次内存分配：连接产生新字符串\n\n    string strD = string.Format(\"Hello {0}, this is {1} and {2}.\", strB, strC[0], strC[1]);\n    // 第 5 次内存分配：Format 产生新字符串\n}\n```\n\n这是一段\"恐怖\"的程序。循环中每一次迭代做了以下事情：\n\n1. 将 `strA` 字符串和 `i` 的整数字符串连接，`strB` 获得的是从内存中**新分配**的字符串；\n2. 将 `strB` 按 `'e'` 切割成 `strC`，切割出的两个字符串又**新分配**了两段内存；\n3. 将 `strB` 与 `strC[0]` 连接，又再**申请**了一段内存来存放连接后的内容，并赋值给 `strB`，`strB` 原来指向的内容因为没有变量引用就被\"丢弃\"了；\n4. 最后用 `string.Format` 将 4 个字符串组合起来，又**新分配**了一段内存来存放组合后的内容。\n\n这里需要注意一点：**字符串常量不会被 GC 回收**。比如这段程序中的 `\"test\"` 和 `\"Hello {0}, this is {1} and {2}.\"` 这两个常量，它们常驻于内存——即使没有变量指向它们，它们也不会被回收；下次使用时也不需要重新分配内存。\n\n> **字符串常量与字符串驻留（String Interning）**  \n> 在 C# 中，编译期的字符串字面量会被自动**驻留（Intern）**到 CLR 管理的字符串驻留池（Intern Pool）中。驻留池是一个全局的哈希表，存储了所有唯一的字符串字面量。当代码中出现相同的字符串字面量时，编译器会让所有引用指向驻留池中的同一块内存。这就是为什么 `Object.ReferenceEquals(\"hello\", \"hello\")` 会返回 `true`——两个 `\"hello\"` 指向的是同一个驻留字符串。  \n> 动态创建的字符串（如通过拼接、`StringBuilder` 等生成的字符串）**不会**被自动驻留。如果希望手动将动态字符串加入驻留池，可以调用 `string.Intern()` 方法。需要注意的是，驻留的字符串在应用程序域的整个生命周期内都不会被 GC 回收，因此不要滥用驻留功能，否则可能导致内存持续增长。\n\n每次循环中，这段代码向内存申请了 5 次新的内存段，并且抛弃了 1 次（`strA + i.ToString()` 的结果因为没有变量指向而被丢弃）。这还不是最糟糕的——最糟糕的是，每次循环结束都会将前面分配的所有临时内存内容全部抛弃，重新再分配一次。如此反复申请和抛弃，总共向内存申请了约 500 次内存段，并全部抛弃，**内存被浪费得非常严重**。\n\n为什么会这样呢？究其原因，C# 对字符串**没有任何缓存机制**，每次操作都需要重新分配 `string` 内容。据我所知，很多语言都没有字符串的缓存机制，因此我们的字符串连接、切割、组合等操作都会向内存申请新的内容，并抛弃没有变量指向的旧字符串，等待 GC 回收。我们知道 GC 一次回收会消耗大量 CPU，如果我们不注意字符串的使用方式、不断浪费内存，将导致程序不定时卡顿。并且随着程序运行时间越来越长、各模块中不良代码的积累，程序卡顿的频率会逐渐升高，运行效率也会越来越差。\n\n### 如何解决字符串问题\n\n#### 1. 自建缓存机制\n\n可以用一些标志性的 Key 键值来一一对应字符串，比如游戏项目中常用 ID 来构造某个字符串：\n\n```csharp\nint ID = 101;\n\nResData resData = GetDataById(ID);\n\nstring strName = \"This is \" + resData.Name;\n\nreturn strName;\n```\n\n> 一个 ID 对应一个字符串的场景，我们可以建立一个字典容器将其缓存起来，下次使用时就不需要重新申请内存了。\n\n```csharp\nDictionary<int, string> strCache;\n\nstring strName = null;\nif (!strCache.TryGetValue(id, out strName))\n{\n    // 缓存中不存在，创建并存入缓存\n    ResData resData = GetDataById(id);\n    strName = \"This is \" + resData.Name;\n    strCache.Add(id, strName);\n}\n\nreturn strName;\n```\n\n我们用 `Dictionary` 容器将字符串缓存起来，每次先查询字典中是否存在，有则直接使用，没有则创建一个并将其存入字典，以便下次复用。\n\n#### 2. 使用不安全代码直接操作内存\n\n由于 `string` 类本身的任何修改操作都会申请新的内存，因此我们需要突破这个限制——直接使用指针来修改 `string` 中字符的值，这样就能重复利用已分配的 `string` 内存，不需要反复重新分配。\n\nC# 虽然托管了大部分内存内容，但它也允许我们使用**非托管方式**来直接访问和修改内存——这就是 `unsafe` 关键字的用途。通过非托管方式，我们可以绕过 `string` 的不可变性，直接改变其中的字符内容，使已分配的字符串内存能被重复利用。来看一个例子：\n\n```csharp\nstring strA = \"aaa\";\n\n// 使用 \"bbb\" + \"b\" 的方式生成新字符串，是为了避免修改常量字符串内存块\nstring strB = \"bbb\" + \"b\";\n\n// 使用 fixed 关键字固定指针所指向的对象的内存\nfixed (char* strA_ptr = strA)\n{\n    fixed (char* strB_ptr = strB)\n    {\n        // 将 strA 的前 3 个字符内容直接拷贝到 strB 的内存中\n        memcopy((byte*)strB_ptr, (byte*)strA_ptr, 3 * sizeof(char));\n    }\n}\n\nConsole.WriteLine(strB); // 此时 strB 内容变为 \"aaab\"\n```\n\n> 注意：这里特意用 `\"bbb\" + \"b\"` 而不是直接写 `\"bbbb\"`，是为了确保 `strB` 不是常量字符串内存块，而是运行时期动态分配的新字符串，这样我们修改它的内容才是安全的。\n\n其中 `memcopy` 并非系统函数，需要自己实现，写法也很简单——拿到两个指针，根据长度遍历并逐字节赋值：\n\n```csharp\npublic unsafe void memcopy(byte* dest, byte* src, int len)\n{\n    while ((--len) >= 0)\n    {\n        dest[len] = src[len];\n    }\n}\n```\n\n> **注意**：使用 `unsafe` 代码需要在项目设置中启用 \"Allow unsafe code\" 选项，且这类代码绕过了 C# 的内存安全检查，使用时需格外谨慎。\n\n我们把 `strB` 的前 3 个字符的内容替换成了 `strA` 中的内容，整个过程并没有增加任何新内存，因为我们使用了不安全的非托管方法直接操控内存。通过这种方式，我们可以将已分配的 `string` 缓存起来重复利用。下面来看一个实际应用的例子：\n\n```csharp\n// 按字符串长度缓存可复用的字符串\nDictionary<int, string> cacheStr;\n\npublic unsafe string Concat(string strA, string strB)\n{\n    int a_length = strA.Length;\n    int b_length = strB.Length;\n    int sum_length = a_length + b_length;\n\n    string strResult = null;\n\n    if (!cacheStr.TryGetValue(sum_length, out strResult))\n    {\n        // 缓存中不存在该长度的字符串，直接连接后存入缓存\n        strResult = strA + strB;\n        cacheStr.Add(sum_length, strResult);\n        return strResult;\n    }\n\n    // 缓存中存在可用字符串，用指针直接修改其内容\n    fixed (char* strA_ptr = strA)\n    {\n        fixed (char* strB_ptr = strB)\n        {\n            fixed (char* strResult_ptr = strResult)\n            {\n                // 将 strA 的内容拷贝到 strResult 的开头\n                memcopy((byte*)strResult_ptr, (byte*)strA_ptr, a_length * sizeof(char));\n\n                // 将 strB 的内容拷贝到 strResult 中 a_length 偏移之后的位置\n                memcopy((byte*)strResult_ptr + a_length * sizeof(char), (byte*)strB_ptr, b_length * sizeof(char));\n            }\n        }\n    }\n\n    return strResult;\n}\n```\n\n当需要将两个字符串连接起来时，先在缓存中查找是否有可用长度的字符串：没有则直接连接并缓存；有则取出来，用指针方式直接修改缓存字符串的值。\n\n#### 3. C# 自带方案\n\n上述 `unsafe` 方案的核心思想是\"预分配一块可复用的缓冲区，用的时候直接往里面写内容\"，这是一种教学性质的极端优化演示。在实际开发中，C# 自身已经提供了更安全、更易用的替代方案，其内核与我们手动实现的方式完全一致。\n\n##### StringBuilder 缓存\n\n`StringBuilder` 内部维护一个 `char[]` 缓冲区，其工作原理与上述手动按长度缓存 `string` 的思路如出一辙：`Append` 往缓冲区末尾填字符，`Clear()` 只把内部写指针归零，**不释放底层的 `char[]` 数组**，下次使用时直接覆盖写入，零额外分配。\n\n我们不需要为 `StringBuilder` 实现任何包装——只需要把 `StringBuilder` 实例本身缓存起来即可：\n\n```csharp\n// 缓存一个 StringBuilder 实例并反复使用\nStringBuilder sb = new StringBuilder(256);  // 预估合理初始容量，避免内部数组扩容\n\nsb.Append(\"Hello \");\nsb.Append(name);\nsb.Append(\", this is \");\nsb.Append(123);\n\nstring result = sb.ToString();\n\nsb.Clear();  // 清空后下次直接复用，不产生新内存分配\n```\n\n> **注意**：`sb.ToString()` 仍然会产生一次新的 `string` 分配（这是不可避免的，因为你需要把结果拿出去）。但拼接过程中的中间字符串分配被完全消除了——这正是 `StringBuilder` 相比 `+` 拼接的优势所在。\n\n在 Unity 项目中，可以结合对象池来做更全面的管理：\n\n```csharp\ninternal static class StringBuilderPool\n{\n    // 使用前面介绍的 ObjectPool 来管理 StringBuilder 实例\n    private static readonly ObjectPool<StringBuilder> s_Pool =\n        new ObjectPool<StringBuilder>(null, sb => sb.Clear());\n\n    public static StringBuilder Get() => s_Pool.Get();\n    public static void Release(StringBuilder sb) => s_Pool.Release(sb);\n}\n```\n\n每次需要拼接字符串时从池中取，用完后归还，避免反复创建 `StringBuilder` 实例。以下是一个实际使用的例子：\n\n```csharp\n// 模拟每帧生成日志字符串的场景\nvoid BuildLogLine(string playerName, int score)\n{\n    StringBuilder sb = StringBuilderPool.Get();  // 从池中取出\n\n    sb.Append(\"[\");\n    sb.Append(playerName);\n    sb.Append(\"]  score: \");\n    sb.Append(score);\n    sb.Append(\", rank: \");\n    sb.Append(GetRank(score));\n\n    string log = sb.ToString();                   // 转为 string 输出\n\n    StringBuilderPool.Release(sb);               // 用完归还池中，Release 回调自动 Clear\n    // 之后同一实例可被其他调用者复用，无需 new StringBuilder()\n}\n```\n\n> 这个场景下，池中只需维持少数几个 `StringBuilder` 实例即可覆盖所有调用，对象复用率极高。\n\n> `StringBuilder` 的初始容量建议根据实际场景预估。如果预估容量偏小，内部 `char[]` 扩容时会产生一次数组拷贝——这同样是内存分配，应尽量避免。如果不确定容量，宁可稍微偏大一些。\n\n##### string.Intern 驻留池\n\n对于**字符串种类有限但反复出现**的场景（比如配置表 key、网络协议字段名、资源路径等），可以利用 C# 自带的字符串驻留池（Intern Pool）：\n\n```csharp\n// 将动态字符串加入驻留池（如果池中已存在同内容的字符串，返回已有引用）\nstring cached = string.Intern(someDynamicString);\n\n// 仅查找驻留池，不自动加入（不存在则返回 null）\nstring existing = string.IsInterned(someDynamicString);\n```\n\n驻留之后，所有相同内容的字符串都指向同一块内存，后续的 `==` 比较会命中 `ReferenceEquals` 而**直接返回 `true`**，彻底避免逐字符遍历——这正是后文\"字符串比较的隐藏开销\"一节中提到的优化方向。\n\n> **注意**：驻留池的字符串在整个应用程序域生命周期内都不会被 GC 回收，因此只适合\"种类有限\"的场景。不要对高度动态变化、种类无穷的字符串（如用户输入、随机生成的内容）大量使用 `string.Intern`，否则驻留池会持续膨胀，反而导致内存泄漏。\n\n| 方案 | 安全性 | 分配次数 | 适用场景 |\n|------|--------|----------|----------|\n| `StringBuilder` 缓存 | ✅ 安全 | 仅 `ToString()` 时 1 次 | 拼接、格式化等通用场景 |\n| `string.Intern` | ✅ 安全 | 0（复用已有引用） | 种类有限的整串缓存（Key / 路径） |\n| 手动 `unsafe` + 缓存 | ⚠️ 不安全 | 0 | 极端优化场景，不建议日常使用 |\n\n### 字符串的其他隐藏问题\n\n#### Length 属性并非 O(1)\n\n`string` 的 `Length` 属性可不是省油的灯。由于 C# 的 `string` 类中只存放了 `char[]` 数据，没有单独存储长度值的字段，因此取长度时需要通过遍历字符串来找到结尾的 `\\0` 字符。类似于如下伪代码：\n\n```csharp\nint sum = 0;\nfor (; data[sum] != '\\0'; ++sum) ;\nreturn sum;\n```\n\n> 如果我们大量使用 `Length`，就会浪费很多 CPU，因为每次调用它都会遍历整个字符串。好在实际开发中，我们通常不会在循环中反复访问同一字符串的 `Length`——一种简单的优化是将其缓存到局部变量中。\n\n#### 字符串比较的隐藏开销\n\n字符串比较也有隐藏的性能问题。当两个字符串进行比较时，`string` 的 `Equals` 方法首先会比较两个字符串的**引用指针**是否一致，一致则直接返回 `true`。如果指针不一致，则会先比较两者的**长度**是否相等，长度不同则直接返回 `false`；长度相同才会逐个遍历每一个字符，判断它们是否全部相等。我们来看看 `Equals` 的实际源码：\n\n```csharp\npublic bool Equals(String value)\n{\n    if (this == null)                        // 防止 reverse-pinvoke 等场景的空引用\n        throw new NullReferenceException();\n\n    if (value == null)\n        return false;\n\n    if (Object.ReferenceEquals(this, value)) // 第一步：比较引用是否相同\n        return true;\n    \n    if (this.Length != value.Length)         // 第二步：比较长度是否相同\n        return false;\n\n    return EqualsHelper(this, value);        // 第三步：逐字符遍历比较\n}\n```\n\n> 这段代码从源码中提取。在判空之后，依次进行：引用比较 → 长度比较 → 逐字符遍历。只有当前两步都无法得出结论时，才会进入最昂贵的逐字符比较。\n\n这意味着，如果两个字符串内容相同但来自不同的内存段（例如分别通过拼接得到），那么比较它们时就不得不遍历所有字符。如下示例：\n\n```csharp\nstring strA = \"Hello \";\nstring strB = \"Hello \";\n\nstrA = strA + \"C\";   // strA 在堆上新分配一块内存\nstrB = strB + \"C\";   // strB 也在堆上新分配一块内存（与 strA 不同）\n\n// 虽然 strA 和 strB 内容完全相同（都是 \"Hello C\"），但它们位于不同的内存段\n// 引用比较（ReferenceEquals）会返回 false\n// 因此 Equals 会进入逐字符遍历比较\nif (strA == strB)\n{\n    Console.WriteLine(\"相等\"); // 最终结果：相等，但经过了昂贵的遍历比较\n}\n```\n\n> 在这个例子中，`strA` 和 `strB` 内容完全相同，但由于分别通过拼接产生，位于不同的内存地址，因此 `ReferenceEquals` 返回 `false`，`Equals` 不得不进入逐字符遍历。如果这样的比较频繁发生，累积的性能损耗也不容忽视。将字符串缓存起来（如前文所述），让相同内容的字符串指向同一块内存，就能让 `ReferenceEquals` 快速返回 `true`，从而避免遍历。\n\n---\n\n## 参考资料\n\n- [《Unity3D高级编程之进阶主程》 第一章，程序逻辑优化技巧 - 技术人生 - 编程技术 - JESSE人生](http://www.luzexi.com/2019/12/31/Unity3D%E9%AB%98%E7%BA%A7%E7%BC%96%E7%A8%8B%E4%B9%8B%E8%BF%9B%E9%98%B6%E4%B8%BB%E7%A8%8B-%E7%A8%8B%E5%BA%8F%E9%80%BB%E8%BE%91%E4%BC%98%E5%8C%96%E6%8A%80%E5%B7%A7)\n","slug":"程序逻辑优化技巧","published":true,"updated":"2026-07-24T06:59:58.090Z","_id":"cuidM2caLxkXNJRinz1csjkBt","comments":true,"layout":"post","photos":[],"html":"<hr>\n<h2 id=\"写在前面\"><a href=\"#写在前面\" class=\"headerlink\" title=\"写在前面\"></a>写在前面</h2><p>这个分类下的文章是我对 《Unity3D高级编程之进阶主程》一书的学习笔记。</p>\n<p>这个博客对应的章节是 </p>\n<ul>\n<li>第一章，程序逻辑优化技巧</li>\n</ul>\n<p>部分内容为ai生成，如有错误恳请指出。</p>\n<hr>\n<h2 id=\"一、使用-List-和-Dictionary\"><a href=\"#一、使用-List-和-Dictionary\" class=\"headerlink\" title=\"一、使用 List 和 Dictionary\"></a>一、使用 List 和 Dictionary</h2><p><code>List</code> 和 <code>Dictionary</code> 是 C# 中最常用的集合类型，它们的底层都是由数组实现的。理解它们的内部机制，才能避免在性能敏感的场景中踩坑。</p>\n<h3 id=\"List\"><a href=\"#List\" class=\"headerlink\" title=\"List\"></a>List</h3><p><code>List</code> 在插入元素时，需要将插入位置之后的所有元素依次向后移动，因此 <code>Insert</code> 的时间复杂度是 <strong>O(n)</strong>。</p>\n<p><code>Contains</code> 通过遍历数组来查找目标元素，时间复杂度也是 <strong>O(n)</strong>。</p>\n<p><code>Remove</code> 同样需要先找到元素再移动后续元素，复杂度同样是 <strong>O(n)</strong>。</p>\n<blockquote>\n<p>因此，如果频繁调用 <code>Insert</code>、<code>Contains</code>、<code>Remove</code> 这些 O(n) 操作，尤其是在 List 容量较大的情况下，会带来很多不必要的性能消耗。在合适的场景下，可以考虑使用 <code>HashSet</code>、<code>Dictionary</code> 等 O(1) 查找的数据结构来替代。</p>\n</blockquote>\n<h3 id=\"Dictionary\"><a href=\"#Dictionary\" class=\"headerlink\" title=\"Dictionary\"></a>Dictionary</h3><p>在编写程序时应注意给 <code>Dictionary</code> 设置一个合理的初始容量，而不是任由它自行扩容。扩容时的数组重新分配不仅会加重 <strong>GC</strong> 的负担，过小的容量还会让哈希冲突变得更加频繁，进一步导致性能损耗。</p>\n<p>此外，如果使用<strong>类实例</strong>作为 <code>Dictionary</code> 的 Key，<code>Dictionary</code> 会使用 <code>Object</code> 的 <code>GetHashCode()</code> 方法来获取类实例的哈希值。而 <code>GetHashCode()</code> 是通过算法将内存地址转化为哈希值的，它并不会缓存计算结果——每次调用都会重新计算一次，这是一种比较隐蔽的性能损耗。如果频繁通过 <code>GetHashCode</code> 来提取 Value，我们应该关注一下 <code>GetHashCode</code> 的计算开销，考虑是否可以用唯一 ID（如 <code>int</code> 或 <code>string</code>）的方式来代替 <code>GetHashCode</code> 算法。</p>\n<hr>\n<h2 id=\"二、使用-Struct\"><a href=\"#二、使用-Struct\" class=\"headerlink\" title=\"二、使用 Struct\"></a>二、使用 Struct</h2><p><code>Struct</code> 是值类型。当作为局部变量使用时，<code>Struct</code> 分配的内存在<strong>栈</strong>上，生命周期结束后回收也十分简单——只需将栈的尾指针回退即可。与分配在堆上的类实例不同，值类型的分配和回收不会产生内存碎片，也不需要 GC 介入。</p>\n<p>另外，CPU 从栈上读取数据比从堆上读取数据更高效，因为栈内存具有更好的访问局部性。</p>\n<h3 id=\"CPU-缓存命中和-Struct-数组\"><a href=\"#CPU-缓存命中和-Struct-数组\" class=\"headerlink\" title=\"CPU 缓存命中和 Struct 数组\"></a>CPU 缓存命中和 Struct 数组</h3><p>CPU 在读取内存时，会把连续的一大块内容加载到缓存中。从缓存读取数据比直接从内存读取快得多（可达 100 倍以上）。下次读取数据时，CPU 会先在缓存中查找，如果命中则无需再次访问内存。</p>\n<blockquote>\n<p>连续内存在 CPU 读取时，缓存的命中率高；而非连续内存则命中率较低。我们应该尽可能让 CPU 读取数据时命中缓存，从而提升访问效率。</p>\n</blockquote>\n<p>正因为如此，<code>Struct</code> 数组对提高内存访问速度很有帮助。由于 <code>Struct</code> 是值类型，<code>Struct</code> 数组中的所有元素都是连续存储的——每个元素的数据实实在在”嵌”在数组的连续内存空间中。而 Class 数组则不同：虽然数组中的引用（指针）变量是连续的，但每个引用所指向的、位于堆上的实际数据则像碎片一样分散在各处。</p>\n<h3 id=\"使用值类型数组\"><a href=\"#使用值类型数组\" class=\"headerlink\" title=\"使用值类型数组\"></a>使用值类型数组</h3><p>不过，并非所有 <code>Struct</code> 都能提高缓存命中率。如果一个 <code>Struct</code> 太大，超过了 CPU 一次缓存加载的数据块大小，那么缓存的效果就会大打折扣——因为一次加载的数据可能只包含一个甚至半个 Struct。因此，很多高性能架构彻底抛弃了在对象上直接存放数据的方式，转而使用<strong>值类型连续数组</strong>来提高 CPU 缓存命中率：将同一类数据集中到基本类型的数组中，而具体对象上只存放一个索引值，通过索引来间接访问数组中的数据。我们来看一个例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br><span class=\"line\">26</span><br><span class=\"line\">27</span><br><span class=\"line\">28</span><br><span class=\"line\">29</span><br><span class=\"line\">30</span><br><span class=\"line\">31</span><br><span class=\"line\">32</span><br><span class=\"line\">33</span><br><span class=\"line\">34</span><br><span class=\"line\">35</span><br><span class=\"line\">36</span><br><span class=\"line\">37</span><br><span class=\"line\">38</span><br><span class=\"line\">39</span><br><span class=\"line\">40</span><br><span class=\"line\">41</span><br><span class=\"line\">42</span><br><span class=\"line\">43</span><br><span class=\"line\">44</span><br><span class=\"line\">45</span><br><span class=\"line\">46</span><br><span class=\"line\">47</span><br><span class=\"line\">48</span><br><span class=\"line\">49</span><br><span class=\"line\">50</span><br><span class=\"line\">51</span><br><span class=\"line\">52</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 传统 OOP 方式：每个对象持有自己的数据</span></span><br><span class=\"line\"><span class=\"keyword\">class</span> <span class=\"title\">A</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> a;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">float</span> b;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">bool</span> c;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 改进方式：对象只持有索引，实际数据集中存储在数组中</span></span><br><span class=\"line\"><span class=\"keyword\">class</span> <span class=\"title\">B</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> index;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 数据中心：将所有同类型数据以值类型数组的形式集中存放</span></span><br><span class=\"line\"><span class=\"keyword\">class</span> <span class=\"title\">C</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">static</span> C _instance;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"keyword\">static</span> C instance</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"keyword\">get</span></span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"keyword\">if</span> (<span class=\"literal\">null</span> == _instance)</span><br><span class=\"line\">            &#123;</span><br><span class=\"line\">                _instance = <span class=\"keyword\">new</span> C();</span><br><span class=\"line\">            &#125;</span><br><span class=\"line\">            <span class=\"keyword\">return</span> _instance;</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 所有数据集中存放在值类型数组中，内存连续</span></span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span>[] a = <span class=\"keyword\">new</span> <span class=\"built_in\">int</span>[] &#123; <span class=\"number\">2</span>, <span class=\"number\">3</span>, <span class=\"number\">5</span>, <span class=\"number\">6</span> &#125;;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">float</span>[] b = <span class=\"keyword\">new</span> <span class=\"built_in\">float</span>[] &#123; <span class=\"number\">2.1f</span>, <span class=\"number\">3.4f</span>, <span class=\"number\">1.5f</span>, <span class=\"number\">5.4f</span> &#125;;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">bool</span>[] c = <span class=\"keyword\">new</span> <span class=\"built_in\">bool</span>[] &#123; <span class=\"literal\">false</span>, <span class=\"literal\">true</span>, <span class=\"literal\">false</span>, <span class=\"literal\">true</span> &#125;;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">void</span> <span class=\"title\">Main</span>()</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// A 类方式：引用数组连续，但实际对象数据分散在堆上</span></span><br><span class=\"line\">    A[] arrayA = <span class=\"keyword\">new</span> A[<span class=\"number\">3</span>] &#123; <span class=\"keyword\">new</span> A(), <span class=\"keyword\">new</span> A(), <span class=\"keyword\">new</span> A() &#125;;</span><br><span class=\"line\"></span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">&quot;A class: a=&#123;0&#125; b=&#123;1&#125; c=&#123;2&#125;&quot;</span>, arrayA[<span class=\"number\">0</span>].a, arrayA[<span class=\"number\">0</span>].b, arrayA[<span class=\"number\">0</span>].c);</span><br><span class=\"line\">    </span><br><span class=\"line\">    <span class=\"comment\">// B 类方式：对象只持有一个索引，通过索引访问集中存储的数据</span></span><br><span class=\"line\">    B b = <span class=\"keyword\">new</span> B();</span><br><span class=\"line\">    b.index = <span class=\"number\">2</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">    C c = C.instance;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 数据访问时，通过索引去值类型数组中读取，所有数据在内存中连续</span></span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">&quot;B class: a=&#123;0&#125; b=&#123;1&#125; c=&#123;2&#125;&quot;</span>, c.a[b.index], c.b[b.index], c.c[b.index]);</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>上述代码中，<code>A</code> 类使用了我们非常熟悉的面向对象编程方式，把所有属性变量都放在自己身上，数据集合以引用的方式存储在数组中；而 <code>B</code> 类则将数据集中存放在了 <code>C</code> 类中，自己只存放一个索引值。</p>\n<p>当两者都对数据进行存取时，<code>A</code> 类的数据在内存中是分散的——因为每次 <code>new A()</code> 时，都是从堆内存中找一块空地来分配，并不保证相邻实例的内存连续，<code>arrayA</code> 中只是引用连续，而非数据本身连续。而 <code>B</code> 类的数据则存储在值类型的数组中，值类型数组在分配时一定是内存连续的，这样就能更多地利用缓存，提高 CPU 读取数据时的命中率。</p>\n<hr>\n<h2 id=\"三、对象池\"><a href=\"#三、对象池\" class=\"headerlink\" title=\"三、对象池\"></a>三、对象池</h2><p>类实例在堆上分配内存后，当引用变量被置空或丢弃，这些内存就会交由**垃圾回收器（GC）**来回收。然而，内存分配本身是耗时的，垃圾回收的过程则更加昂贵。</p>\n<p>简单来说，垃圾回收的过程是：当内存不够用时，GC 会遍历所有已分配的内存块，检查哪些类实例已经没有任何变量引用，然后将其回收。如果回收后依然内存不足，就会向操作系统申请更多内存。很明显，业务逻辑越庞大、数据量越多，GC 需要检查的内容就越多。</p>\n<p>垃圾回收过程如此繁重，每次回收都会消耗大量 CPU 算力，因此我们不应该在 C# 中随意 <code>new</code> 创建类实例，然后又不管它们的死活随意丢弃或置空引用，让类实例不断地被创建和抛弃。</p>\n<blockquote>\n<p><strong>销毁实例 vs 置空引用：它们对 GC 的影响有何不同？</strong><br>无论是将引用变量设为 <code>null</code>，还是让变量离开作用域被自然销毁，最终的效果是相同的——被引用的对象变得”不可达”。GC 会在下一次回收时标记这些不可达对象，并释放它们的内存。两者的区别在于时机：显式置空（<code>obj = null</code>）可以提前告诉 GC 该对象已不需要，让 GC 在下次回收时就能处理它；而离开作用域则是隐式的，在栈帧弹出后引用自然消失。对于<strong>长生命周期</strong>的对象（如静态字段、单例引用），如果某个被引用的对象不再需要了，应当显式地将引用置空——否则这个引用永远不会离开作用域，相关对象也会一直无法被回收。</p>\n</blockquote>\n<h3 id=\"小心-List-的使用\"><a href=\"#小心-List-的使用\" class=\"headerlink\" title=\"小心 List 的使用\"></a>小心 List 的使用</h3><p>除了 <code>new</code> 某个类导致 GC 耗时增加外，以我的经验来看，很容易被我们忽略的是 <code>new List</code> 这种操作。我们在日常编程中大量使用动态数组，并且随时将它丢弃——因为它实在太容易创建和抛弃了。类似的，<code>Dictionary&lt;int, List&lt;T&gt;&gt;</code> 也是常被忽视的内存分配消耗来源之一：被装进 Dictionary 中的 List 经常被随意移除（Remove）并丢弃，却没有考虑过它是否能被再次利用。</p>\n<h3 id=\"Unity-提供的通用对象池工具\"><a href=\"#Unity-提供的通用对象池工具\" class=\"headerlink\" title=\"Unity 提供的通用对象池工具\"></a>Unity 提供的通用对象池工具</h3><p>我们应该尽可能使用<strong>对象池</strong>来重复利用已经创建的对象，而不是反复创建和销毁，这有助于减少内存分配的耗时，减少堆内存中的内存块数量，最终降低垃圾回收带来的 CPU 损耗。</p>\n<p>下面两个对象池的类是从 Unity 的 UI 库中提取出来的，都是非常实用的对象池工具，我们应该尽可能使用它们。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br><span class=\"line\">26</span><br><span class=\"line\">27</span><br><span class=\"line\">28</span><br><span class=\"line\">29</span><br><span class=\"line\">30</span><br><span class=\"line\">31</span><br><span class=\"line\">32</span><br><span class=\"line\">33</span><br><span class=\"line\">34</span><br><span class=\"line\">35</span><br><span class=\"line\">36</span><br><span class=\"line\">37</span><br><span class=\"line\">38</span><br><span class=\"line\">39</span><br><span class=\"line\">40</span><br><span class=\"line\">41</span><br><span class=\"line\">42</span><br><span class=\"line\">43</span><br><span class=\"line\">44</span><br><span class=\"line\">45</span><br><span class=\"line\">46</span><br><span class=\"line\">47</span><br><span class=\"line\">48</span><br><span class=\"line\">49</span><br><span class=\"line\">50</span><br><span class=\"line\">51</span><br><span class=\"line\">52</span><br><span class=\"line\">53</span><br><span class=\"line\">54</span><br><span class=\"line\">55</span><br><span class=\"line\">56</span><br><span class=\"line\">57</span><br><span class=\"line\">58</span><br><span class=\"line\">59</span><br><span class=\"line\">60</span><br><span class=\"line\">61</span><br><span class=\"line\">62</span><br><span class=\"line\">63</span><br><span class=\"line\">64</span><br><span class=\"line\">65</span><br><span class=\"line\">66</span><br><span class=\"line\">67</span><br><span class=\"line\">68</span><br><span class=\"line\">69</span><br><span class=\"line\">70</span><br><span class=\"line\">71</span><br><span class=\"line\">72</span><br><span class=\"line\">73</span><br><span class=\"line\">74</span><br><span class=\"line\">75</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">internal</span> <span class=\"keyword\">class</span> <span class=\"title\">ObjectPool</span>&lt;<span class=\"title\">T</span>&gt; <span class=\"keyword\">where</span> <span class=\"title\">T</span> : <span class=\"title\">new</span>() <span class=\"comment\">// T 必须要有一个无参构造函数</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 使用栈结构存储闲置对象，Push/Pop 操作均为 O(1)</span></span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">readonly</span> Stack&lt;T&gt; m_Stack = <span class=\"keyword\">new</span> Stack&lt;T&gt;();</span><br><span class=\"line\">    </span><br><span class=\"line\">    <span class=\"comment\">// 从池中取出对象时的回调，可用于初始化对象状态</span></span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">readonly</span> UnityAction&lt;T&gt; m_ActionOnGet;</span><br><span class=\"line\">    </span><br><span class=\"line\">    <span class=\"comment\">// 将对象归还池中时的回调，可用于重置对象状态</span></span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">readonly</span> UnityAction&lt;T&gt; m_ActionOnRelease;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 池总共创建过的对象数量</span></span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> countAll &#123; <span class=\"keyword\">get</span>; <span class=\"keyword\">private</span> <span class=\"keyword\">set</span>; &#125;</span><br><span class=\"line\">    <span class=\"comment\">// 当前正在使用的对象数量</span></span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> countActive &#123; <span class=\"keyword\">get</span> &#123; <span class=\"keyword\">return</span> countAll - countInactive; &#125; &#125;</span><br><span class=\"line\">    <span class=\"comment\">// 当前闲置在池中、可供取用的对象数量</span></span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> countInactive &#123; <span class=\"keyword\">get</span> &#123; <span class=\"keyword\">return</span> m_Stack.Count; &#125; &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"title\">ObjectPool</span>(<span class=\"params\">UnityAction&lt;T&gt; actionOnGet, UnityAction&lt;T&gt; actionOnRelease</span>)</span></span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        m_ActionOnGet = actionOnGet;</span><br><span class=\"line\">        m_ActionOnRelease = actionOnRelease;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 从池中获取一个对象（优先复用闲置对象，没有则新建）</span></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> T <span class=\"title\">Get</span>()</span></span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        T element;</span><br><span class=\"line\">        <span class=\"keyword\">if</span> (m_Stack.Count == <span class=\"number\">0</span>)</span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"comment\">// 池中无闲置对象，创建新实例并增加总数计数</span></span><br><span class=\"line\">            element = <span class=\"keyword\">new</span> T();</span><br><span class=\"line\">            countAll++;</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">        <span class=\"keyword\">else</span></span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"comment\">// 从栈顶弹出一个闲置对象复用</span></span><br><span class=\"line\">            element = m_Stack.Pop();</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">        <span class=\"comment\">// 调用 Get 回调（如果有的话），例如重新初始化对象状态</span></span><br><span class=\"line\">        <span class=\"keyword\">if</span> (m_ActionOnGet != <span class=\"literal\">null</span>)</span><br><span class=\"line\">            m_ActionOnGet(element);</span><br><span class=\"line\">        <span class=\"keyword\">return</span> element;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 将对象归还到池中</span></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">void</span> <span class=\"title\">Release</span>(<span class=\"params\">T element</span>)</span></span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"comment\">// 简单的安全检查：防止连续重复归还同一个对象</span></span><br><span class=\"line\">        <span class=\"keyword\">if</span> (m_Stack.Count &gt; <span class=\"number\">0</span> &amp;&amp; ReferenceEquals(m_Stack.Peek(), element))</span><br><span class=\"line\">            Debug.LogError(<span class=\"string\">&quot;Internal error. Trying to destroy object that is already released to pool.&quot;</span>);</span><br><span class=\"line\">        <span class=\"comment\">// 调用 Release 回调（如果有的话），例如清空对象的状态</span></span><br><span class=\"line\">        <span class=\"keyword\">if</span> (m_ActionOnRelease != <span class=\"literal\">null</span>)</span><br><span class=\"line\">            m_ActionOnRelease(element);</span><br><span class=\"line\">        <span class=\"comment\">// 将对象压入栈中以供后续复用</span></span><br><span class=\"line\">        m_Stack.Push(element);</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">internal</span> <span class=\"keyword\">static</span> <span class=\"keyword\">class</span> <span class=\"title\">ListPool</span>&lt;<span class=\"title\">T</span>&gt;</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 为 List&lt;T&gt; 创建专用的对象池</span></span><br><span class=\"line\">    <span class=\"comment\">// Get 时不需要额外操作，Release 时自动清空列表内容</span></span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">static</span> <span class=\"keyword\">readonly</span> ObjectPool&lt;List&lt;T&gt;&gt; s_ListPool = <span class=\"keyword\">new</span> ObjectPool&lt;List&lt;T&gt;&gt;(<span class=\"literal\">null</span>, l =&gt; l.Clear());</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> List&lt;T&gt; <span class=\"title\">Get</span>()</span></span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"keyword\">return</span> s_ListPool.Get();</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> <span class=\"keyword\">void</span> <span class=\"title\">Release</span>(<span class=\"params\">List&lt;T&gt; toRelease</span>)</span></span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        s_ListPool.Release(toRelease);</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>上述对象池使用栈（<code>Stack</code>）作为存储容器，将废弃的对象存储起来，在需要时从栈顶推出实例交给使用者。栈的出栈和入栈都是 O(1) 操作，非常高效。</p>\n<p>对象池本身并不复杂，复杂的是使用——程序中所有需要创建对象实例、销毁对象实例、移除对象实例的地方，都需要用对象池来管理。我们来看几个使用上述 <code>ObjectPool</code> 和 <code>ListPool</code> 的例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br><span class=\"line\">26</span><br><span class=\"line\">27</span><br><span class=\"line\">28</span><br><span class=\"line\">29</span><br><span class=\"line\">30</span><br><span class=\"line\">31</span><br><span class=\"line\">32</span><br><span class=\"line\">33</span><br><span class=\"line\">34</span><br><span class=\"line\">35</span><br><span class=\"line\">36</span><br><span class=\"line\">37</span><br><span class=\"line\">38</span><br><span class=\"line\">39</span><br><span class=\"line\">40</span><br><span class=\"line\">41</span><br><span class=\"line\">42</span><br><span class=\"line\">43</span><br><span class=\"line\">44</span><br><span class=\"line\">45</span><br><span class=\"line\">46</span><br><span class=\"line\">47</span><br><span class=\"line\">48</span><br><span class=\"line\">49</span><br><span class=\"line\">50</span><br><span class=\"line\">51</span><br><span class=\"line\">52</span><br><span class=\"line\">53</span><br><span class=\"line\">54</span><br><span class=\"line\">55</span><br><span class=\"line\">56</span><br><span class=\"line\">57</span><br><span class=\"line\">58</span><br><span class=\"line\">59</span><br><span class=\"line\">60</span><br><span class=\"line\">61</span><br><span class=\"line\">62</span><br><span class=\"line\">63</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">public</span> <span class=\"keyword\">class</span> <span class=\"title\">A</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> a;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">float</span> b;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 为 A 类型创建专用的对象池实例</span></span><br><span class=\"line\"><span class=\"keyword\">internal</span> <span class=\"keyword\">static</span> <span class=\"keyword\">class</span> <span class=\"title\">PoolA</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">static</span> <span class=\"keyword\">readonly</span> ObjectPool&lt;A&gt; s_Pool = <span class=\"keyword\">new</span> ObjectPool&lt;A&gt;(<span class=\"literal\">null</span>, <span class=\"literal\">null</span>);</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> A <span class=\"title\">Get</span>()</span> =&gt; s_Pool.Get();</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> <span class=\"keyword\">void</span> <span class=\"title\">Release</span>(<span class=\"params\">A obj</span>)</span> =&gt; s_Pool.Release(obj);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">void</span> <span class=\"title\">Main</span>()</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 示例 1：在 Dictionary 中管理对象池中的对象</span></span><br><span class=\"line\">    Dictionary&lt;<span class=\"built_in\">int</span>, A&gt; dic = <span class=\"keyword\">new</span> Dictionary&lt;<span class=\"built_in\">int</span>, A&gt;(<span class=\"number\">16</span>);</span><br><span class=\"line\">    <span class=\"keyword\">for</span> (<span class=\"built_in\">int</span> i = <span class=\"number\">0</span>; i &lt; <span class=\"number\">1000</span>; i++)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"comment\">// 从对象池中获取 A 实例，避免 new A() 导致的堆内存分配</span></span><br><span class=\"line\">        A a = PoolA.Get();</span><br><span class=\"line\">        a.a = i % <span class=\"number\">10</span>;   <span class=\"comment\">// 键在 0-9 之间循环，因此会重复命中同一个键</span></span><br><span class=\"line\">        a.b = <span class=\"number\">3.5f</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">        A item = <span class=\"literal\">null</span>;</span><br><span class=\"line\">        <span class=\"keyword\">if</span> (dic.TryGetValue(a.a, <span class=\"keyword\">out</span> item))</span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"comment\">// 键已存在（键会循环重复），覆盖前将旧对象归还对象池</span></span><br><span class=\"line\">            PoolA.Release(item);</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">        dic[a.a] = a;</span><br><span class=\"line\"></span><br><span class=\"line\">        <span class=\"comment\">// 随机移除某个键，被移除的对象归还对象池</span></span><br><span class=\"line\">        <span class=\"built_in\">int</span> removeKey = Random.RangeInt(<span class=\"number\">0</span>, <span class=\"number\">10</span>);</span><br><span class=\"line\">        <span class=\"keyword\">if</span> (dic.TryGetValue(removeKey, <span class=\"keyword\">out</span> item))</span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            PoolA.Release(item);</span><br><span class=\"line\">            dic.Remove(removeKey);</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 示例 2：使用 ListPool 管理 List 的生命周期</span></span><br><span class=\"line\">    Dictionary&lt;<span class=\"built_in\">int</span>, List&lt;A&gt;&gt; dic2 = <span class=\"keyword\">new</span> Dictionary&lt;<span class=\"built_in\">int</span>, List&lt;A&gt;&gt;(<span class=\"number\">1000</span>);</span><br><span class=\"line\">    <span class=\"keyword\">for</span> (<span class=\"built_in\">int</span> i = <span class=\"number\">0</span>; i &lt; <span class=\"number\">1000</span>; i++)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"comment\">// 从 ListPool 中获取 List 实例，避免 new List&lt;A&gt;() 导致的堆内存分配</span></span><br><span class=\"line\">        List&lt;A&gt; arrayA = ListPool&lt;A&gt;.Get();</span><br><span class=\"line\"></span><br><span class=\"line\">        dic2.Add(i, arrayA);</span><br><span class=\"line\"></span><br><span class=\"line\">        List&lt;A&gt; item = <span class=\"literal\">null</span>;</span><br><span class=\"line\">        <span class=\"built_in\">int</span> removeKey = Random.RangeInt(<span class=\"number\">0</span>, <span class=\"number\">1000</span>);</span><br><span class=\"line\">        <span class=\"keyword\">if</span> (dic2.TryGetValue(removeKey, <span class=\"keyword\">out</span> item))</span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"comment\">// 移除时将 List 归还对象池，Release 时 ListPool 会自动调用 Clear() 清空列表</span></span><br><span class=\"line\">            ListPool&lt;A&gt;.Release(item);</span><br><span class=\"line\">            dic2.Remove(removeKey);</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>上述代码中，<code>A</code> 类和 <code>List&lt;A&gt;</code> 各有 1000 次创建需求，每次创建都使用对象池，并在 Dictionary 移除时将对象送还对象池。这样一来，我们就能不断复用被”废弃”的对象，而不需要总是创建新的。所有被归还的对象都会被存储在池中，不会被垃圾回收器回收，最终内存被不断重复利用，从而减少了内存分配和释放带来的消耗。</p>\n<hr>\n<h2 id=\"四、字符串-String\"><a href=\"#四、字符串-String\" class=\"headerlink\" title=\"四、字符串 String\"></a>四、字符串 String</h2><p>字符串的性能问题在大部分语言中都是比较棘手的问题，C# 中尤其如此。在 C# 中 <code>string</code> 是引用类型，每次动态创建一个字符串时，C# 都会在堆内存中分配一块内存来存放字符串内容。我们来看看它到底有多”恐怖”。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">string</span> strA = <span class=\"string\">&quot;test&quot;</span>;</span><br><span class=\"line\"><span class=\"keyword\">for</span> (<span class=\"built_in\">int</span> i = <span class=\"number\">0</span>; i &lt; <span class=\"number\">100</span>; i++)</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"built_in\">string</span> strB = strA + i.ToString();        <span class=\"comment\">// 第 1 次内存分配：连接产生新字符串</span></span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"built_in\">string</span>[] strC = strB.Split(<span class=\"string\">&#x27;e&#x27;</span>);           <span class=\"comment\">// 第 2、3 次内存分配：分割产生两个新字符串</span></span><br><span class=\"line\"></span><br><span class=\"line\">    strB = strB + strC[<span class=\"number\">0</span>];                     <span class=\"comment\">// 第 4 次内存分配：连接产生新字符串</span></span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"built_in\">string</span> strD = <span class=\"built_in\">string</span>.Format(<span class=\"string\">&quot;Hello &#123;0&#125;, this is &#123;1&#125; and &#123;2&#125;.&quot;</span>, strB, strC[<span class=\"number\">0</span>], strC[<span class=\"number\">1</span>]);</span><br><span class=\"line\">    <span class=\"comment\">// 第 5 次内存分配：Format 产生新字符串</span></span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>这是一段”恐怖”的程序。循环中每一次迭代做了以下事情：</p>\n<ol>\n<li>将 <code>strA</code> 字符串和 <code>i</code> 的整数字符串连接，<code>strB</code> 获得的是从内存中<strong>新分配</strong>的字符串；</li>\n<li>将 <code>strB</code> 按 <code>&#39;e&#39;</code> 切割成 <code>strC</code>，切割出的两个字符串又<strong>新分配</strong>了两段内存；</li>\n<li>将 <code>strB</code> 与 <code>strC[0]</code> 连接，又再<strong>申请</strong>了一段内存来存放连接后的内容，并赋值给 <code>strB</code>，<code>strB</code> 原来指向的内容因为没有变量引用就被”丢弃”了；</li>\n<li>最后用 <code>string.Format</code> 将 4 个字符串组合起来，又<strong>新分配</strong>了一段内存来存放组合后的内容。</li>\n</ol>\n<p>这里需要注意一点：<strong>字符串常量不会被 GC 回收</strong>。比如这段程序中的 <code>&quot;test&quot;</code> 和 <code>&quot;Hello {0}, this is {1} and {2}.&quot;</code> 这两个常量，它们常驻于内存——即使没有变量指向它们，它们也不会被回收；下次使用时也不需要重新分配内存。</p>\n<blockquote>\n<p><strong>字符串常量与字符串驻留（String Interning）</strong><br>在 C# 中，编译期的字符串字面量会被自动**驻留（Intern）**到 CLR 管理的字符串驻留池（Intern Pool）中。驻留池是一个全局的哈希表，存储了所有唯一的字符串字面量。当代码中出现相同的字符串字面量时，编译器会让所有引用指向驻留池中的同一块内存。这就是为什么 <code>Object.ReferenceEquals(&quot;hello&quot;, &quot;hello&quot;)</code> 会返回 <code>true</code>——两个 <code>&quot;hello&quot;</code> 指向的是同一个驻留字符串。<br>动态创建的字符串（如通过拼接、<code>StringBuilder</code> 等生成的字符串）<strong>不会</strong>被自动驻留。如果希望手动将动态字符串加入驻留池，可以调用 <code>string.Intern()</code> 方法。需要注意的是，驻留的字符串在应用程序域的整个生命周期内都不会被 GC 回收，因此不要滥用驻留功能，否则可能导致内存持续增长。</p>\n</blockquote>\n<p>每次循环中，这段代码向内存申请了 5 次新的内存段，并且抛弃了 1 次（<code>strA + i.ToString()</code> 的结果因为没有变量指向而被丢弃）。这还不是最糟糕的——最糟糕的是，每次循环结束都会将前面分配的所有临时内存内容全部抛弃，重新再分配一次。如此反复申请和抛弃，总共向内存申请了约 500 次内存段，并全部抛弃，<strong>内存被浪费得非常严重</strong>。</p>\n<p>为什么会这样呢？究其原因，C# 对字符串<strong>没有任何缓存机制</strong>，每次操作都需要重新分配 <code>string</code> 内容。据我所知，很多语言都没有字符串的缓存机制，因此我们的字符串连接、切割、组合等操作都会向内存申请新的内容，并抛弃没有变量指向的旧字符串，等待 GC 回收。我们知道 GC 一次回收会消耗大量 CPU，如果我们不注意字符串的使用方式、不断浪费内存，将导致程序不定时卡顿。并且随着程序运行时间越来越长、各模块中不良代码的积累，程序卡顿的频率会逐渐升高，运行效率也会越来越差。</p>\n<h3 id=\"如何解决字符串问题\"><a href=\"#如何解决字符串问题\" class=\"headerlink\" title=\"如何解决字符串问题\"></a>如何解决字符串问题</h3><h4 id=\"1-自建缓存机制\"><a href=\"#1-自建缓存机制\" class=\"headerlink\" title=\"1. 自建缓存机制\"></a>1. 自建缓存机制</h4><p>可以用一些标志性的 Key 键值来一一对应字符串，比如游戏项目中常用 ID 来构造某个字符串：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">int</span> ID = <span class=\"number\">101</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">ResData resData = GetDataById(ID);</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"built_in\">string</span> strName = <span class=\"string\">&quot;This is &quot;</span> + resData.Name;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">return</span> strName;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>一个 ID 对应一个字符串的场景，我们可以建立一个字典容器将其缓存起来，下次使用时就不需要重新申请内存了。</p>\n</blockquote>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">Dictionary&lt;<span class=\"built_in\">int</span>, <span class=\"built_in\">string</span>&gt; strCache;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"built_in\">string</span> strName = <span class=\"literal\">null</span>;</span><br><span class=\"line\"><span class=\"keyword\">if</span> (!strCache.TryGetValue(id, <span class=\"keyword\">out</span> strName))</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 缓存中不存在，创建并存入缓存</span></span><br><span class=\"line\">    ResData resData = GetDataById(id);</span><br><span class=\"line\">    strName = <span class=\"string\">&quot;This is &quot;</span> + resData.Name;</span><br><span class=\"line\">    strCache.Add(id, strName);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">return</span> strName;</span><br></pre></td></tr></table></figure>\n\n<p>我们用 <code>Dictionary</code> 容器将字符串缓存起来，每次先查询字典中是否存在，有则直接使用，没有则创建一个并将其存入字典，以便下次复用。</p>\n<h4 id=\"2-使用不安全代码直接操作内存\"><a href=\"#2-使用不安全代码直接操作内存\" class=\"headerlink\" title=\"2. 使用不安全代码直接操作内存\"></a>2. 使用不安全代码直接操作内存</h4><p>由于 <code>string</code> 类本身的任何修改操作都会申请新的内存，因此我们需要突破这个限制——直接使用指针来修改 <code>string</code> 中字符的值，这样就能重复利用已分配的 <code>string</code> 内存，不需要反复重新分配。</p>\n<p>C# 虽然托管了大部分内存内容，但它也允许我们使用<strong>非托管方式</strong>来直接访问和修改内存——这就是 <code>unsafe</code> 关键字的用途。通过非托管方式，我们可以绕过 <code>string</code> 的不可变性，直接改变其中的字符内容，使已分配的字符串内存能被重复利用。来看一个例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">string</span> strA = <span class=\"string\">&quot;aaa&quot;</span>;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 使用 &quot;bbb&quot; + &quot;b&quot; 的方式生成新字符串，是为了避免修改常量字符串内存块</span></span><br><span class=\"line\"><span class=\"built_in\">string</span> strB = <span class=\"string\">&quot;bbb&quot;</span> + <span class=\"string\">&quot;b&quot;</span>;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 使用 fixed 关键字固定指针所指向的对象的内存</span></span><br><span class=\"line\"><span class=\"keyword\">fixed</span> (<span class=\"built_in\">char</span>* strA_ptr = strA)</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">fixed</span> (<span class=\"built_in\">char</span>* strB_ptr = strB)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"comment\">// 将 strA 的前 3 个字符内容直接拷贝到 strB 的内存中</span></span><br><span class=\"line\">        memcopy((<span class=\"built_in\">byte</span>*)strB_ptr, (<span class=\"built_in\">byte</span>*)strA_ptr, <span class=\"number\">3</span> * <span class=\"keyword\">sizeof</span>(<span class=\"built_in\">char</span>));</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">Console.WriteLine(strB); <span class=\"comment\">// 此时 strB 内容变为 &quot;aaab&quot;</span></span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>注意：这里特意用 <code>&quot;bbb&quot; + &quot;b&quot;</code> 而不是直接写 <code>&quot;bbbb&quot;</code>，是为了确保 <code>strB</code> 不是常量字符串内存块，而是运行时期动态分配的新字符串，这样我们修改它的内容才是安全的。</p>\n</blockquote>\n<p>其中 <code>memcopy</code> 并非系统函数，需要自己实现，写法也很简单——拿到两个指针，根据长度遍历并逐字节赋值：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">unsafe</span> <span class=\"keyword\">void</span> <span class=\"title\">memcopy</span>(<span class=\"params\"><span class=\"built_in\">byte</span>* dest, <span class=\"built_in\">byte</span>* src, <span class=\"built_in\">int</span> len</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">while</span> ((--len) &gt;= <span class=\"number\">0</span>)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        dest[len] = src[len];</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p><strong>注意</strong>：使用 <code>unsafe</code> 代码需要在项目设置中启用 “Allow unsafe code” 选项，且这类代码绕过了 C# 的内存安全检查，使用时需格外谨慎。</p>\n</blockquote>\n<p>我们把 <code>strB</code> 的前 3 个字符的内容替换成了 <code>strA</code> 中的内容，整个过程并没有增加任何新内存，因为我们使用了不安全的非托管方法直接操控内存。通过这种方式，我们可以将已分配的 <code>string</code> 缓存起来重复利用。下面来看一个实际应用的例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br><span class=\"line\">26</span><br><span class=\"line\">27</span><br><span class=\"line\">28</span><br><span class=\"line\">29</span><br><span class=\"line\">30</span><br><span class=\"line\">31</span><br><span class=\"line\">32</span><br><span class=\"line\">33</span><br><span class=\"line\">34</span><br><span class=\"line\">35</span><br><span class=\"line\">36</span><br><span class=\"line\">37</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 按字符串长度缓存可复用的字符串</span></span><br><span class=\"line\">Dictionary&lt;<span class=\"built_in\">int</span>, <span class=\"built_in\">string</span>&gt; cacheStr;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">unsafe</span> <span class=\"built_in\">string</span> <span class=\"title\">Concat</span>(<span class=\"params\"><span class=\"built_in\">string</span> strA, <span class=\"built_in\">string</span> strB</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"built_in\">int</span> a_length = strA.Length;</span><br><span class=\"line\">    <span class=\"built_in\">int</span> b_length = strB.Length;</span><br><span class=\"line\">    <span class=\"built_in\">int</span> sum_length = a_length + b_length;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"built_in\">string</span> strResult = <span class=\"literal\">null</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"keyword\">if</span> (!cacheStr.TryGetValue(sum_length, <span class=\"keyword\">out</span> strResult))</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"comment\">// 缓存中不存在该长度的字符串，直接连接后存入缓存</span></span><br><span class=\"line\">        strResult = strA + strB;</span><br><span class=\"line\">        cacheStr.Add(sum_length, strResult);</span><br><span class=\"line\">        <span class=\"keyword\">return</span> strResult;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 缓存中存在可用字符串，用指针直接修改其内容</span></span><br><span class=\"line\">    <span class=\"keyword\">fixed</span> (<span class=\"built_in\">char</span>* strA_ptr = strA)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"keyword\">fixed</span> (<span class=\"built_in\">char</span>* strB_ptr = strB)</span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"keyword\">fixed</span> (<span class=\"built_in\">char</span>* strResult_ptr = strResult)</span><br><span class=\"line\">            &#123;</span><br><span class=\"line\">                <span class=\"comment\">// 将 strA 的内容拷贝到 strResult 的开头</span></span><br><span class=\"line\">                memcopy((<span class=\"built_in\">byte</span>*)strResult_ptr, (<span class=\"built_in\">byte</span>*)strA_ptr, a_length * <span class=\"keyword\">sizeof</span>(<span class=\"built_in\">char</span>));</span><br><span class=\"line\"></span><br><span class=\"line\">                <span class=\"comment\">// 将 strB 的内容拷贝到 strResult 中 a_length 偏移之后的位置</span></span><br><span class=\"line\">                memcopy((<span class=\"built_in\">byte</span>*)strResult_ptr + a_length * <span class=\"keyword\">sizeof</span>(<span class=\"built_in\">char</span>), (<span class=\"built_in\">byte</span>*)strB_ptr, b_length * <span class=\"keyword\">sizeof</span>(<span class=\"built_in\">char</span>));</span><br><span class=\"line\">            &#125;</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"keyword\">return</span> strResult;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>当需要将两个字符串连接起来时，先在缓存中查找是否有可用长度的字符串：没有则直接连接并缓存；有则取出来，用指针方式直接修改缓存字符串的值。</p>\n<h4 id=\"3-C-自带方案\"><a href=\"#3-C-自带方案\" class=\"headerlink\" title=\"3. C# 自带方案\"></a>3. C# 自带方案</h4><p>上述 <code>unsafe</code> 方案的核心思想是”预分配一块可复用的缓冲区，用的时候直接往里面写内容”，这是一种教学性质的极端优化演示。在实际开发中，C# 自身已经提供了更安全、更易用的替代方案，其内核与我们手动实现的方式完全一致。</p>\n<h5 id=\"StringBuilder-缓存\"><a href=\"#StringBuilder-缓存\" class=\"headerlink\" title=\"StringBuilder 缓存\"></a>StringBuilder 缓存</h5><p><code>StringBuilder</code> 内部维护一个 <code>char[]</code> 缓冲区，其工作原理与上述手动按长度缓存 <code>string</code> 的思路如出一辙：<code>Append</code> 往缓冲区末尾填字符，<code>Clear()</code> 只把内部写指针归零，<strong>不释放底层的 <code>char[]</code> 数组</strong>，下次使用时直接覆盖写入，零额外分配。</p>\n<p>我们不需要为 <code>StringBuilder</code> 实现任何包装——只需要把 <code>StringBuilder</code> 实例本身缓存起来即可：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 缓存一个 StringBuilder 实例并反复使用</span></span><br><span class=\"line\">StringBuilder sb = <span class=\"keyword\">new</span> StringBuilder(<span class=\"number\">256</span>);  <span class=\"comment\">// 预估合理初始容量，避免内部数组扩容</span></span><br><span class=\"line\"></span><br><span class=\"line\">sb.Append(<span class=\"string\">&quot;Hello &quot;</span>);</span><br><span class=\"line\">sb.Append(name);</span><br><span class=\"line\">sb.Append(<span class=\"string\">&quot;, this is &quot;</span>);</span><br><span class=\"line\">sb.Append(<span class=\"number\">123</span>);</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"built_in\">string</span> result = sb.ToString();</span><br><span class=\"line\"></span><br><span class=\"line\">sb.Clear();  <span class=\"comment\">// 清空后下次直接复用，不产生新内存分配</span></span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p><strong>注意</strong>：<code>sb.ToString()</code> 仍然会产生一次新的 <code>string</code> 分配（这是不可避免的，因为你需要把结果拿出去）。但拼接过程中的中间字符串分配被完全消除了——这正是 <code>StringBuilder</code> 相比 <code>+</code> 拼接的优势所在。</p>\n</blockquote>\n<p>在 Unity 项目中，可以结合对象池来做更全面的管理：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">internal</span> <span class=\"keyword\">static</span> <span class=\"keyword\">class</span> <span class=\"title\">StringBuilderPool</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 使用前面介绍的 ObjectPool 来管理 StringBuilder 实例</span></span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">static</span> <span class=\"keyword\">readonly</span> ObjectPool&lt;StringBuilder&gt; s_Pool =</span><br><span class=\"line\">        <span class=\"keyword\">new</span> ObjectPool&lt;StringBuilder&gt;(<span class=\"literal\">null</span>, sb =&gt; sb.Clear());</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> StringBuilder <span class=\"title\">Get</span>()</span> =&gt; s_Pool.Get();</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> <span class=\"keyword\">void</span> <span class=\"title\">Release</span>(<span class=\"params\">StringBuilder sb</span>)</span> =&gt; s_Pool.Release(sb);</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>每次需要拼接字符串时从池中取，用完后归还，避免反复创建 <code>StringBuilder</code> 实例。以下是一个实际使用的例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 模拟每帧生成日志字符串的场景</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">BuildLogLine</span>(<span class=\"params\"><span class=\"built_in\">string</span> playerName, <span class=\"built_in\">int</span> score</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    StringBuilder sb = StringBuilderPool.Get();  <span class=\"comment\">// 从池中取出</span></span><br><span class=\"line\"></span><br><span class=\"line\">    sb.Append(<span class=\"string\">&quot;[&quot;</span>);</span><br><span class=\"line\">    sb.Append(playerName);</span><br><span class=\"line\">    sb.Append(<span class=\"string\">&quot;]  score: &quot;</span>);</span><br><span class=\"line\">    sb.Append(score);</span><br><span class=\"line\">    sb.Append(<span class=\"string\">&quot;, rank: &quot;</span>);</span><br><span class=\"line\">    sb.Append(GetRank(score));</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"built_in\">string</span> log = sb.ToString();                   <span class=\"comment\">// 转为 string 输出</span></span><br><span class=\"line\"></span><br><span class=\"line\">    StringBuilderPool.Release(sb);               <span class=\"comment\">// 用完归还池中，Release 回调自动 Clear</span></span><br><span class=\"line\">    <span class=\"comment\">// 之后同一实例可被其他调用者复用，无需 new StringBuilder()</span></span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>这个场景下，池中只需维持少数几个 <code>StringBuilder</code> 实例即可覆盖所有调用，对象复用率极高。</p>\n</blockquote>\n<blockquote>\n<p><code>StringBuilder</code> 的初始容量建议根据实际场景预估。如果预估容量偏小，内部 <code>char[]</code> 扩容时会产生一次数组拷贝——这同样是内存分配，应尽量避免。如果不确定容量，宁可稍微偏大一些。</p>\n</blockquote>\n<h5 id=\"string-Intern-驻留池\"><a href=\"#string-Intern-驻留池\" class=\"headerlink\" title=\"string.Intern 驻留池\"></a>string.Intern 驻留池</h5><p>对于<strong>字符串种类有限但反复出现</strong>的场景（比如配置表 key、网络协议字段名、资源路径等），可以利用 C# 自带的字符串驻留池（Intern Pool）：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 将动态字符串加入驻留池（如果池中已存在同内容的字符串，返回已有引用）</span></span><br><span class=\"line\"><span class=\"built_in\">string</span> cached = <span class=\"built_in\">string</span>.Intern(someDynamicString);</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 仅查找驻留池，不自动加入（不存在则返回 null）</span></span><br><span class=\"line\"><span class=\"built_in\">string</span> existing = <span class=\"built_in\">string</span>.IsInterned(someDynamicString);</span><br></pre></td></tr></table></figure>\n\n<p>驻留之后，所有相同内容的字符串都指向同一块内存，后续的 <code>==</code> 比较会命中 <code>ReferenceEquals</code> 而<strong>直接返回 <code>true</code></strong>，彻底避免逐字符遍历——这正是后文”字符串比较的隐藏开销”一节中提到的优化方向。</p>\n<blockquote>\n<p><strong>注意</strong>：驻留池的字符串在整个应用程序域生命周期内都不会被 GC 回收，因此只适合”种类有限”的场景。不要对高度动态变化、种类无穷的字符串（如用户输入、随机生成的内容）大量使用 <code>string.Intern</code>，否则驻留池会持续膨胀，反而导致内存泄漏。</p>\n</blockquote>\n<table>\n<thead>\n<tr>\n<th>方案</th>\n<th>安全性</th>\n<th>分配次数</th>\n<th>适用场景</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>StringBuilder</code> 缓存</td>\n<td>✅ 安全</td>\n<td>仅 <code>ToString()</code> 时 1 次</td>\n<td>拼接、格式化等通用场景</td>\n</tr>\n<tr>\n<td><code>string.Intern</code></td>\n<td>✅ 安全</td>\n<td>0（复用已有引用）</td>\n<td>种类有限的整串缓存（Key &#x2F; 路径）</td>\n</tr>\n<tr>\n<td>手动 <code>unsafe</code> + 缓存</td>\n<td>⚠️ 不安全</td>\n<td>0</td>\n<td>极端优化场景，不建议日常使用</td>\n</tr>\n</tbody></table>\n<h3 id=\"字符串的其他隐藏问题\"><a href=\"#字符串的其他隐藏问题\" class=\"headerlink\" title=\"字符串的其他隐藏问题\"></a>字符串的其他隐藏问题</h3><h4 id=\"Length-属性并非-O-1\"><a href=\"#Length-属性并非-O-1\" class=\"headerlink\" title=\"Length 属性并非 O(1)\"></a>Length 属性并非 O(1)</h4><p><code>string</code> 的 <code>Length</code> 属性可不是省油的灯。由于 C# 的 <code>string</code> 类中只存放了 <code>char[]</code> 数据，没有单独存储长度值的字段，因此取长度时需要通过遍历字符串来找到结尾的 <code>\\0</code> 字符。类似于如下伪代码：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">int</span> sum = <span class=\"number\">0</span>;</span><br><span class=\"line\"><span class=\"keyword\">for</span> (; data[sum] != <span class=\"string\">&#x27;\\0&#x27;</span>; ++sum) ;</span><br><span class=\"line\"><span class=\"keyword\">return</span> sum;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>如果我们大量使用 <code>Length</code>，就会浪费很多 CPU，因为每次调用它都会遍历整个字符串。好在实际开发中，我们通常不会在循环中反复访问同一字符串的 <code>Length</code>——一种简单的优化是将其缓存到局部变量中。</p>\n</blockquote>\n<h4 id=\"字符串比较的隐藏开销\"><a href=\"#字符串比较的隐藏开销\" class=\"headerlink\" title=\"字符串比较的隐藏开销\"></a>字符串比较的隐藏开销</h4><p>字符串比较也有隐藏的性能问题。当两个字符串进行比较时，<code>string</code> 的 <code>Equals</code> 方法首先会比较两个字符串的<strong>引用指针</strong>是否一致，一致则直接返回 <code>true</code>。如果指针不一致，则会先比较两者的<strong>长度</strong>是否相等，长度不同则直接返回 <code>false</code>；长度相同才会逐个遍历每一个字符，判断它们是否全部相等。我们来看看 <code>Equals</code> 的实际源码：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"built_in\">bool</span> <span class=\"title\">Equals</span>(<span class=\"params\">String <span class=\"keyword\">value</span></span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">if</span> (<span class=\"keyword\">this</span> == <span class=\"literal\">null</span>)                        <span class=\"comment\">// 防止 reverse-pinvoke 等场景的空引用</span></span><br><span class=\"line\">        <span class=\"keyword\">throw</span> <span class=\"keyword\">new</span> NullReferenceException();</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"keyword\">if</span> (<span class=\"keyword\">value</span> == <span class=\"literal\">null</span>)</span><br><span class=\"line\">        <span class=\"keyword\">return</span> <span class=\"literal\">false</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"keyword\">if</span> (Object.ReferenceEquals(<span class=\"keyword\">this</span>, <span class=\"keyword\">value</span>)) <span class=\"comment\">// 第一步：比较引用是否相同</span></span><br><span class=\"line\">        <span class=\"keyword\">return</span> <span class=\"literal\">true</span>;</span><br><span class=\"line\">    </span><br><span class=\"line\">    <span class=\"keyword\">if</span> (<span class=\"keyword\">this</span>.Length != <span class=\"keyword\">value</span>.Length)         <span class=\"comment\">// 第二步：比较长度是否相同</span></span><br><span class=\"line\">        <span class=\"keyword\">return</span> <span class=\"literal\">false</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"keyword\">return</span> EqualsHelper(<span class=\"keyword\">this</span>, <span class=\"keyword\">value</span>);        <span class=\"comment\">// 第三步：逐字符遍历比较</span></span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>这段代码从源码中提取。在判空之后，依次进行：引用比较 → 长度比较 → 逐字符遍历。只有当前两步都无法得出结论时，才会进入最昂贵的逐字符比较。</p>\n</blockquote>\n<p>这意味着，如果两个字符串内容相同但来自不同的内存段（例如分别通过拼接得到），那么比较它们时就不得不遍历所有字符。如下示例：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">string</span> strA = <span class=\"string\">&quot;Hello &quot;</span>;</span><br><span class=\"line\"><span class=\"built_in\">string</span> strB = <span class=\"string\">&quot;Hello &quot;</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">strA = strA + <span class=\"string\">&quot;C&quot;</span>;   <span class=\"comment\">// strA 在堆上新分配一块内存</span></span><br><span class=\"line\">strB = strB + <span class=\"string\">&quot;C&quot;</span>;   <span class=\"comment\">// strB 也在堆上新分配一块内存（与 strA 不同）</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 虽然 strA 和 strB 内容完全相同（都是 &quot;Hello C&quot;），但它们位于不同的内存段</span></span><br><span class=\"line\"><span class=\"comment\">// 引用比较（ReferenceEquals）会返回 false</span></span><br><span class=\"line\"><span class=\"comment\">// 因此 Equals 会进入逐字符遍历比较</span></span><br><span class=\"line\"><span class=\"keyword\">if</span> (strA == strB)</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">&quot;相等&quot;</span>); <span class=\"comment\">// 最终结果：相等，但经过了昂贵的遍历比较</span></span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>在这个例子中，<code>strA</code> 和 <code>strB</code> 内容完全相同，但由于分别通过拼接产生，位于不同的内存地址，因此 <code>ReferenceEquals</code> 返回 <code>false</code>，<code>Equals</code> 不得不进入逐字符遍历。如果这样的比较频繁发生，累积的性能损耗也不容忽视。将字符串缓存起来（如前文所述），让相同内容的字符串指向同一块内存，就能让 <code>ReferenceEquals</code> 快速返回 <code>true</code>，从而避免遍历。</p>\n</blockquote>\n<hr>\n<h2 id=\"参考资料\"><a href=\"#参考资料\" class=\"headerlink\" title=\"参考资料\"></a>参考资料</h2><ul>\n<li><a href=\"http://www.luzexi.com/2019/12/31/Unity3D%E9%AB%98%E7%BA%A7%E7%BC%96%E7%A8%8B%E4%B9%8B%E8%BF%9B%E9%98%B6%E4%B8%BB%E7%A8%8B-%E7%A8%8B%E5%BA%8F%E9%80%BB%E8%BE%91%E4%BC%98%E5%8C%96%E6%8A%80%E5%B7%A7\">《Unity3D高级编程之进阶主程》 第一章，程序逻辑优化技巧 - 技术人生 - 编程技术 - JESSE人生</a></li>\n</ul>\n","excerpt":"","more":"<hr>\n<h2 id=\"写在前面\"><a href=\"#写在前面\" class=\"headerlink\" title=\"写在前面\"></a>写在前面</h2><p>这个分类下的文章是我对 《Unity3D高级编程之进阶主程》一书的学习笔记。</p>\n<p>这个博客对应的章节是 </p>\n<ul>\n<li>第一章，程序逻辑优化技巧</li>\n</ul>\n<p>部分内容为ai生成，如有错误恳请指出。</p>\n<hr>\n<h2 id=\"一、使用-List-和-Dictionary\"><a href=\"#一、使用-List-和-Dictionary\" class=\"headerlink\" title=\"一、使用 List 和 Dictionary\"></a>一、使用 List 和 Dictionary</h2><p><code>List</code> 和 <code>Dictionary</code> 是 C# 中最常用的集合类型，它们的底层都是由数组实现的。理解它们的内部机制，才能避免在性能敏感的场景中踩坑。</p>\n<h3 id=\"List\"><a href=\"#List\" class=\"headerlink\" title=\"List\"></a>List</h3><p><code>List</code> 在插入元素时，需要将插入位置之后的所有元素依次向后移动，因此 <code>Insert</code> 的时间复杂度是 <strong>O(n)</strong>。</p>\n<p><code>Contains</code> 通过遍历数组来查找目标元素，时间复杂度也是 <strong>O(n)</strong>。</p>\n<p><code>Remove</code> 同样需要先找到元素再移动后续元素，复杂度同样是 <strong>O(n)</strong>。</p>\n<blockquote>\n<p>因此，如果频繁调用 <code>Insert</code>、<code>Contains</code>、<code>Remove</code> 这些 O(n) 操作，尤其是在 List 容量较大的情况下，会带来很多不必要的性能消耗。在合适的场景下，可以考虑使用 <code>HashSet</code>、<code>Dictionary</code> 等 O(1) 查找的数据结构来替代。</p>\n</blockquote>\n<h3 id=\"Dictionary\"><a href=\"#Dictionary\" class=\"headerlink\" title=\"Dictionary\"></a>Dictionary</h3><p>在编写程序时应注意给 <code>Dictionary</code> 设置一个合理的初始容量，而不是任由它自行扩容。扩容时的数组重新分配不仅会加重 <strong>GC</strong> 的负担，过小的容量还会让哈希冲突变得更加频繁，进一步导致性能损耗。</p>\n<p>此外，如果使用<strong>类实例</strong>作为 <code>Dictionary</code> 的 Key，<code>Dictionary</code> 会使用 <code>Object</code> 的 <code>GetHashCode()</code> 方法来获取类实例的哈希值。而 <code>GetHashCode()</code> 是通过算法将内存地址转化为哈希值的，它并不会缓存计算结果——每次调用都会重新计算一次，这是一种比较隐蔽的性能损耗。如果频繁通过 <code>GetHashCode</code> 来提取 Value，我们应该关注一下 <code>GetHashCode</code> 的计算开销，考虑是否可以用唯一 ID（如 <code>int</code> 或 <code>string</code>）的方式来代替 <code>GetHashCode</code> 算法。</p>\n<hr>\n<h2 id=\"二、使用-Struct\"><a href=\"#二、使用-Struct\" class=\"headerlink\" title=\"二、使用 Struct\"></a>二、使用 Struct</h2><p><code>Struct</code> 是值类型。当作为局部变量使用时，<code>Struct</code> 分配的内存在<strong>栈</strong>上，生命周期结束后回收也十分简单——只需将栈的尾指针回退即可。与分配在堆上的类实例不同，值类型的分配和回收不会产生内存碎片，也不需要 GC 介入。</p>\n<p>另外，CPU 从栈上读取数据比从堆上读取数据更高效，因为栈内存具有更好的访问局部性。</p>\n<h3 id=\"CPU-缓存命中和-Struct-数组\"><a href=\"#CPU-缓存命中和-Struct-数组\" class=\"headerlink\" title=\"CPU 缓存命中和 Struct 数组\"></a>CPU 缓存命中和 Struct 数组</h3><p>CPU 在读取内存时，会把连续的一大块内容加载到缓存中。从缓存读取数据比直接从内存读取快得多（可达 100 倍以上）。下次读取数据时，CPU 会先在缓存中查找，如果命中则无需再次访问内存。</p>\n<blockquote>\n<p>连续内存在 CPU 读取时，缓存的命中率高；而非连续内存则命中率较低。我们应该尽可能让 CPU 读取数据时命中缓存，从而提升访问效率。</p>\n</blockquote>\n<p>正因为如此，<code>Struct</code> 数组对提高内存访问速度很有帮助。由于 <code>Struct</code> 是值类型，<code>Struct</code> 数组中的所有元素都是连续存储的——每个元素的数据实实在在”嵌”在数组的连续内存空间中。而 Class 数组则不同：虽然数组中的引用（指针）变量是连续的，但每个引用所指向的、位于堆上的实际数据则像碎片一样分散在各处。</p>\n<h3 id=\"使用值类型数组\"><a href=\"#使用值类型数组\" class=\"headerlink\" title=\"使用值类型数组\"></a>使用值类型数组</h3><p>不过，并非所有 <code>Struct</code> 都能提高缓存命中率。如果一个 <code>Struct</code> 太大，超过了 CPU 一次缓存加载的数据块大小，那么缓存的效果就会大打折扣——因为一次加载的数据可能只包含一个甚至半个 Struct。因此，很多高性能架构彻底抛弃了在对象上直接存放数据的方式，转而使用<strong>值类型连续数组</strong>来提高 CPU 缓存命中率：将同一类数据集中到基本类型的数组中，而具体对象上只存放一个索引值，通过索引来间接访问数组中的数据。我们来看一个例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br><span class=\"line\">26</span><br><span class=\"line\">27</span><br><span class=\"line\">28</span><br><span class=\"line\">29</span><br><span class=\"line\">30</span><br><span class=\"line\">31</span><br><span class=\"line\">32</span><br><span class=\"line\">33</span><br><span class=\"line\">34</span><br><span class=\"line\">35</span><br><span class=\"line\">36</span><br><span class=\"line\">37</span><br><span class=\"line\">38</span><br><span class=\"line\">39</span><br><span class=\"line\">40</span><br><span class=\"line\">41</span><br><span class=\"line\">42</span><br><span class=\"line\">43</span><br><span class=\"line\">44</span><br><span class=\"line\">45</span><br><span class=\"line\">46</span><br><span class=\"line\">47</span><br><span class=\"line\">48</span><br><span class=\"line\">49</span><br><span class=\"line\">50</span><br><span class=\"line\">51</span><br><span class=\"line\">52</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 传统 OOP 方式：每个对象持有自己的数据</span></span><br><span class=\"line\"><span class=\"keyword\">class</span> <span class=\"title\">A</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> a;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">float</span> b;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">bool</span> c;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 改进方式：对象只持有索引，实际数据集中存储在数组中</span></span><br><span class=\"line\"><span class=\"keyword\">class</span> <span class=\"title\">B</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> index;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 数据中心：将所有同类型数据以值类型数组的形式集中存放</span></span><br><span class=\"line\"><span class=\"keyword\">class</span> <span class=\"title\">C</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">static</span> C _instance;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"keyword\">static</span> C instance</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"keyword\">get</span></span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"keyword\">if</span> (<span class=\"literal\">null</span> == _instance)</span><br><span class=\"line\">            &#123;</span><br><span class=\"line\">                _instance = <span class=\"keyword\">new</span> C();</span><br><span class=\"line\">            &#125;</span><br><span class=\"line\">            <span class=\"keyword\">return</span> _instance;</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 所有数据集中存放在值类型数组中，内存连续</span></span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span>[] a = <span class=\"keyword\">new</span> <span class=\"built_in\">int</span>[] &#123; <span class=\"number\">2</span>, <span class=\"number\">3</span>, <span class=\"number\">5</span>, <span class=\"number\">6</span> &#125;;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">float</span>[] b = <span class=\"keyword\">new</span> <span class=\"built_in\">float</span>[] &#123; <span class=\"number\">2.1f</span>, <span class=\"number\">3.4f</span>, <span class=\"number\">1.5f</span>, <span class=\"number\">5.4f</span> &#125;;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">bool</span>[] c = <span class=\"keyword\">new</span> <span class=\"built_in\">bool</span>[] &#123; <span class=\"literal\">false</span>, <span class=\"literal\">true</span>, <span class=\"literal\">false</span>, <span class=\"literal\">true</span> &#125;;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">void</span> <span class=\"title\">Main</span>()</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// A 类方式：引用数组连续，但实际对象数据分散在堆上</span></span><br><span class=\"line\">    A[] arrayA = <span class=\"keyword\">new</span> A[<span class=\"number\">3</span>] &#123; <span class=\"keyword\">new</span> A(), <span class=\"keyword\">new</span> A(), <span class=\"keyword\">new</span> A() &#125;;</span><br><span class=\"line\"></span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">&quot;A class: a=&#123;0&#125; b=&#123;1&#125; c=&#123;2&#125;&quot;</span>, arrayA[<span class=\"number\">0</span>].a, arrayA[<span class=\"number\">0</span>].b, arrayA[<span class=\"number\">0</span>].c);</span><br><span class=\"line\">    </span><br><span class=\"line\">    <span class=\"comment\">// B 类方式：对象只持有一个索引，通过索引访问集中存储的数据</span></span><br><span class=\"line\">    B b = <span class=\"keyword\">new</span> B();</span><br><span class=\"line\">    b.index = <span class=\"number\">2</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">    C c = C.instance;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 数据访问时，通过索引去值类型数组中读取，所有数据在内存中连续</span></span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">&quot;B class: a=&#123;0&#125; b=&#123;1&#125; c=&#123;2&#125;&quot;</span>, c.a[b.index], c.b[b.index], c.c[b.index]);</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>上述代码中，<code>A</code> 类使用了我们非常熟悉的面向对象编程方式，把所有属性变量都放在自己身上，数据集合以引用的方式存储在数组中；而 <code>B</code> 类则将数据集中存放在了 <code>C</code> 类中，自己只存放一个索引值。</p>\n<p>当两者都对数据进行存取时，<code>A</code> 类的数据在内存中是分散的——因为每次 <code>new A()</code> 时，都是从堆内存中找一块空地来分配，并不保证相邻实例的内存连续，<code>arrayA</code> 中只是引用连续，而非数据本身连续。而 <code>B</code> 类的数据则存储在值类型的数组中，值类型数组在分配时一定是内存连续的，这样就能更多地利用缓存，提高 CPU 读取数据时的命中率。</p>\n<hr>\n<h2 id=\"三、对象池\"><a href=\"#三、对象池\" class=\"headerlink\" title=\"三、对象池\"></a>三、对象池</h2><p>类实例在堆上分配内存后，当引用变量被置空或丢弃，这些内存就会交由**垃圾回收器（GC）**来回收。然而，内存分配本身是耗时的，垃圾回收的过程则更加昂贵。</p>\n<p>简单来说，垃圾回收的过程是：当内存不够用时，GC 会遍历所有已分配的内存块，检查哪些类实例已经没有任何变量引用，然后将其回收。如果回收后依然内存不足，就会向操作系统申请更多内存。很明显，业务逻辑越庞大、数据量越多，GC 需要检查的内容就越多。</p>\n<p>垃圾回收过程如此繁重，每次回收都会消耗大量 CPU 算力，因此我们不应该在 C# 中随意 <code>new</code> 创建类实例，然后又不管它们的死活随意丢弃或置空引用，让类实例不断地被创建和抛弃。</p>\n<blockquote>\n<p><strong>销毁实例 vs 置空引用：它们对 GC 的影响有何不同？</strong><br>无论是将引用变量设为 <code>null</code>，还是让变量离开作用域被自然销毁，最终的效果是相同的——被引用的对象变得”不可达”。GC 会在下一次回收时标记这些不可达对象，并释放它们的内存。两者的区别在于时机：显式置空（<code>obj = null</code>）可以提前告诉 GC 该对象已不需要，让 GC 在下次回收时就能处理它；而离开作用域则是隐式的，在栈帧弹出后引用自然消失。对于<strong>长生命周期</strong>的对象（如静态字段、单例引用），如果某个被引用的对象不再需要了，应当显式地将引用置空——否则这个引用永远不会离开作用域，相关对象也会一直无法被回收。</p>\n</blockquote>\n<h3 id=\"小心-List-的使用\"><a href=\"#小心-List-的使用\" class=\"headerlink\" title=\"小心 List 的使用\"></a>小心 List 的使用</h3><p>除了 <code>new</code> 某个类导致 GC 耗时增加外，以我的经验来看，很容易被我们忽略的是 <code>new List</code> 这种操作。我们在日常编程中大量使用动态数组，并且随时将它丢弃——因为它实在太容易创建和抛弃了。类似的，<code>Dictionary&lt;int, List&lt;T&gt;&gt;</code> 也是常被忽视的内存分配消耗来源之一：被装进 Dictionary 中的 List 经常被随意移除（Remove）并丢弃，却没有考虑过它是否能被再次利用。</p>\n<h3 id=\"Unity-提供的通用对象池工具\"><a href=\"#Unity-提供的通用对象池工具\" class=\"headerlink\" title=\"Unity 提供的通用对象池工具\"></a>Unity 提供的通用对象池工具</h3><p>我们应该尽可能使用<strong>对象池</strong>来重复利用已经创建的对象，而不是反复创建和销毁，这有助于减少内存分配的耗时，减少堆内存中的内存块数量，最终降低垃圾回收带来的 CPU 损耗。</p>\n<p>下面两个对象池的类是从 Unity 的 UI 库中提取出来的，都是非常实用的对象池工具，我们应该尽可能使用它们。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br><span class=\"line\">26</span><br><span class=\"line\">27</span><br><span class=\"line\">28</span><br><span class=\"line\">29</span><br><span class=\"line\">30</span><br><span class=\"line\">31</span><br><span class=\"line\">32</span><br><span class=\"line\">33</span><br><span class=\"line\">34</span><br><span class=\"line\">35</span><br><span class=\"line\">36</span><br><span class=\"line\">37</span><br><span class=\"line\">38</span><br><span class=\"line\">39</span><br><span class=\"line\">40</span><br><span class=\"line\">41</span><br><span class=\"line\">42</span><br><span class=\"line\">43</span><br><span class=\"line\">44</span><br><span class=\"line\">45</span><br><span class=\"line\">46</span><br><span class=\"line\">47</span><br><span class=\"line\">48</span><br><span class=\"line\">49</span><br><span class=\"line\">50</span><br><span class=\"line\">51</span><br><span class=\"line\">52</span><br><span class=\"line\">53</span><br><span class=\"line\">54</span><br><span class=\"line\">55</span><br><span class=\"line\">56</span><br><span class=\"line\">57</span><br><span class=\"line\">58</span><br><span class=\"line\">59</span><br><span class=\"line\">60</span><br><span class=\"line\">61</span><br><span class=\"line\">62</span><br><span class=\"line\">63</span><br><span class=\"line\">64</span><br><span class=\"line\">65</span><br><span class=\"line\">66</span><br><span class=\"line\">67</span><br><span class=\"line\">68</span><br><span class=\"line\">69</span><br><span class=\"line\">70</span><br><span class=\"line\">71</span><br><span class=\"line\">72</span><br><span class=\"line\">73</span><br><span class=\"line\">74</span><br><span class=\"line\">75</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">internal</span> <span class=\"keyword\">class</span> <span class=\"title\">ObjectPool</span>&lt;<span class=\"title\">T</span>&gt; <span class=\"keyword\">where</span> <span class=\"title\">T</span> : <span class=\"title\">new</span>() <span class=\"comment\">// T 必须要有一个无参构造函数</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 使用栈结构存储闲置对象，Push/Pop 操作均为 O(1)</span></span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">readonly</span> Stack&lt;T&gt; m_Stack = <span class=\"keyword\">new</span> Stack&lt;T&gt;();</span><br><span class=\"line\">    </span><br><span class=\"line\">    <span class=\"comment\">// 从池中取出对象时的回调，可用于初始化对象状态</span></span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">readonly</span> UnityAction&lt;T&gt; m_ActionOnGet;</span><br><span class=\"line\">    </span><br><span class=\"line\">    <span class=\"comment\">// 将对象归还池中时的回调，可用于重置对象状态</span></span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">readonly</span> UnityAction&lt;T&gt; m_ActionOnRelease;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 池总共创建过的对象数量</span></span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> countAll &#123; <span class=\"keyword\">get</span>; <span class=\"keyword\">private</span> <span class=\"keyword\">set</span>; &#125;</span><br><span class=\"line\">    <span class=\"comment\">// 当前正在使用的对象数量</span></span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> countActive &#123; <span class=\"keyword\">get</span> &#123; <span class=\"keyword\">return</span> countAll - countInactive; &#125; &#125;</span><br><span class=\"line\">    <span class=\"comment\">// 当前闲置在池中、可供取用的对象数量</span></span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> countInactive &#123; <span class=\"keyword\">get</span> &#123; <span class=\"keyword\">return</span> m_Stack.Count; &#125; &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"title\">ObjectPool</span>(<span class=\"params\">UnityAction&lt;T&gt; actionOnGet, UnityAction&lt;T&gt; actionOnRelease</span>)</span></span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        m_ActionOnGet = actionOnGet;</span><br><span class=\"line\">        m_ActionOnRelease = actionOnRelease;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 从池中获取一个对象（优先复用闲置对象，没有则新建）</span></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> T <span class=\"title\">Get</span>()</span></span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        T element;</span><br><span class=\"line\">        <span class=\"keyword\">if</span> (m_Stack.Count == <span class=\"number\">0</span>)</span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"comment\">// 池中无闲置对象，创建新实例并增加总数计数</span></span><br><span class=\"line\">            element = <span class=\"keyword\">new</span> T();</span><br><span class=\"line\">            countAll++;</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">        <span class=\"keyword\">else</span></span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"comment\">// 从栈顶弹出一个闲置对象复用</span></span><br><span class=\"line\">            element = m_Stack.Pop();</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">        <span class=\"comment\">// 调用 Get 回调（如果有的话），例如重新初始化对象状态</span></span><br><span class=\"line\">        <span class=\"keyword\">if</span> (m_ActionOnGet != <span class=\"literal\">null</span>)</span><br><span class=\"line\">            m_ActionOnGet(element);</span><br><span class=\"line\">        <span class=\"keyword\">return</span> element;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 将对象归还到池中</span></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">void</span> <span class=\"title\">Release</span>(<span class=\"params\">T element</span>)</span></span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"comment\">// 简单的安全检查：防止连续重复归还同一个对象</span></span><br><span class=\"line\">        <span class=\"keyword\">if</span> (m_Stack.Count &gt; <span class=\"number\">0</span> &amp;&amp; ReferenceEquals(m_Stack.Peek(), element))</span><br><span class=\"line\">            Debug.LogError(<span class=\"string\">&quot;Internal error. Trying to destroy object that is already released to pool.&quot;</span>);</span><br><span class=\"line\">        <span class=\"comment\">// 调用 Release 回调（如果有的话），例如清空对象的状态</span></span><br><span class=\"line\">        <span class=\"keyword\">if</span> (m_ActionOnRelease != <span class=\"literal\">null</span>)</span><br><span class=\"line\">            m_ActionOnRelease(element);</span><br><span class=\"line\">        <span class=\"comment\">// 将对象压入栈中以供后续复用</span></span><br><span class=\"line\">        m_Stack.Push(element);</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">internal</span> <span class=\"keyword\">static</span> <span class=\"keyword\">class</span> <span class=\"title\">ListPool</span>&lt;<span class=\"title\">T</span>&gt;</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 为 List&lt;T&gt; 创建专用的对象池</span></span><br><span class=\"line\">    <span class=\"comment\">// Get 时不需要额外操作，Release 时自动清空列表内容</span></span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">static</span> <span class=\"keyword\">readonly</span> ObjectPool&lt;List&lt;T&gt;&gt; s_ListPool = <span class=\"keyword\">new</span> ObjectPool&lt;List&lt;T&gt;&gt;(<span class=\"literal\">null</span>, l =&gt; l.Clear());</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> List&lt;T&gt; <span class=\"title\">Get</span>()</span></span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"keyword\">return</span> s_ListPool.Get();</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> <span class=\"keyword\">void</span> <span class=\"title\">Release</span>(<span class=\"params\">List&lt;T&gt; toRelease</span>)</span></span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        s_ListPool.Release(toRelease);</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>上述对象池使用栈（<code>Stack</code>）作为存储容器，将废弃的对象存储起来，在需要时从栈顶推出实例交给使用者。栈的出栈和入栈都是 O(1) 操作，非常高效。</p>\n<p>对象池本身并不复杂，复杂的是使用——程序中所有需要创建对象实例、销毁对象实例、移除对象实例的地方，都需要用对象池来管理。我们来看几个使用上述 <code>ObjectPool</code> 和 <code>ListPool</code> 的例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br><span class=\"line\">26</span><br><span class=\"line\">27</span><br><span class=\"line\">28</span><br><span class=\"line\">29</span><br><span class=\"line\">30</span><br><span class=\"line\">31</span><br><span class=\"line\">32</span><br><span class=\"line\">33</span><br><span class=\"line\">34</span><br><span class=\"line\">35</span><br><span class=\"line\">36</span><br><span class=\"line\">37</span><br><span class=\"line\">38</span><br><span class=\"line\">39</span><br><span class=\"line\">40</span><br><span class=\"line\">41</span><br><span class=\"line\">42</span><br><span class=\"line\">43</span><br><span class=\"line\">44</span><br><span class=\"line\">45</span><br><span class=\"line\">46</span><br><span class=\"line\">47</span><br><span class=\"line\">48</span><br><span class=\"line\">49</span><br><span class=\"line\">50</span><br><span class=\"line\">51</span><br><span class=\"line\">52</span><br><span class=\"line\">53</span><br><span class=\"line\">54</span><br><span class=\"line\">55</span><br><span class=\"line\">56</span><br><span class=\"line\">57</span><br><span class=\"line\">58</span><br><span class=\"line\">59</span><br><span class=\"line\">60</span><br><span class=\"line\">61</span><br><span class=\"line\">62</span><br><span class=\"line\">63</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">public</span> <span class=\"keyword\">class</span> <span class=\"title\">A</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> a;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">float</span> b;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 为 A 类型创建专用的对象池实例</span></span><br><span class=\"line\"><span class=\"keyword\">internal</span> <span class=\"keyword\">static</span> <span class=\"keyword\">class</span> <span class=\"title\">PoolA</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">static</span> <span class=\"keyword\">readonly</span> ObjectPool&lt;A&gt; s_Pool = <span class=\"keyword\">new</span> ObjectPool&lt;A&gt;(<span class=\"literal\">null</span>, <span class=\"literal\">null</span>);</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> A <span class=\"title\">Get</span>()</span> =&gt; s_Pool.Get();</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> <span class=\"keyword\">void</span> <span class=\"title\">Release</span>(<span class=\"params\">A obj</span>)</span> =&gt; s_Pool.Release(obj);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">void</span> <span class=\"title\">Main</span>()</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 示例 1：在 Dictionary 中管理对象池中的对象</span></span><br><span class=\"line\">    Dictionary&lt;<span class=\"built_in\">int</span>, A&gt; dic = <span class=\"keyword\">new</span> Dictionary&lt;<span class=\"built_in\">int</span>, A&gt;(<span class=\"number\">16</span>);</span><br><span class=\"line\">    <span class=\"keyword\">for</span> (<span class=\"built_in\">int</span> i = <span class=\"number\">0</span>; i &lt; <span class=\"number\">1000</span>; i++)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"comment\">// 从对象池中获取 A 实例，避免 new A() 导致的堆内存分配</span></span><br><span class=\"line\">        A a = PoolA.Get();</span><br><span class=\"line\">        a.a = i % <span class=\"number\">10</span>;   <span class=\"comment\">// 键在 0-9 之间循环，因此会重复命中同一个键</span></span><br><span class=\"line\">        a.b = <span class=\"number\">3.5f</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">        A item = <span class=\"literal\">null</span>;</span><br><span class=\"line\">        <span class=\"keyword\">if</span> (dic.TryGetValue(a.a, <span class=\"keyword\">out</span> item))</span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"comment\">// 键已存在（键会循环重复），覆盖前将旧对象归还对象池</span></span><br><span class=\"line\">            PoolA.Release(item);</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">        dic[a.a] = a;</span><br><span class=\"line\"></span><br><span class=\"line\">        <span class=\"comment\">// 随机移除某个键，被移除的对象归还对象池</span></span><br><span class=\"line\">        <span class=\"built_in\">int</span> removeKey = Random.RangeInt(<span class=\"number\">0</span>, <span class=\"number\">10</span>);</span><br><span class=\"line\">        <span class=\"keyword\">if</span> (dic.TryGetValue(removeKey, <span class=\"keyword\">out</span> item))</span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            PoolA.Release(item);</span><br><span class=\"line\">            dic.Remove(removeKey);</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 示例 2：使用 ListPool 管理 List 的生命周期</span></span><br><span class=\"line\">    Dictionary&lt;<span class=\"built_in\">int</span>, List&lt;A&gt;&gt; dic2 = <span class=\"keyword\">new</span> Dictionary&lt;<span class=\"built_in\">int</span>, List&lt;A&gt;&gt;(<span class=\"number\">1000</span>);</span><br><span class=\"line\">    <span class=\"keyword\">for</span> (<span class=\"built_in\">int</span> i = <span class=\"number\">0</span>; i &lt; <span class=\"number\">1000</span>; i++)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"comment\">// 从 ListPool 中获取 List 实例，避免 new List&lt;A&gt;() 导致的堆内存分配</span></span><br><span class=\"line\">        List&lt;A&gt; arrayA = ListPool&lt;A&gt;.Get();</span><br><span class=\"line\"></span><br><span class=\"line\">        dic2.Add(i, arrayA);</span><br><span class=\"line\"></span><br><span class=\"line\">        List&lt;A&gt; item = <span class=\"literal\">null</span>;</span><br><span class=\"line\">        <span class=\"built_in\">int</span> removeKey = Random.RangeInt(<span class=\"number\">0</span>, <span class=\"number\">1000</span>);</span><br><span class=\"line\">        <span class=\"keyword\">if</span> (dic2.TryGetValue(removeKey, <span class=\"keyword\">out</span> item))</span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"comment\">// 移除时将 List 归还对象池，Release 时 ListPool 会自动调用 Clear() 清空列表</span></span><br><span class=\"line\">            ListPool&lt;A&gt;.Release(item);</span><br><span class=\"line\">            dic2.Remove(removeKey);</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>上述代码中，<code>A</code> 类和 <code>List&lt;A&gt;</code> 各有 1000 次创建需求，每次创建都使用对象池，并在 Dictionary 移除时将对象送还对象池。这样一来，我们就能不断复用被”废弃”的对象，而不需要总是创建新的。所有被归还的对象都会被存储在池中，不会被垃圾回收器回收，最终内存被不断重复利用，从而减少了内存分配和释放带来的消耗。</p>\n<hr>\n<h2 id=\"四、字符串-String\"><a href=\"#四、字符串-String\" class=\"headerlink\" title=\"四、字符串 String\"></a>四、字符串 String</h2><p>字符串的性能问题在大部分语言中都是比较棘手的问题，C# 中尤其如此。在 C# 中 <code>string</code> 是引用类型，每次动态创建一个字符串时，C# 都会在堆内存中分配一块内存来存放字符串内容。我们来看看它到底有多”恐怖”。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">string</span> strA = <span class=\"string\">&quot;test&quot;</span>;</span><br><span class=\"line\"><span class=\"keyword\">for</span> (<span class=\"built_in\">int</span> i = <span class=\"number\">0</span>; i &lt; <span class=\"number\">100</span>; i++)</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"built_in\">string</span> strB = strA + i.ToString();        <span class=\"comment\">// 第 1 次内存分配：连接产生新字符串</span></span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"built_in\">string</span>[] strC = strB.Split(<span class=\"string\">&#x27;e&#x27;</span>);           <span class=\"comment\">// 第 2、3 次内存分配：分割产生两个新字符串</span></span><br><span class=\"line\"></span><br><span class=\"line\">    strB = strB + strC[<span class=\"number\">0</span>];                     <span class=\"comment\">// 第 4 次内存分配：连接产生新字符串</span></span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"built_in\">string</span> strD = <span class=\"built_in\">string</span>.Format(<span class=\"string\">&quot;Hello &#123;0&#125;, this is &#123;1&#125; and &#123;2&#125;.&quot;</span>, strB, strC[<span class=\"number\">0</span>], strC[<span class=\"number\">1</span>]);</span><br><span class=\"line\">    <span class=\"comment\">// 第 5 次内存分配：Format 产生新字符串</span></span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>这是一段”恐怖”的程序。循环中每一次迭代做了以下事情：</p>\n<ol>\n<li>将 <code>strA</code> 字符串和 <code>i</code> 的整数字符串连接，<code>strB</code> 获得的是从内存中<strong>新分配</strong>的字符串；</li>\n<li>将 <code>strB</code> 按 <code>&#39;e&#39;</code> 切割成 <code>strC</code>，切割出的两个字符串又<strong>新分配</strong>了两段内存；</li>\n<li>将 <code>strB</code> 与 <code>strC[0]</code> 连接，又再<strong>申请</strong>了一段内存来存放连接后的内容，并赋值给 <code>strB</code>，<code>strB</code> 原来指向的内容因为没有变量引用就被”丢弃”了；</li>\n<li>最后用 <code>string.Format</code> 将 4 个字符串组合起来，又<strong>新分配</strong>了一段内存来存放组合后的内容。</li>\n</ol>\n<p>这里需要注意一点：<strong>字符串常量不会被 GC 回收</strong>。比如这段程序中的 <code>&quot;test&quot;</code> 和 <code>&quot;Hello {0}, this is {1} and {2}.&quot;</code> 这两个常量，它们常驻于内存——即使没有变量指向它们，它们也不会被回收；下次使用时也不需要重新分配内存。</p>\n<blockquote>\n<p><strong>字符串常量与字符串驻留（String Interning）</strong><br>在 C# 中，编译期的字符串字面量会被自动**驻留（Intern）**到 CLR 管理的字符串驻留池（Intern Pool）中。驻留池是一个全局的哈希表，存储了所有唯一的字符串字面量。当代码中出现相同的字符串字面量时，编译器会让所有引用指向驻留池中的同一块内存。这就是为什么 <code>Object.ReferenceEquals(&quot;hello&quot;, &quot;hello&quot;)</code> 会返回 <code>true</code>——两个 <code>&quot;hello&quot;</code> 指向的是同一个驻留字符串。<br>动态创建的字符串（如通过拼接、<code>StringBuilder</code> 等生成的字符串）<strong>不会</strong>被自动驻留。如果希望手动将动态字符串加入驻留池，可以调用 <code>string.Intern()</code> 方法。需要注意的是，驻留的字符串在应用程序域的整个生命周期内都不会被 GC 回收，因此不要滥用驻留功能，否则可能导致内存持续增长。</p>\n</blockquote>\n<p>每次循环中，这段代码向内存申请了 5 次新的内存段，并且抛弃了 1 次（<code>strA + i.ToString()</code> 的结果因为没有变量指向而被丢弃）。这还不是最糟糕的——最糟糕的是，每次循环结束都会将前面分配的所有临时内存内容全部抛弃，重新再分配一次。如此反复申请和抛弃，总共向内存申请了约 500 次内存段，并全部抛弃，<strong>内存被浪费得非常严重</strong>。</p>\n<p>为什么会这样呢？究其原因，C# 对字符串<strong>没有任何缓存机制</strong>，每次操作都需要重新分配 <code>string</code> 内容。据我所知，很多语言都没有字符串的缓存机制，因此我们的字符串连接、切割、组合等操作都会向内存申请新的内容，并抛弃没有变量指向的旧字符串，等待 GC 回收。我们知道 GC 一次回收会消耗大量 CPU，如果我们不注意字符串的使用方式、不断浪费内存，将导致程序不定时卡顿。并且随着程序运行时间越来越长、各模块中不良代码的积累，程序卡顿的频率会逐渐升高，运行效率也会越来越差。</p>\n<h3 id=\"如何解决字符串问题\"><a href=\"#如何解决字符串问题\" class=\"headerlink\" title=\"如何解决字符串问题\"></a>如何解决字符串问题</h3><h4 id=\"1-自建缓存机制\"><a href=\"#1-自建缓存机制\" class=\"headerlink\" title=\"1. 自建缓存机制\"></a>1. 自建缓存机制</h4><p>可以用一些标志性的 Key 键值来一一对应字符串，比如游戏项目中常用 ID 来构造某个字符串：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">int</span> ID = <span class=\"number\">101</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">ResData resData = GetDataById(ID);</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"built_in\">string</span> strName = <span class=\"string\">&quot;This is &quot;</span> + resData.Name;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">return</span> strName;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>一个 ID 对应一个字符串的场景，我们可以建立一个字典容器将其缓存起来，下次使用时就不需要重新申请内存了。</p>\n</blockquote>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">Dictionary&lt;<span class=\"built_in\">int</span>, <span class=\"built_in\">string</span>&gt; strCache;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"built_in\">string</span> strName = <span class=\"literal\">null</span>;</span><br><span class=\"line\"><span class=\"keyword\">if</span> (!strCache.TryGetValue(id, <span class=\"keyword\">out</span> strName))</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 缓存中不存在，创建并存入缓存</span></span><br><span class=\"line\">    ResData resData = GetDataById(id);</span><br><span class=\"line\">    strName = <span class=\"string\">&quot;This is &quot;</span> + resData.Name;</span><br><span class=\"line\">    strCache.Add(id, strName);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">return</span> strName;</span><br></pre></td></tr></table></figure>\n\n<p>我们用 <code>Dictionary</code> 容器将字符串缓存起来，每次先查询字典中是否存在，有则直接使用，没有则创建一个并将其存入字典，以便下次复用。</p>\n<h4 id=\"2-使用不安全代码直接操作内存\"><a href=\"#2-使用不安全代码直接操作内存\" class=\"headerlink\" title=\"2. 使用不安全代码直接操作内存\"></a>2. 使用不安全代码直接操作内存</h4><p>由于 <code>string</code> 类本身的任何修改操作都会申请新的内存，因此我们需要突破这个限制——直接使用指针来修改 <code>string</code> 中字符的值，这样就能重复利用已分配的 <code>string</code> 内存，不需要反复重新分配。</p>\n<p>C# 虽然托管了大部分内存内容，但它也允许我们使用<strong>非托管方式</strong>来直接访问和修改内存——这就是 <code>unsafe</code> 关键字的用途。通过非托管方式，我们可以绕过 <code>string</code> 的不可变性，直接改变其中的字符内容，使已分配的字符串内存能被重复利用。来看一个例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">string</span> strA = <span class=\"string\">&quot;aaa&quot;</span>;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 使用 &quot;bbb&quot; + &quot;b&quot; 的方式生成新字符串，是为了避免修改常量字符串内存块</span></span><br><span class=\"line\"><span class=\"built_in\">string</span> strB = <span class=\"string\">&quot;bbb&quot;</span> + <span class=\"string\">&quot;b&quot;</span>;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 使用 fixed 关键字固定指针所指向的对象的内存</span></span><br><span class=\"line\"><span class=\"keyword\">fixed</span> (<span class=\"built_in\">char</span>* strA_ptr = strA)</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">fixed</span> (<span class=\"built_in\">char</span>* strB_ptr = strB)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"comment\">// 将 strA 的前 3 个字符内容直接拷贝到 strB 的内存中</span></span><br><span class=\"line\">        memcopy((<span class=\"built_in\">byte</span>*)strB_ptr, (<span class=\"built_in\">byte</span>*)strA_ptr, <span class=\"number\">3</span> * <span class=\"keyword\">sizeof</span>(<span class=\"built_in\">char</span>));</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">Console.WriteLine(strB); <span class=\"comment\">// 此时 strB 内容变为 &quot;aaab&quot;</span></span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>注意：这里特意用 <code>&quot;bbb&quot; + &quot;b&quot;</code> 而不是直接写 <code>&quot;bbbb&quot;</code>，是为了确保 <code>strB</code> 不是常量字符串内存块，而是运行时期动态分配的新字符串，这样我们修改它的内容才是安全的。</p>\n</blockquote>\n<p>其中 <code>memcopy</code> 并非系统函数，需要自己实现，写法也很简单——拿到两个指针，根据长度遍历并逐字节赋值：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">unsafe</span> <span class=\"keyword\">void</span> <span class=\"title\">memcopy</span>(<span class=\"params\"><span class=\"built_in\">byte</span>* dest, <span class=\"built_in\">byte</span>* src, <span class=\"built_in\">int</span> len</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">while</span> ((--len) &gt;= <span class=\"number\">0</span>)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        dest[len] = src[len];</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p><strong>注意</strong>：使用 <code>unsafe</code> 代码需要在项目设置中启用 “Allow unsafe code” 选项，且这类代码绕过了 C# 的内存安全检查，使用时需格外谨慎。</p>\n</blockquote>\n<p>我们把 <code>strB</code> 的前 3 个字符的内容替换成了 <code>strA</code> 中的内容，整个过程并没有增加任何新内存，因为我们使用了不安全的非托管方法直接操控内存。通过这种方式，我们可以将已分配的 <code>string</code> 缓存起来重复利用。下面来看一个实际应用的例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br><span class=\"line\">26</span><br><span class=\"line\">27</span><br><span class=\"line\">28</span><br><span class=\"line\">29</span><br><span class=\"line\">30</span><br><span class=\"line\">31</span><br><span class=\"line\">32</span><br><span class=\"line\">33</span><br><span class=\"line\">34</span><br><span class=\"line\">35</span><br><span class=\"line\">36</span><br><span class=\"line\">37</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 按字符串长度缓存可复用的字符串</span></span><br><span class=\"line\">Dictionary&lt;<span class=\"built_in\">int</span>, <span class=\"built_in\">string</span>&gt; cacheStr;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">unsafe</span> <span class=\"built_in\">string</span> <span class=\"title\">Concat</span>(<span class=\"params\"><span class=\"built_in\">string</span> strA, <span class=\"built_in\">string</span> strB</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"built_in\">int</span> a_length = strA.Length;</span><br><span class=\"line\">    <span class=\"built_in\">int</span> b_length = strB.Length;</span><br><span class=\"line\">    <span class=\"built_in\">int</span> sum_length = a_length + b_length;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"built_in\">string</span> strResult = <span class=\"literal\">null</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"keyword\">if</span> (!cacheStr.TryGetValue(sum_length, <span class=\"keyword\">out</span> strResult))</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"comment\">// 缓存中不存在该长度的字符串，直接连接后存入缓存</span></span><br><span class=\"line\">        strResult = strA + strB;</span><br><span class=\"line\">        cacheStr.Add(sum_length, strResult);</span><br><span class=\"line\">        <span class=\"keyword\">return</span> strResult;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"comment\">// 缓存中存在可用字符串，用指针直接修改其内容</span></span><br><span class=\"line\">    <span class=\"keyword\">fixed</span> (<span class=\"built_in\">char</span>* strA_ptr = strA)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        <span class=\"keyword\">fixed</span> (<span class=\"built_in\">char</span>* strB_ptr = strB)</span><br><span class=\"line\">        &#123;</span><br><span class=\"line\">            <span class=\"keyword\">fixed</span> (<span class=\"built_in\">char</span>* strResult_ptr = strResult)</span><br><span class=\"line\">            &#123;</span><br><span class=\"line\">                <span class=\"comment\">// 将 strA 的内容拷贝到 strResult 的开头</span></span><br><span class=\"line\">                memcopy((<span class=\"built_in\">byte</span>*)strResult_ptr, (<span class=\"built_in\">byte</span>*)strA_ptr, a_length * <span class=\"keyword\">sizeof</span>(<span class=\"built_in\">char</span>));</span><br><span class=\"line\"></span><br><span class=\"line\">                <span class=\"comment\">// 将 strB 的内容拷贝到 strResult 中 a_length 偏移之后的位置</span></span><br><span class=\"line\">                memcopy((<span class=\"built_in\">byte</span>*)strResult_ptr + a_length * <span class=\"keyword\">sizeof</span>(<span class=\"built_in\">char</span>), (<span class=\"built_in\">byte</span>*)strB_ptr, b_length * <span class=\"keyword\">sizeof</span>(<span class=\"built_in\">char</span>));</span><br><span class=\"line\">            &#125;</span><br><span class=\"line\">        &#125;</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"keyword\">return</span> strResult;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>当需要将两个字符串连接起来时，先在缓存中查找是否有可用长度的字符串：没有则直接连接并缓存；有则取出来，用指针方式直接修改缓存字符串的值。</p>\n<h4 id=\"3-C-自带方案\"><a href=\"#3-C-自带方案\" class=\"headerlink\" title=\"3. C# 自带方案\"></a>3. C# 自带方案</h4><p>上述 <code>unsafe</code> 方案的核心思想是”预分配一块可复用的缓冲区，用的时候直接往里面写内容”，这是一种教学性质的极端优化演示。在实际开发中，C# 自身已经提供了更安全、更易用的替代方案，其内核与我们手动实现的方式完全一致。</p>\n<h5 id=\"StringBuilder-缓存\"><a href=\"#StringBuilder-缓存\" class=\"headerlink\" title=\"StringBuilder 缓存\"></a>StringBuilder 缓存</h5><p><code>StringBuilder</code> 内部维护一个 <code>char[]</code> 缓冲区，其工作原理与上述手动按长度缓存 <code>string</code> 的思路如出一辙：<code>Append</code> 往缓冲区末尾填字符，<code>Clear()</code> 只把内部写指针归零，<strong>不释放底层的 <code>char[]</code> 数组</strong>，下次使用时直接覆盖写入，零额外分配。</p>\n<p>我们不需要为 <code>StringBuilder</code> 实现任何包装——只需要把 <code>StringBuilder</code> 实例本身缓存起来即可：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 缓存一个 StringBuilder 实例并反复使用</span></span><br><span class=\"line\">StringBuilder sb = <span class=\"keyword\">new</span> StringBuilder(<span class=\"number\">256</span>);  <span class=\"comment\">// 预估合理初始容量，避免内部数组扩容</span></span><br><span class=\"line\"></span><br><span class=\"line\">sb.Append(<span class=\"string\">&quot;Hello &quot;</span>);</span><br><span class=\"line\">sb.Append(name);</span><br><span class=\"line\">sb.Append(<span class=\"string\">&quot;, this is &quot;</span>);</span><br><span class=\"line\">sb.Append(<span class=\"number\">123</span>);</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"built_in\">string</span> result = sb.ToString();</span><br><span class=\"line\"></span><br><span class=\"line\">sb.Clear();  <span class=\"comment\">// 清空后下次直接复用，不产生新内存分配</span></span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p><strong>注意</strong>：<code>sb.ToString()</code> 仍然会产生一次新的 <code>string</code> 分配（这是不可避免的，因为你需要把结果拿出去）。但拼接过程中的中间字符串分配被完全消除了——这正是 <code>StringBuilder</code> 相比 <code>+</code> 拼接的优势所在。</p>\n</blockquote>\n<p>在 Unity 项目中，可以结合对象池来做更全面的管理：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">internal</span> <span class=\"keyword\">static</span> <span class=\"keyword\">class</span> <span class=\"title\">StringBuilderPool</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 使用前面介绍的 ObjectPool 来管理 StringBuilder 实例</span></span><br><span class=\"line\">    <span class=\"keyword\">private</span> <span class=\"keyword\">static</span> <span class=\"keyword\">readonly</span> ObjectPool&lt;StringBuilder&gt; s_Pool =</span><br><span class=\"line\">        <span class=\"keyword\">new</span> ObjectPool&lt;StringBuilder&gt;(<span class=\"literal\">null</span>, sb =&gt; sb.Clear());</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> StringBuilder <span class=\"title\">Get</span>()</span> =&gt; s_Pool.Get();</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">static</span> <span class=\"keyword\">void</span> <span class=\"title\">Release</span>(<span class=\"params\">StringBuilder sb</span>)</span> =&gt; s_Pool.Release(sb);</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>每次需要拼接字符串时从池中取，用完后归还，避免反复创建 <code>StringBuilder</code> 实例。以下是一个实际使用的例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 模拟每帧生成日志字符串的场景</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">BuildLogLine</span>(<span class=\"params\"><span class=\"built_in\">string</span> playerName, <span class=\"built_in\">int</span> score</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    StringBuilder sb = StringBuilderPool.Get();  <span class=\"comment\">// 从池中取出</span></span><br><span class=\"line\"></span><br><span class=\"line\">    sb.Append(<span class=\"string\">&quot;[&quot;</span>);</span><br><span class=\"line\">    sb.Append(playerName);</span><br><span class=\"line\">    sb.Append(<span class=\"string\">&quot;]  score: &quot;</span>);</span><br><span class=\"line\">    sb.Append(score);</span><br><span class=\"line\">    sb.Append(<span class=\"string\">&quot;, rank: &quot;</span>);</span><br><span class=\"line\">    sb.Append(GetRank(score));</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"built_in\">string</span> log = sb.ToString();                   <span class=\"comment\">// 转为 string 输出</span></span><br><span class=\"line\"></span><br><span class=\"line\">    StringBuilderPool.Release(sb);               <span class=\"comment\">// 用完归还池中，Release 回调自动 Clear</span></span><br><span class=\"line\">    <span class=\"comment\">// 之后同一实例可被其他调用者复用，无需 new StringBuilder()</span></span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>这个场景下，池中只需维持少数几个 <code>StringBuilder</code> 实例即可覆盖所有调用，对象复用率极高。</p>\n</blockquote>\n<blockquote>\n<p><code>StringBuilder</code> 的初始容量建议根据实际场景预估。如果预估容量偏小，内部 <code>char[]</code> 扩容时会产生一次数组拷贝——这同样是内存分配，应尽量避免。如果不确定容量，宁可稍微偏大一些。</p>\n</blockquote>\n<h5 id=\"string-Intern-驻留池\"><a href=\"#string-Intern-驻留池\" class=\"headerlink\" title=\"string.Intern 驻留池\"></a>string.Intern 驻留池</h5><p>对于<strong>字符串种类有限但反复出现</strong>的场景（比如配置表 key、网络协议字段名、资源路径等），可以利用 C# 自带的字符串驻留池（Intern Pool）：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 将动态字符串加入驻留池（如果池中已存在同内容的字符串，返回已有引用）</span></span><br><span class=\"line\"><span class=\"built_in\">string</span> cached = <span class=\"built_in\">string</span>.Intern(someDynamicString);</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 仅查找驻留池，不自动加入（不存在则返回 null）</span></span><br><span class=\"line\"><span class=\"built_in\">string</span> existing = <span class=\"built_in\">string</span>.IsInterned(someDynamicString);</span><br></pre></td></tr></table></figure>\n\n<p>驻留之后，所有相同内容的字符串都指向同一块内存，后续的 <code>==</code> 比较会命中 <code>ReferenceEquals</code> 而<strong>直接返回 <code>true</code></strong>，彻底避免逐字符遍历——这正是后文”字符串比较的隐藏开销”一节中提到的优化方向。</p>\n<blockquote>\n<p><strong>注意</strong>：驻留池的字符串在整个应用程序域生命周期内都不会被 GC 回收，因此只适合”种类有限”的场景。不要对高度动态变化、种类无穷的字符串（如用户输入、随机生成的内容）大量使用 <code>string.Intern</code>，否则驻留池会持续膨胀，反而导致内存泄漏。</p>\n</blockquote>\n<table>\n<thead>\n<tr>\n<th>方案</th>\n<th>安全性</th>\n<th>分配次数</th>\n<th>适用场景</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>StringBuilder</code> 缓存</td>\n<td>✅ 安全</td>\n<td>仅 <code>ToString()</code> 时 1 次</td>\n<td>拼接、格式化等通用场景</td>\n</tr>\n<tr>\n<td><code>string.Intern</code></td>\n<td>✅ 安全</td>\n<td>0（复用已有引用）</td>\n<td>种类有限的整串缓存（Key &#x2F; 路径）</td>\n</tr>\n<tr>\n<td>手动 <code>unsafe</code> + 缓存</td>\n<td>⚠️ 不安全</td>\n<td>0</td>\n<td>极端优化场景，不建议日常使用</td>\n</tr>\n</tbody></table>\n<h3 id=\"字符串的其他隐藏问题\"><a href=\"#字符串的其他隐藏问题\" class=\"headerlink\" title=\"字符串的其他隐藏问题\"></a>字符串的其他隐藏问题</h3><h4 id=\"Length-属性并非-O-1\"><a href=\"#Length-属性并非-O-1\" class=\"headerlink\" title=\"Length 属性并非 O(1)\"></a>Length 属性并非 O(1)</h4><p><code>string</code> 的 <code>Length</code> 属性可不是省油的灯。由于 C# 的 <code>string</code> 类中只存放了 <code>char[]</code> 数据，没有单独存储长度值的字段，因此取长度时需要通过遍历字符串来找到结尾的 <code>\\0</code> 字符。类似于如下伪代码：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">int</span> sum = <span class=\"number\">0</span>;</span><br><span class=\"line\"><span class=\"keyword\">for</span> (; data[sum] != <span class=\"string\">&#x27;\\0&#x27;</span>; ++sum) ;</span><br><span class=\"line\"><span class=\"keyword\">return</span> sum;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>如果我们大量使用 <code>Length</code>，就会浪费很多 CPU，因为每次调用它都会遍历整个字符串。好在实际开发中，我们通常不会在循环中反复访问同一字符串的 <code>Length</code>——一种简单的优化是将其缓存到局部变量中。</p>\n</blockquote>\n<h4 id=\"字符串比较的隐藏开销\"><a href=\"#字符串比较的隐藏开销\" class=\"headerlink\" title=\"字符串比较的隐藏开销\"></a>字符串比较的隐藏开销</h4><p>字符串比较也有隐藏的性能问题。当两个字符串进行比较时，<code>string</code> 的 <code>Equals</code> 方法首先会比较两个字符串的<strong>引用指针</strong>是否一致，一致则直接返回 <code>true</code>。如果指针不一致，则会先比较两者的<strong>长度</strong>是否相等，长度不同则直接返回 <code>false</code>；长度相同才会逐个遍历每一个字符，判断它们是否全部相等。我们来看看 <code>Equals</code> 的实际源码：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"built_in\">bool</span> <span class=\"title\">Equals</span>(<span class=\"params\">String <span class=\"keyword\">value</span></span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">if</span> (<span class=\"keyword\">this</span> == <span class=\"literal\">null</span>)                        <span class=\"comment\">// 防止 reverse-pinvoke 等场景的空引用</span></span><br><span class=\"line\">        <span class=\"keyword\">throw</span> <span class=\"keyword\">new</span> NullReferenceException();</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"keyword\">if</span> (<span class=\"keyword\">value</span> == <span class=\"literal\">null</span>)</span><br><span class=\"line\">        <span class=\"keyword\">return</span> <span class=\"literal\">false</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"keyword\">if</span> (Object.ReferenceEquals(<span class=\"keyword\">this</span>, <span class=\"keyword\">value</span>)) <span class=\"comment\">// 第一步：比较引用是否相同</span></span><br><span class=\"line\">        <span class=\"keyword\">return</span> <span class=\"literal\">true</span>;</span><br><span class=\"line\">    </span><br><span class=\"line\">    <span class=\"keyword\">if</span> (<span class=\"keyword\">this</span>.Length != <span class=\"keyword\">value</span>.Length)         <span class=\"comment\">// 第二步：比较长度是否相同</span></span><br><span class=\"line\">        <span class=\"keyword\">return</span> <span class=\"literal\">false</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"keyword\">return</span> EqualsHelper(<span class=\"keyword\">this</span>, <span class=\"keyword\">value</span>);        <span class=\"comment\">// 第三步：逐字符遍历比较</span></span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>这段代码从源码中提取。在判空之后，依次进行：引用比较 → 长度比较 → 逐字符遍历。只有当前两步都无法得出结论时，才会进入最昂贵的逐字符比较。</p>\n</blockquote>\n<p>这意味着，如果两个字符串内容相同但来自不同的内存段（例如分别通过拼接得到），那么比较它们时就不得不遍历所有字符。如下示例：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">string</span> strA = <span class=\"string\">&quot;Hello &quot;</span>;</span><br><span class=\"line\"><span class=\"built_in\">string</span> strB = <span class=\"string\">&quot;Hello &quot;</span>;</span><br><span class=\"line\"></span><br><span class=\"line\">strA = strA + <span class=\"string\">&quot;C&quot;</span>;   <span class=\"comment\">// strA 在堆上新分配一块内存</span></span><br><span class=\"line\">strB = strB + <span class=\"string\">&quot;C&quot;</span>;   <span class=\"comment\">// strB 也在堆上新分配一块内存（与 strA 不同）</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 虽然 strA 和 strB 内容完全相同（都是 &quot;Hello C&quot;），但它们位于不同的内存段</span></span><br><span class=\"line\"><span class=\"comment\">// 引用比较（ReferenceEquals）会返回 false</span></span><br><span class=\"line\"><span class=\"comment\">// 因此 Equals 会进入逐字符遍历比较</span></span><br><span class=\"line\"><span class=\"keyword\">if</span> (strA == strB)</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">&quot;相等&quot;</span>); <span class=\"comment\">// 最终结果：相等，但经过了昂贵的遍历比较</span></span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<blockquote>\n<p>在这个例子中，<code>strA</code> 和 <code>strB</code> 内容完全相同，但由于分别通过拼接产生，位于不同的内存地址，因此 <code>ReferenceEquals</code> 返回 <code>false</code>，<code>Equals</code> 不得不进入逐字符遍历。如果这样的比较频繁发生，累积的性能损耗也不容忽视。将字符串缓存起来（如前文所述），让相同内容的字符串指向同一块内存，就能让 <code>ReferenceEquals</code> 快速返回 <code>true</code>，从而避免遍历。</p>\n</blockquote>\n<hr>\n<h2 id=\"参考资料\"><a href=\"#参考资料\" class=\"headerlink\" title=\"参考资料\"></a>参考资料</h2><ul>\n<li><a href=\"http://www.luzexi.com/2019/12/31/Unity3D%E9%AB%98%E7%BA%A7%E7%BC%96%E7%A8%8B%E4%B9%8B%E8%BF%9B%E9%98%B6%E4%B8%BB%E7%A8%8B-%E7%A8%8B%E5%BA%8F%E9%80%BB%E8%BE%91%E4%BC%98%E5%8C%96%E6%8A%80%E5%B7%A7\">《Unity3D高级编程之进阶主程》 第一章，程序逻辑优化技巧 - 技术人生 - 编程技术 - JESSE人生</a></li>\n</ul>\n","path":"posts/3106393728/","permalink":"https://littlefish04.github.io/blog/posts/3106393728/","tags":[{"name":"C#基础","_id":"cuidEVxotwXSTgQIZdC1V3uXF","slug":"C-基础","path":"tags/C-基础/","permalink":"https://littlefish04.github.io/blog/tags/C-%E5%9F%BA%E7%A1%80/","length":3}],"categories":[{"name":"Unity 基础学习","_id":"cuid7HSfdH4tzZIrbuEqDRY72","slug":"Unity-基础学习","path":"categories/Unity-基础学习/","permalink":"https://littlefish04.github.io/blog/categories/Unity-%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0/","length":3}],"prev":null,"next":{"title":"委托、事件、装箱、拆箱","date":"2026-07-22T13:53:07.000Z","summary":"深入分析 C# 委托、事件的底层实现原理，以及装箱拆箱的内部机制与性能优化方法。","slug":"委托、事件、装箱、拆箱","published":true,"updated":"2026-07-24T06:59:58.090Z","_id":"cuidCZeIey9KAzCSG38oKiQAR","layout":"post","photos":[],"excerpt":"","path":"posts/4235838758/","permalink":"https://littlefish04.github.io/blog/posts/4235838758/","__post":true},"__post":true}