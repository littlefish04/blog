{"title":"委托、事件、装箱、拆箱","date":"2026-07-22T13:53:07.000Z","toc":true,"summary":"深入分析 C# 委托、事件的底层实现原理，以及装箱拆箱的内部机制与性能优化方法","source":"_posts/委托、事件、装箱、拆箱.md","raw":"---\ntitle: 委托、事件、装箱、拆箱\ncategories: Unity 基础学习\ntoc: true\ndate: 2026-07-22 21:53:07\ntags:\n  - C#基础\nsummary: 深入分析 C# 委托、事件的底层实现原理，以及装箱拆箱的内部机制与性能优化方法\ndescription: 本文从值类型与引用类型的区别出发，深入剖析 C# 委托的底层链表结构、event 关键字的封装原理，以及装箱拆箱的内部操作过程。同时结合 struct 的特性，给出了重载虚方法、泛型约束、统一接口提前装箱三种实用优化策略，帮助你写出更高效的 C# 代码。\n---\n\n---\n## 写在前面\n\n这个分类下的文章是我对 《Unity3D高级编程之进阶主程》一书的学习笔记。\n\n这个博客对应的章节是 \n\n- 第一章，C#要点技术(四) 委托、事件、装箱、拆箱\n\n部分内容为ai生成，如有错误恳请指出。\n\n---\n\n## 一、值类型和引用类型\n\n值类型包括所有整数、浮点数、`bool`，以及用 `struct` 声明的结构。这里要特别留意 `struct`——它是经常容易犯错误的地方。很多人会把 `struct` 当作类来使用，这是完全错误的。因为它是值类型，在赋值操作时通过**直接拷贝数据**完成，所以常会出现这样的困惑：`a`、`b` 同是结构体实例，`a` 赋值给 `b`，当 `b` 更改了数据后却发现 `a` 的数据没有同步。事实上它们根本就是两块独立的数据空间——当 `a` 赋值给 `b` 时并不是引用拷贝，而是**整个数据空间的拷贝**，相当于有了两个不同的西瓜，只是长得比较像而已。\n\n```csharp\n// struct 是值类型，赋值 = 完整拷贝\nstruct Point\n{\n    public int X;\n    public int Y;\n}\n\nPoint a = new Point { X = 1, Y = 2 };\nPoint b = a;          // 把 a 的所有数据完整拷贝给 b\nb.X = 10;             // 修改 b 的 X\n\nConsole.WriteLine(a.X);  // 输出 1 —— a 不受影响\nConsole.WriteLine(b.X);  // 输出 10\n```\n\n引用类型包括类、接口、委托（委托也是类）、数组，以及内置的 `object` 与 `string`。在 C# 里万物皆是类——虽然 `int` 等值类型本质上也是类，只不过它们是特殊的、值类型的类。\"类都是引用类型\"虽然不算完全精确，但也不妨碍它作为一个好记的口诀。\n\n值类型的变量会**直接存储数据**，而引用类型的变量持有的是**数据的引用**，其真实数据存储在托管堆中。当声明一个引用类型变量时，只在栈（或堆）中分配一小片内存用于容纳一个地址，此时并没有为其分配堆上的内存空间，因此它为 `null`；直到使用 `new` 创建实例时，才在堆上分配空间，并把堆上空间的地址保存给这个引用变量，这时引用变量才真正指向了有效的内存。\n\n这也解释了为什么 `struct` 和类的赋值表现完全不同。`struct` 赋值时，是把整块数据拷贝过去——被赋值的变量已经在一块全新的内存里存储了数据，与原变量再无瓜葛。而类的赋值，只是把数据的**引用**拷贝过去——被赋值的变量和赋给它的变量里存着**同一个地址**，指向堆上的同一块数据空间。所以 `b` 更改了数据之后，再用 `a` 访问，看到的也是被改过的那块数据。\n\n---\n\n## 二、委托和事件\n\n### 委托\n\n在 C# 中万物皆是类，大部分时间里都看不到指针的身影，最多也只是引用，因为指针被封装在内部函数当中。但回调函数的需求依然存在，于是 C# 引入了**委托（delegate）**的概念，所有函数指针的功能都以委托的方式来达成。委托可以被视为一个更高级的函数指针——它不仅能将地址指向另一个函数，还能传递参数、获取返回值等。系统还为委托对象自动生成了同步和异步的调用方式，开发人员使用 `BeginInvoke`、`EndInvoke` 方法就可以避开 `Thread` 类，直接实现多线程调用。\n\n#### 委托的常见语法\n\n```csharp\n// 1. 声明委托类型\npublic delegate int CalculateDelegate(int x, int y);\n\n// 2. 创建委托实例\nCalculateDelegate calc = new CalculateDelegate(Add);\n\n// 3. 使用委托——像调用方法一样\nint result = calc(3, 5);        // 输出 8\nresult = calc.Invoke(3, 5);     // 与上一行等价\n\n// 4. 多播委托 —— 一个委托挂载多个方法，Invoke()会按顺序依次调用委托中的所有方法\ncalc += Subtract;               // 添加方法\ncalc -= Add;                    // 移除方法\n\nstatic int Add(int a, int b) => a + b; // lambda 表达式\nstatic int Subtract(int a, int b) => a - b;\n```\n\n不过在实际开发中，较少直接使用自定义委托，而是使用 C# 为我们准备好的 `Action` 和 `Func` 这两个泛型委托。两者的区别在于：\n\n| 委托 | 返回值 | 示例 |\n|------|--------|------|\n| `Action` | 无返回值 | `Action<string> print = Console.WriteLine;` |\n| `Action<T1, T2, ...>` | 无返回值 | `Action<int, int> log = (x, y) => Console.WriteLine(x + y);` |\n| `Func<T, TResult>` | 有返回值（最后一个泛型参数是返回类型） | `Func<int, int, int> add = (a, b) => a + b;` |\n\n```csharp\n// Action 示例 —— 无返回值\nAction<string> log = msg => Console.WriteLine($\"[LOG] {msg}\");\nlog(\"Hello\");   // 输出：[LOG] Hello\n\n// Func 示例 —— 有返回值\nFunc<int, int, int> multiply = (a, b) => a * b;\nint product = multiply(3, 4);   // 12\n```\n\n#### 委托的底层实现\n\n我们在创建委托时，实际上就是创建了一个委托类实例。这个委托类继承自 `System.MulticastDelegate`，类实例中包含 `BeginInvoke`、`EndInvoke`、`Invoke` 三个方法，分别对应异步开始调用、结束异步调用和直接调用。\n\n`Delegate` 类中有一个变量用于存储函数地址。当使用 `=` 操作时，把函数地址赋值给该变量存起来。不过这个存储函数地址的变量实际上是一个**调用列表（invocation list）**，其内部结构为一个链表：委托实例持有 `_prev` 字段指向链表中的前一个委托节点。当链表中只有一个方法时，`_prev` 为 `null`；当再 `+=` 一个方法时，实际是创建了一个新的 `MulticastDelegate` 实例，其 `_prev` 指向原来的实例，以此串成链表。注意，每 `+=` 一次都会生成一个新实例并串到链表头部，所以 `_prev` 链的方向是**新 → 旧**。当委托被调用（`Invoke`）时，CLR 会先沿着 `_prev` 链走到最末端（即最早添加的节点），然后**从旧到新**依次调用每个节点的方法，保证回调顺序与注册顺序一致。\n\n`MulticastDelegate` 还重写了 `+=` 和 `-=` 这两个操作符，其内部实现对应的就是 `Delegate.Combine` 和 `Delegate.Remove` 静态方法：对委托执行 `+=` 相当于把新的函数地址加入调用列表；执行 `-=` 则是从调用列表中移除该函数。\n\n看到这里我们就彻底明白了：`delegate` 关键字本质上是一个语法糖，背后由 C# 编译器将其重写为一个继承自 `System.MulticastDelegate` 的类。\n\n### 事件\n\n#### 事件的常见语法\n\n```csharp\n// 1. 声明事件（基于已有的委托类型）\npublic event Action<string> OnMessageReceived;\n\n// 2. 注册事件\nOnMessageReceived += HandleMessage;\n\n// 3. 注销事件\nOnMessageReceived -= HandleMessage;\n\n// 4. 触发事件（只能在声明事件的类内部调用）\nOnMessageReceived?.Invoke(\"Hello World\");\n\n// 事件处理方法\nvoid HandleMessage(string msg)\n{\n    Console.WriteLine($\"收到消息：{msg}\");\n}\n```\n\n#### 事件的底层实现\n\n`event` 很简单，它在委托（delegate）的基础上又做了一次封装。这次封装的意义在于：**限制用户直接操作委托实例中变量的权限**。\n\n如果不使用 `event`，委托字段就像一个普通的公共变量，外部代码可以直接对它执行 `=` 赋值，从而清空之前所有订阅的方法。而 `event` 关键字本质上会生成类似属性（property）的 `add` 和 `remove` 访问器，将底层的委托字段设为私有，外部只能通过 `+=`/`-=` 来增减方法，无法直接赋值覆盖。其原理大致如下：\n\n```csharp\n// event 背后的等价封装（简化示意）\nprivate Action<string> _onMessage;   // 底层委托字段是私有的\n\npublic event Action<string> OnMessage\n{\n    add    { _onMessage += value; }   // 只能通过 += 注册\n    remove { _onMessage -= value; }   // 只能通过 -= 注销\n}\n```\n\n封装之后，用户不再能够用赋值操作（即 `=` 操作符）来直接改变委托变量，只能通过注册或注销委托的方式来增减委托函数的数量。也就是说，被 `event` 声明的委托不再对外暴露 `=` 操作，但仍然保留了 `+=` 和 `-=` 操作符可供使用。\n\n为什么要做这样的限制呢？在平时的编程中，项目庞大、经手人员众多，我们常常无法得知其他人在编写的代码意图是什么。如果委托字段直接暴露在外，随时可能被 `=` 赋值而**清空前面累积起来的委托链表**，操作的权限范围太大，导致 bug 难以排查。而声明为 `event` 后，编译器在内部重新封装了委托，让暴露在外的委托不再有被随时清空和重置的风险——只能一个个注册、一个个注销，这样就保证了\"谁注册谁负责销毁\"的秩序，更好地维护了委托链的完整。\n\n---\n\n## 三、装箱和拆箱\n\n什么是装箱和拆箱？其实很简单：**把值类型实例转换为引用类型实例，就是装箱；反过来，把引用类型实例转换为值类型实例，就是拆箱。**\n\n举个例子：\n\n```csharp\nint a = 5;\nobject obj = a;     // 装箱\n```\n\n`a` 是值类型，是直接持有数据的变量；`obj` 是引用类型，指针与数据是分离的。把 `a` 赋值给 `obj`，实际上就是 `obj` 在堆上开辟了一块新的数据空间，拷贝了 `a` 的数据，并为自己创建了一个指针指向这块数据空间。接着上面的代码继续：\n\n```csharp\nint b = (int)obj;   // 拆箱\n```\n\n这就是拆箱——把 `obj` 指向的内存空间复制一份交给 `b`。因为 `b` 是值类型，它不允许指向某个内存空间，只能靠复制数据来传递数据。\n\n### 堆内存和栈内存\n\n这里稍微阐述一下堆和栈内存，因为很多人对此有错误的认识。为什么要有栈内存和堆内存之分？用简短的语言来说明：\n\n> 栈是本着**先进后出（LIFO）**原则的存储机制，它是一段**连续的内存**，所以对栈数据的定位比较快速。而堆则是**随机分配**的空间，处理的数据比较多，无论如何至少要两次定位。堆内存的创建和删除节点的时间复杂度是 O(log n)，栈创建和删除的时间复杂度则是 O(1)，栈速度更快。\n>\n> 那么既然栈速度这么快，全部用栈不就好了？这又涉及生命周期的问题。栈中的生命周期是必须确定的，销毁时必须按次序从最后分配的块开始销毁，创建后什么时候销毁必须是一个定值，所以在分配和销毁时不灵活，基本只用于函数调用和递归调用这类生命周期比较确定的地方。相反，堆内存可以存放生命周期不确定的内存块，满足\"需要时再删除\"的需求，所以堆内存更适合全局性质的、需要灵活分配和销毁的内存块。\n\n很多人把\"值类型放在栈中、引用类型放在堆中\"当作绝对规律，这是错误的。栈内存主要为**确定性生命周期**的内存服务，堆内存则更多服务于**无序的、随时可能释放**的内存。因此值类型既可以在堆内也可以在栈内，引用类型的指针部分同样可以在栈内或堆内——但引用类型**指向的内存块**一定在堆内。\n\n### 什么时候会用到装箱\n\n大部分时候只有当程序、逻辑或接口需要更加通用时，才会需要装箱。比如：\n\n```csharp\n// 情况一：调用含 object 类型参数的方法\nvoid PrintObject(object obj)\n{\n    Console.WriteLine(obj);\n}\n\nint value = 42;\nPrintObject(value);     // int → object，发生装箱\n\n// 情况二：非泛型容器，元素类型定义为 object\nArrayList list = new ArrayList();\nlist.Add(100);          // int → object，发生装箱\nlist.Add(\"hello\");      // string 本身就是引用类型，不装箱\n```\n\n当你需要将一个值类型（如 `int`）传入参数为 `object` 的方法时，就需要装箱。又比如一个非泛型的容器（如 `ArrayList`）为了保证通用，将元素类型定义为 `object`，值类型数据放入时就会触发装箱。\n\n### 装箱和拆箱的内部操作\n\n**装箱**：根据相应的值类型在堆中分配一个内存块，再将数据拷贝给它。具体按以下三步进行：\n\n1. **第一步**：在堆内存中新分配一个内存块（大小为值类型实例大小加上一个方法表指针和一个 `SyncBlockIndex`）。\n2. **第二步**：将值类型的实例字段拷贝到新分配的内存块中。\n3. **第三步**：返回堆中新分配对象的地址——这个地址就是一个指向对象的引用了。\n\n**拆箱**则更为简单：先检查对象实例，确保它是给定值类型的一个装箱值，再将该值从实例复制到值类型变量的内存块中。\n\n### 装箱、拆箱对执行效率的影响及优化\n\n由于装箱和拆箱时生成的是全新的对象，不断地分配和销毁内存不仅大量消耗 CPU，也会增加内存碎片，从而降低性能。那该如何优化呢？\n\n最关键的就是**减少装箱和拆箱操作**——在编程规范中要牢记：装箱和拆箱是消耗较大的操作，写代码时需要特别留意。\n\n整数、浮点数、布尔等数值型变量变化手段不多，主要靠加强编码规范来减少装拆箱。而 `struct` 有点不一样——它既是值类型，又可以像类一样实现接口，用途更多样、转换途径也更多，稍不留神就会埋下性能隐患。所以这里重点讲讲 `struct` 的优化方法。\n\n#### 通过重载函数来避免装箱\n\n比如常用的 `ToString()`、`GetHashCode()`、`Equals()` 方法：如果 `struct` 没有重写这些方法，调用它们时会先装箱再调用，导致内存分配的性能损耗。\n\n为什么 struct 调用这些方法需要先装箱？因为这些方法的默认实现定义在 `System.Object` 或 `System.ValueType` 基类中，都是虚方法（`virtual`）。当 struct 没有重写它们时，虚方法调用需要查方法表（vtable），而方法表存储在引用类型对象中——值类型实例本身不携带方法表指针。因此必须先将 struct 装箱为一个引用类型对象，才能完成虚方法的派发。例如：\n\n```csharp\nstruct MyStruct\n{\n    public int Value;\n}\n\nMyStruct s = new MyStruct { Value = 10 };\ns.ToString();     // MyStruct 没有重写 ToString → 先装箱再调用 → 产生 GC 垃圾\n```\n\n而要避免这一点很简单——在 struct 中手动重写这些方法即可：\n\n```csharp\nstruct MyStruct\n{\n    public int Value;\n\n    public override string ToString() => Value.ToString();  // 重载后不再装箱\n    public override int GetHashCode() => Value.GetHashCode();\n    public override bool Equals(object obj) => obj is MyStruct s && Value == s.Value;\n}\n```\n\n所以，对于那些需要调用的引用类型方法，struct 必须逐一**手动重载**。\n\n#### 通过泛型来避免装箱\n\n别忘了 struct 也是可以实现接口的。在不同的、相似的、具有父子关系的 struct 之间，可以用泛型来传递参数，这样就不用装箱后再传递了。\n\n```csharp\ninterface IPrintable { void Print(); }\n\nstruct Document : IPrintable\n{\n    public string Name;\n    public void Print() => Console.WriteLine($\"打印：{Name}\");\n}\n\n// 不使用泛型 —— 传参时装箱\nvoid ProcessByInterface(IPrintable printable)\n{\n    printable.Print();\n}\n\n// 使用泛型 —— 不会装箱\nvoid ProcessByGeneric<T>(T printable) where T : IPrintable\n{\n    printable.Print();\n}\n\nDocument doc = new Document { Name = \"报告\" };\nProcessByInterface(doc);   // doc 转为 IPrintable → 装箱\nProcessByGeneric(doc);     // 泛型直接约束为 IPrintable，不装箱\n```\n\n为什么传给 `IPrintable` 类型的参数就需要装箱？原因是 struct 实例本质上只是栈上的一段**纯数据**，没有对象头（Object Header），也没有方法表指针（MethodTable Pointer）。而接口方法调用属于**虚分派（virtual dispatch）**，CLR 必须通过方法表来查找实际要调用的方法——栈上的 struct 不携带这些信息，所以必须先装箱，在堆上生成一个带有完整对象头和接口映射表（Interface Map）的包装对象，才能完成接口分派。\n\n```csharp\n// 通过具体类型直接调用 —— 不装箱\nDocument doc = new Document();\ndoc.Print();     // 编译器知道 doc 就是 Document，直接静态调用\n\n// 通过接口引用调用 —— 需要装箱\nIPrintable ip = doc;   // doc 装箱：在堆上创建带方法表指针的副本\nip.Print();            // 通过接口分派，走方法表查找\n```\n\n而泛型则绕过了这个问题。由于 JIT 编译时会对每个值类型生成**专用版本**（代码特化），当 `T` 是 `Document` 时，JIT 生成的方法内部等价于直接接收 `Document` 类型的参数。调用 `Print()` 时走的是**具体类型的静态分派**，而不是接口虚分派——`where T : IPrintable` 约束仅用于编译期检查，确保 `T` 实现了 `Print()`，运行时完全不经过接口查表，因此全程不需要装箱。\n\n#### 通过继承统一的接口提前装箱，避免多次重复拆装箱\n\n很多时候拆装箱不可避免，那么我们就让多种 struct 继承某个统一的接口。这样不同 struct 就可以用相同的接口访问。将 struct 传递到其他方法时，相当于**提前进行了一次装箱操作**——在方法中得到的是引用类型的值，并且具备所需的接口，避免了在方法内部重复多次的拆装箱。\n\n```csharp\ninterface IShape\n{\n    double Area();\n}\n\nstruct Square : IShape\n{\n    public double Side;\n    public double Area() => Side * Side;\n}\n\nstruct Circle : IShape\n{\n    public double Radius;\n    public double Area() => Math.PI * Radius * Radius;\n}\n\n// Test 方法接收 IShape 引用类型，struct 传入时装箱一次\nvoid Test(IShape shape)\n{\n    // 方法内部可以多次调用 shape.Area()、shape 的方法等\n    // 不会再发生装箱，因为 shape 已经是引用类型\n    Console.WriteLine($\"面积：{shape.Area()}\");\n    Console.WriteLine($\"类型：{shape.GetType().Name}\");\n}\n\nSquare square = new Square { Side = 5 };\nCircle circle = new Circle { Radius = 3 };\n\nTest(square);   // Square → IShape，装箱一次，内部不再重复装箱\nTest(circle);   // Circle → IShape，装箱一次，内部不再重复装箱\n```\n\n对比一下：如果没有统一的接口，Square 和 Circle 又都需要传入同一个方法处理，就只能退回到用 `object` 来接收：\n\n```csharp\n// 没有统一接口 → 用 object 凑合\nvoid ProcessWithoutInterface(object obj)\n{\n    // 每次访问具体字段/方法，都要先拆箱\n    if (obj is Square)\n    {\n        Square s = (Square)obj;         // 拆箱！\n        Console.WriteLine($\"正方形面积：{s.Side * s.Side}\");\n    }\n    else if (obj is Circle)\n    {\n        Circle c = (Circle)obj;         // 拆箱！\n        Console.WriteLine($\"圆形面积：{Math.PI * c.Radius * c.Radius}\");\n    }\n    // 如果方法里还要再做其他操作（比如打印类型名、序列化等），\n    // 每次都从 object 里拆出来，拆箱次数 = if 分支数 × 操作次数\n}\n\nSquare square = new Square { Side = 5 };\nCircle circle = new Circle { Radius = 3 };\n\nProcessWithoutInterface(square);  // square → object（装箱）\nProcessWithoutInterface(circle);  // circle → object（装箱）\n// 更糟的是：方法内部每次 (Square)obj / (Circle)obj 都在拆箱\n// 如果用 is 做类型判断再强转，等于拆了两次（is 一次，强转又一次）\n```\n\n可以看到，没有统一接口时：传入方法时**装箱一次**，但方法内部每次要访问具体数据就得**拆箱一次**——如果有 N 种 struct 类型、每种有 M 个操作，最坏情况下拆箱次数 = N × M。而使用统一接口后，只需在入口处装箱一次，整个方法体内都在跟已装箱的引用类型打交道，零额外开销。\n\n这种做法的优势在于：装箱发生在**方法入口处**，只做一次。方法内部操作的都是已装箱好的引用类型对象，无需再次拆装箱，从而将性能影响控制在最小范围。\n\n<div class=\"tip\">\n最后依然要提醒大家：struct 值类型数据结构如果没有真正理解它的原理，用起来可能会引发很多麻烦。切记不要盲目认为使用结构体一定会让性能提升——在没有完全彻底理解之前贸然大量使用 struct，可能会对你的程序性能带来重创。\n</div>\n\n---\n\n## 参考资料\n\n- [《Unity3D高级编程之进阶主程》第一章，C#要点技术(四) 委托、事件、装箱、拆箱 - 技术人生 - 编程技术 - JESSE人生](http://www.luzexi.com/2019/01/26/Unity3D%E9%AB%98%E7%BA%A7%E7%BC%96%E7%A8%8B%E4%B9%8B%E8%BF%9B%E9%98%B6%E4%B8%BB%E7%A8%8B-CSharp%E8%A6%81%E7%82%B9%E6%8A%80%E6%9C%AF4)","slug":"委托、事件、装箱、拆箱","published":true,"updated":"2026-07-22T15:30:00.530Z","_id":"cuidprmbDqsgXRu2EEPqIkIPY","comments":true,"layout":"post","photos":[],"html":"<hr>\n<h2 id=\"写在前面\"><a href=\"#写在前面\" class=\"headerlink\" title=\"写在前面\"></a>写在前面</h2><p>这个分类下的文章是我对 《Unity3D高级编程之进阶主程》一书的学习笔记。</p>\n<p>这个博客对应的章节是 </p>\n<ul>\n<li>第一章，C#要点技术(四) 委托、事件、装箱、拆箱</li>\n</ul>\n<p>部分内容为ai生成，如有错误恳请指出。</p>\n<hr>\n<h2 id=\"一、值类型和引用类型\"><a href=\"#一、值类型和引用类型\" class=\"headerlink\" title=\"一、值类型和引用类型\"></a>一、值类型和引用类型</h2><p>值类型包括所有整数、浮点数、<code>bool</code>，以及用 <code>struct</code> 声明的结构。这里要特别留意 <code>struct</code>——它是经常容易犯错误的地方。很多人会把 <code>struct</code> 当作类来使用，这是完全错误的。因为它是值类型，在赋值操作时通过<strong>直接拷贝数据</strong>完成，所以常会出现这样的困惑：<code>a</code>、<code>b</code> 同是结构体实例，<code>a</code> 赋值给 <code>b</code>，当 <code>b</code> 更改了数据后却发现 <code>a</code> 的数据没有同步。事实上它们根本就是两块独立的数据空间——当 <code>a</code> 赋值给 <code>b</code> 时并不是引用拷贝，而是<strong>整个数据空间的拷贝</strong>，相当于有了两个不同的西瓜，只是长得比较像而已。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// struct 是值类型，赋值 = 完整拷贝</span></span><br><span class=\"line\"><span class=\"keyword\">struct</span> Point</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> X;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> Y;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">Point a = <span class=\"keyword\">new</span> Point &#123; X = <span class=\"number\">1</span>, Y = <span class=\"number\">2</span> &#125;;</span><br><span class=\"line\">Point b = a;          <span class=\"comment\">// 把 a 的所有数据完整拷贝给 b</span></span><br><span class=\"line\">b.X = <span class=\"number\">10</span>;             <span class=\"comment\">// 修改 b 的 X</span></span><br><span class=\"line\"></span><br><span class=\"line\">Console.WriteLine(a.X);  <span class=\"comment\">// 输出 1 —— a 不受影响</span></span><br><span class=\"line\">Console.WriteLine(b.X);  <span class=\"comment\">// 输出 10</span></span><br></pre></td></tr></table></figure>\n\n<p>引用类型包括类、接口、委托（委托也是类）、数组，以及内置的 <code>object</code> 与 <code>string</code>。在 C# 里万物皆是类——虽然 <code>int</code> 等值类型本质上也是类，只不过它们是特殊的、值类型的类。”类都是引用类型”虽然不算完全精确，但也不妨碍它作为一个好记的口诀。</p>\n<p>值类型的变量会<strong>直接存储数据</strong>，而引用类型的变量持有的是<strong>数据的引用</strong>，其真实数据存储在托管堆中。当声明一个引用类型变量时，只在栈（或堆）中分配一小片内存用于容纳一个地址，此时并没有为其分配堆上的内存空间，因此它为 <code>null</code>；直到使用 <code>new</code> 创建实例时，才在堆上分配空间，并把堆上空间的地址保存给这个引用变量，这时引用变量才真正指向了有效的内存。</p>\n<p>这也解释了为什么 <code>struct</code> 和类的赋值表现完全不同。<code>struct</code> 赋值时，是把整块数据拷贝过去——被赋值的变量已经在一块全新的内存里存储了数据，与原变量再无瓜葛。而类的赋值，只是把数据的<strong>引用</strong>拷贝过去——被赋值的变量和赋给它的变量里存着<strong>同一个地址</strong>，指向堆上的同一块数据空间。所以 <code>b</code> 更改了数据之后，再用 <code>a</code> 访问，看到的也是被改过的那块数据。</p>\n<hr>\n<h2 id=\"二、委托和事件\"><a href=\"#二、委托和事件\" class=\"headerlink\" title=\"二、委托和事件\"></a>二、委托和事件</h2><h3 id=\"委托\"><a href=\"#委托\" class=\"headerlink\" title=\"委托\"></a>委托</h3><p>在 C# 中万物皆是类，大部分时间里都看不到指针的身影，最多也只是引用，因为指针被封装在内部函数当中。但回调函数的需求依然存在，于是 C# 引入了**委托（delegate）**的概念，所有函数指针的功能都以委托的方式来达成。委托可以被视为一个更高级的函数指针——它不仅能将地址指向另一个函数，还能传递参数、获取返回值等。系统还为委托对象自动生成了同步和异步的调用方式，开发人员使用 <code>BeginInvoke</code>、<code>EndInvoke</code> 方法就可以避开 <code>Thread</code> 类，直接实现多线程调用。</p>\n<h4 id=\"委托的常见语法\"><a href=\"#委托的常见语法\" class=\"headerlink\" title=\"委托的常见语法\"></a>委托的常见语法</h4><figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 1. 声明委托类型</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"built_in\">delegate</span> <span class=\"built_in\">int</span> <span class=\"title\">CalculateDelegate</span>(<span class=\"params\"><span class=\"built_in\">int</span> x, <span class=\"built_in\">int</span> y</span>)</span>;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 2. 创建委托实例</span></span><br><span class=\"line\">CalculateDelegate calc = <span class=\"keyword\">new</span> CalculateDelegate(Add);</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 3. 使用委托——像调用方法一样</span></span><br><span class=\"line\"><span class=\"built_in\">int</span> result = calc(<span class=\"number\">3</span>, <span class=\"number\">5</span>);        <span class=\"comment\">// 输出 8</span></span><br><span class=\"line\">result = calc.Invoke(<span class=\"number\">3</span>, <span class=\"number\">5</span>);     <span class=\"comment\">// 与上一行等价</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 4. 多播委托 —— 一个委托挂载多个方法，Invoke()会按顺序依次调用委托中的所有方法</span></span><br><span class=\"line\">calc += Subtract;               <span class=\"comment\">// 添加方法</span></span><br><span class=\"line\">calc -= Add;                    <span class=\"comment\">// 移除方法</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">static</span> <span class=\"built_in\">int</span> <span class=\"title\">Add</span>(<span class=\"params\"><span class=\"built_in\">int</span> a, <span class=\"built_in\">int</span> b</span>)</span> =&gt; a + b; <span class=\"comment\">// lambda 表达式</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">static</span> <span class=\"built_in\">int</span> <span class=\"title\">Subtract</span>(<span class=\"params\"><span class=\"built_in\">int</span> a, <span class=\"built_in\">int</span> b</span>)</span> =&gt; a - b;</span><br></pre></td></tr></table></figure>\n\n<p>不过在实际开发中，较少直接使用自定义委托，而是使用 C# 为我们准备好的 <code>Action</code> 和 <code>Func</code> 这两个泛型委托。两者的区别在于：</p>\n<table>\n<thead>\n<tr>\n<th>委托</th>\n<th>返回值</th>\n<th>示例</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>Action</code></td>\n<td>无返回值</td>\n<td><code>Action&lt;string&gt; print = Console.WriteLine;</code></td>\n</tr>\n<tr>\n<td><code>Action&lt;T1, T2, ...&gt;</code></td>\n<td>无返回值</td>\n<td><code>Action&lt;int, int&gt; log = (x, y) =&gt; Console.WriteLine(x + y);</code></td>\n</tr>\n<tr>\n<td><code>Func&lt;T, TResult&gt;</code></td>\n<td>有返回值（最后一个泛型参数是返回类型）</td>\n<td><code>Func&lt;int, int, int&gt; add = (a, b) =&gt; a + b;</code></td>\n</tr>\n</tbody></table>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// Action 示例 —— 无返回值</span></span><br><span class=\"line\">Action&lt;<span class=\"built_in\">string</span>&gt; log = msg =&gt; Console.WriteLine(<span class=\"string\">$&quot;[LOG] <span class=\"subst\">&#123;msg&#125;</span>&quot;</span>);</span><br><span class=\"line\">log(<span class=\"string\">&quot;Hello&quot;</span>);   <span class=\"comment\">// 输出：[LOG] Hello</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// Func 示例 —— 有返回值</span></span><br><span class=\"line\">Func&lt;<span class=\"built_in\">int</span>, <span class=\"built_in\">int</span>, <span class=\"built_in\">int</span>&gt; multiply = (a, b) =&gt; a * b;</span><br><span class=\"line\"><span class=\"built_in\">int</span> product = multiply(<span class=\"number\">3</span>, <span class=\"number\">4</span>);   <span class=\"comment\">// 12</span></span><br></pre></td></tr></table></figure>\n\n<h4 id=\"委托的底层实现\"><a href=\"#委托的底层实现\" class=\"headerlink\" title=\"委托的底层实现\"></a>委托的底层实现</h4><p>我们在创建委托时，实际上就是创建了一个委托类实例。这个委托类继承自 <code>System.MulticastDelegate</code>，类实例中包含 <code>BeginInvoke</code>、<code>EndInvoke</code>、<code>Invoke</code> 三个方法，分别对应异步开始调用、结束异步调用和直接调用。</p>\n<p><code>Delegate</code> 类中有一个变量用于存储函数地址。当使用 <code>=</code> 操作时，把函数地址赋值给该变量存起来。不过这个存储函数地址的变量实际上是一个<strong>调用列表（invocation list）</strong>，其内部结构为一个链表：委托实例持有 <code>_prev</code> 字段指向链表中的前一个委托节点。当链表中只有一个方法时，<code>_prev</code> 为 <code>null</code>；当再 <code>+=</code> 一个方法时，实际是创建了一个新的 <code>MulticastDelegate</code> 实例，其 <code>_prev</code> 指向原来的实例，以此串成链表。注意，每 <code>+=</code> 一次都会生成一个新实例并串到链表头部，所以 <code>_prev</code> 链的方向是<strong>新 → 旧</strong>。当委托被调用（<code>Invoke</code>）时，CLR 会先沿着 <code>_prev</code> 链走到最末端（即最早添加的节点），然后<strong>从旧到新</strong>依次调用每个节点的方法，保证回调顺序与注册顺序一致。</p>\n<p><code>MulticastDelegate</code> 还重写了 <code>+=</code> 和 <code>-=</code> 这两个操作符，其内部实现对应的就是 <code>Delegate.Combine</code> 和 <code>Delegate.Remove</code> 静态方法：对委托执行 <code>+=</code> 相当于把新的函数地址加入调用列表；执行 <code>-=</code> 则是从调用列表中移除该函数。</p>\n<p>看到这里我们就彻底明白了：<code>delegate</code> 关键字本质上是一个语法糖，背后由 C# 编译器将其重写为一个继承自 <code>System.MulticastDelegate</code> 的类。</p>\n<h3 id=\"事件\"><a href=\"#事件\" class=\"headerlink\" title=\"事件\"></a>事件</h3><h4 id=\"事件的常见语法\"><a href=\"#事件的常见语法\" class=\"headerlink\" title=\"事件的常见语法\"></a>事件的常见语法</h4><figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 1. 声明事件（基于已有的委托类型）</span></span><br><span class=\"line\"><span class=\"keyword\">public</span> <span class=\"keyword\">event</span> Action&lt;<span class=\"built_in\">string</span>&gt; OnMessageReceived;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 2. 注册事件</span></span><br><span class=\"line\">OnMessageReceived += HandleMessage;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 3. 注销事件</span></span><br><span class=\"line\">OnMessageReceived -= HandleMessage;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 4. 触发事件（只能在声明事件的类内部调用）</span></span><br><span class=\"line\">OnMessageReceived?.Invoke(<span class=\"string\">&quot;Hello World&quot;</span>);</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 事件处理方法</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">HandleMessage</span>(<span class=\"params\"><span class=\"built_in\">string</span> msg</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">$&quot;收到消息：<span class=\"subst\">&#123;msg&#125;</span>&quot;</span>);</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<h4 id=\"事件的底层实现\"><a href=\"#事件的底层实现\" class=\"headerlink\" title=\"事件的底层实现\"></a>事件的底层实现</h4><p><code>event</code> 很简单，它在委托（delegate）的基础上又做了一次封装。这次封装的意义在于：<strong>限制用户直接操作委托实例中变量的权限</strong>。</p>\n<p>如果不使用 <code>event</code>，委托字段就像一个普通的公共变量，外部代码可以直接对它执行 <code>=</code> 赋值，从而清空之前所有订阅的方法。而 <code>event</code> 关键字本质上会生成类似属性（property）的 <code>add</code> 和 <code>remove</code> 访问器，将底层的委托字段设为私有，外部只能通过 <code>+=</code>&#x2F;<code>-=</code> 来增减方法，无法直接赋值覆盖。其原理大致如下：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// event 背后的等价封装（简化示意）</span></span><br><span class=\"line\"><span class=\"keyword\">private</span> Action&lt;<span class=\"built_in\">string</span>&gt; _onMessage;   <span class=\"comment\">// 底层委托字段是私有的</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">public</span> <span class=\"keyword\">event</span> Action&lt;<span class=\"built_in\">string</span>&gt; OnMessage</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">add</span>    &#123; _onMessage += <span class=\"keyword\">value</span>; &#125;   <span class=\"comment\">// 只能通过 += 注册</span></span><br><span class=\"line\">    <span class=\"keyword\">remove</span> &#123; _onMessage -= <span class=\"keyword\">value</span>; &#125;   <span class=\"comment\">// 只能通过 -= 注销</span></span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>封装之后，用户不再能够用赋值操作（即 <code>=</code> 操作符）来直接改变委托变量，只能通过注册或注销委托的方式来增减委托函数的数量。也就是说，被 <code>event</code> 声明的委托不再对外暴露 <code>=</code> 操作，但仍然保留了 <code>+=</code> 和 <code>-=</code> 操作符可供使用。</p>\n<p>为什么要做这样的限制呢？在平时的编程中，项目庞大、经手人员众多，我们常常无法得知其他人在编写的代码意图是什么。如果委托字段直接暴露在外，随时可能被 <code>=</code> 赋值而<strong>清空前面累积起来的委托链表</strong>，操作的权限范围太大，导致 bug 难以排查。而声明为 <code>event</code> 后，编译器在内部重新封装了委托，让暴露在外的委托不再有被随时清空和重置的风险——只能一个个注册、一个个注销，这样就保证了”谁注册谁负责销毁”的秩序，更好地维护了委托链的完整。</p>\n<hr>\n<h2 id=\"三、装箱和拆箱\"><a href=\"#三、装箱和拆箱\" class=\"headerlink\" title=\"三、装箱和拆箱\"></a>三、装箱和拆箱</h2><p>什么是装箱和拆箱？其实很简单：<strong>把值类型实例转换为引用类型实例，就是装箱；反过来，把引用类型实例转换为值类型实例，就是拆箱。</strong></p>\n<p>举个例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">int</span> a = <span class=\"number\">5</span>;</span><br><span class=\"line\"><span class=\"built_in\">object</span> obj = a;     <span class=\"comment\">// 装箱</span></span><br></pre></td></tr></table></figure>\n\n<p><code>a</code> 是值类型，是直接持有数据的变量；<code>obj</code> 是引用类型，指针与数据是分离的。把 <code>a</code> 赋值给 <code>obj</code>，实际上就是 <code>obj</code> 在堆上开辟了一块新的数据空间，拷贝了 <code>a</code> 的数据，并为自己创建了一个指针指向这块数据空间。接着上面的代码继续：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">int</span> b = (<span class=\"built_in\">int</span>)obj;   <span class=\"comment\">// 拆箱</span></span><br></pre></td></tr></table></figure>\n\n<p>这就是拆箱——把 <code>obj</code> 指向的内存空间复制一份交给 <code>b</code>。因为 <code>b</code> 是值类型，它不允许指向某个内存空间，只能靠复制数据来传递数据。</p>\n<h3 id=\"堆内存和栈内存\"><a href=\"#堆内存和栈内存\" class=\"headerlink\" title=\"堆内存和栈内存\"></a>堆内存和栈内存</h3><p>这里稍微阐述一下堆和栈内存，因为很多人对此有错误的认识。为什么要有栈内存和堆内存之分？用简短的语言来说明：</p>\n<blockquote>\n<p>栈是本着<strong>先进后出（LIFO）<strong>原则的存储机制，它是一段</strong>连续的内存</strong>，所以对栈数据的定位比较快速。而堆则是<strong>随机分配</strong>的空间，处理的数据比较多，无论如何至少要两次定位。堆内存的创建和删除节点的时间复杂度是 O(log n)，栈创建和删除的时间复杂度则是 O(1)，栈速度更快。</p>\n<p>那么既然栈速度这么快，全部用栈不就好了？这又涉及生命周期的问题。栈中的生命周期是必须确定的，销毁时必须按次序从最后分配的块开始销毁，创建后什么时候销毁必须是一个定值，所以在分配和销毁时不灵活，基本只用于函数调用和递归调用这类生命周期比较确定的地方。相反，堆内存可以存放生命周期不确定的内存块，满足”需要时再删除”的需求，所以堆内存更适合全局性质的、需要灵活分配和销毁的内存块。</p>\n</blockquote>\n<p>很多人把”值类型放在栈中、引用类型放在堆中”当作绝对规律，这是错误的。栈内存主要为<strong>确定性生命周期</strong>的内存服务，堆内存则更多服务于<strong>无序的、随时可能释放</strong>的内存。因此值类型既可以在堆内也可以在栈内，引用类型的指针部分同样可以在栈内或堆内——但引用类型<strong>指向的内存块</strong>一定在堆内。</p>\n<h3 id=\"什么时候会用到装箱\"><a href=\"#什么时候会用到装箱\" class=\"headerlink\" title=\"什么时候会用到装箱\"></a>什么时候会用到装箱</h3><p>大部分时候只有当程序、逻辑或接口需要更加通用时，才会需要装箱。比如：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 情况一：调用含 object 类型参数的方法</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">PrintObject</span>(<span class=\"params\"><span class=\"built_in\">object</span> obj</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    Console.WriteLine(obj);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"built_in\">int</span> <span class=\"keyword\">value</span> = <span class=\"number\">42</span>;</span><br><span class=\"line\">PrintObject(<span class=\"keyword\">value</span>);     <span class=\"comment\">// int → object，发生装箱</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 情况二：非泛型容器，元素类型定义为 object</span></span><br><span class=\"line\">ArrayList list = <span class=\"keyword\">new</span> ArrayList();</span><br><span class=\"line\">list.Add(<span class=\"number\">100</span>);          <span class=\"comment\">// int → object，发生装箱</span></span><br><span class=\"line\">list.Add(<span class=\"string\">&quot;hello&quot;</span>);      <span class=\"comment\">// string 本身就是引用类型，不装箱</span></span><br></pre></td></tr></table></figure>\n\n<p>当你需要将一个值类型（如 <code>int</code>）传入参数为 <code>object</code> 的方法时，就需要装箱。又比如一个非泛型的容器（如 <code>ArrayList</code>）为了保证通用，将元素类型定义为 <code>object</code>，值类型数据放入时就会触发装箱。</p>\n<h3 id=\"装箱和拆箱的内部操作\"><a href=\"#装箱和拆箱的内部操作\" class=\"headerlink\" title=\"装箱和拆箱的内部操作\"></a>装箱和拆箱的内部操作</h3><p><strong>装箱</strong>：根据相应的值类型在堆中分配一个内存块，再将数据拷贝给它。具体按以下三步进行：</p>\n<ol>\n<li><strong>第一步</strong>：在堆内存中新分配一个内存块（大小为值类型实例大小加上一个方法表指针和一个 <code>SyncBlockIndex</code>）。</li>\n<li><strong>第二步</strong>：将值类型的实例字段拷贝到新分配的内存块中。</li>\n<li><strong>第三步</strong>：返回堆中新分配对象的地址——这个地址就是一个指向对象的引用了。</li>\n</ol>\n<p><strong>拆箱</strong>则更为简单：先检查对象实例，确保它是给定值类型的一个装箱值，再将该值从实例复制到值类型变量的内存块中。</p>\n<h3 id=\"装箱、拆箱对执行效率的影响及优化\"><a href=\"#装箱、拆箱对执行效率的影响及优化\" class=\"headerlink\" title=\"装箱、拆箱对执行效率的影响及优化\"></a>装箱、拆箱对执行效率的影响及优化</h3><p>由于装箱和拆箱时生成的是全新的对象，不断地分配和销毁内存不仅大量消耗 CPU，也会增加内存碎片，从而降低性能。那该如何优化呢？</p>\n<p>最关键的就是<strong>减少装箱和拆箱操作</strong>——在编程规范中要牢记：装箱和拆箱是消耗较大的操作，写代码时需要特别留意。</p>\n<p>整数、浮点数、布尔等数值型变量变化手段不多，主要靠加强编码规范来减少装拆箱。而 <code>struct</code> 有点不一样——它既是值类型，又可以像类一样实现接口，用途更多样、转换途径也更多，稍不留神就会埋下性能隐患。所以这里重点讲讲 <code>struct</code> 的优化方法。</p>\n<h4 id=\"通过重载函数来避免装箱\"><a href=\"#通过重载函数来避免装箱\" class=\"headerlink\" title=\"通过重载函数来避免装箱\"></a>通过重载函数来避免装箱</h4><p>比如常用的 <code>ToString()</code>、<code>GetHashCode()</code>、<code>Equals()</code> 方法：如果 <code>struct</code> 没有重写这些方法，调用它们时会先装箱再调用，导致内存分配的性能损耗。</p>\n<p>为什么 struct 调用这些方法需要先装箱？因为这些方法的默认实现定义在 <code>System.Object</code> 或 <code>System.ValueType</code> 基类中，都是虚方法（<code>virtual</code>）。当 struct 没有重写它们时，虚方法调用需要查方法表（vtable），而方法表存储在引用类型对象中——值类型实例本身不携带方法表指针。因此必须先将 struct 装箱为一个引用类型对象，才能完成虚方法的派发。例如：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">struct</span> MyStruct</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> Value;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">MyStruct s = <span class=\"keyword\">new</span> MyStruct &#123; Value = <span class=\"number\">10</span> &#125;;</span><br><span class=\"line\">s.ToString();     <span class=\"comment\">// MyStruct 没有重写 ToString → 先装箱再调用 → 产生 GC 垃圾</span></span><br></pre></td></tr></table></figure>\n\n<p>而要避免这一点很简单——在 struct 中手动重写这些方法即可：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">struct</span> MyStruct</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> Value;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">override</span> <span class=\"built_in\">string</span> <span class=\"title\">ToString</span>()</span> =&gt; Value.ToString();  <span class=\"comment\">// 重载后不再装箱</span></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">override</span> <span class=\"built_in\">int</span> <span class=\"title\">GetHashCode</span>()</span> =&gt; Value.GetHashCode();</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">override</span> <span class=\"built_in\">bool</span> <span class=\"title\">Equals</span>(<span class=\"params\"><span class=\"built_in\">object</span> obj</span>)</span> =&gt; obj <span class=\"keyword\">is</span> MyStruct s &amp;&amp; Value == s.Value;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>所以，对于那些需要调用的引用类型方法，struct 必须逐一<strong>手动重载</strong>。</p>\n<h4 id=\"通过泛型来避免装箱\"><a href=\"#通过泛型来避免装箱\" class=\"headerlink\" title=\"通过泛型来避免装箱\"></a>通过泛型来避免装箱</h4><p>别忘了 struct 也是可以实现接口的。在不同的、相似的、具有父子关系的 struct 之间，可以用泛型来传递参数，这样就不用装箱后再传递了。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">interface</span> <span class=\"title\">IPrintable</span> &#123; <span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">Print</span>()</span>; &#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">struct</span> Document : IPrintable</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">string</span> Name;</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">void</span> <span class=\"title\">Print</span>()</span> =&gt; Console.WriteLine(<span class=\"string\">$&quot;打印：<span class=\"subst\">&#123;Name&#125;</span>&quot;</span>);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 不使用泛型 —— 传参时装箱</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">ProcessByInterface</span>(<span class=\"params\">IPrintable printable</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    printable.Print();</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 使用泛型 —— 不会装箱</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">ProcessByGeneric</span>&lt;<span class=\"title\">T</span>&gt;(<span class=\"params\">T printable</span>) <span class=\"keyword\">where</span> T : IPrintable</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    printable.Print();</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">Document doc = <span class=\"keyword\">new</span> Document &#123; Name = <span class=\"string\">&quot;报告&quot;</span> &#125;;</span><br><span class=\"line\">ProcessByInterface(doc);   <span class=\"comment\">// doc 转为 IPrintable → 装箱</span></span><br><span class=\"line\">ProcessByGeneric(doc);     <span class=\"comment\">// 泛型直接约束为 IPrintable，不装箱</span></span><br></pre></td></tr></table></figure>\n\n<p>为什么传给 <code>IPrintable</code> 类型的参数就需要装箱？原因是 struct 实例本质上只是栈上的一段<strong>纯数据</strong>，没有对象头（Object Header），也没有方法表指针（MethodTable Pointer）。而接口方法调用属于<strong>虚分派（virtual dispatch）</strong>，CLR 必须通过方法表来查找实际要调用的方法——栈上的 struct 不携带这些信息，所以必须先装箱，在堆上生成一个带有完整对象头和接口映射表（Interface Map）的包装对象，才能完成接口分派。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 通过具体类型直接调用 —— 不装箱</span></span><br><span class=\"line\">Document doc = <span class=\"keyword\">new</span> Document();</span><br><span class=\"line\">doc.Print();     <span class=\"comment\">// 编译器知道 doc 就是 Document，直接静态调用</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 通过接口引用调用 —— 需要装箱</span></span><br><span class=\"line\">IPrintable ip = doc;   <span class=\"comment\">// doc 装箱：在堆上创建带方法表指针的副本</span></span><br><span class=\"line\">ip.Print();            <span class=\"comment\">// 通过接口分派，走方法表查找</span></span><br></pre></td></tr></table></figure>\n\n<p>而泛型则绕过了这个问题。由于 JIT 编译时会对每个值类型生成<strong>专用版本</strong>（代码特化），当 <code>T</code> 是 <code>Document</code> 时，JIT 生成的方法内部等价于直接接收 <code>Document</code> 类型的参数。调用 <code>Print()</code> 时走的是<strong>具体类型的静态分派</strong>，而不是接口虚分派——<code>where T : IPrintable</code> 约束仅用于编译期检查，确保 <code>T</code> 实现了 <code>Print()</code>，运行时完全不经过接口查表，因此全程不需要装箱。</p>\n<h4 id=\"通过继承统一的接口提前装箱，避免多次重复拆装箱\"><a href=\"#通过继承统一的接口提前装箱，避免多次重复拆装箱\" class=\"headerlink\" title=\"通过继承统一的接口提前装箱，避免多次重复拆装箱\"></a>通过继承统一的接口提前装箱，避免多次重复拆装箱</h4><p>很多时候拆装箱不可避免，那么我们就让多种 struct 继承某个统一的接口。这样不同 struct 就可以用相同的接口访问。将 struct 传递到其他方法时，相当于<strong>提前进行了一次装箱操作</strong>——在方法中得到的是引用类型的值，并且具备所需的接口，避免了在方法内部重复多次的拆装箱。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br><span class=\"line\">26</span><br><span class=\"line\">27</span><br><span class=\"line\">28</span><br><span class=\"line\">29</span><br><span class=\"line\">30</span><br><span class=\"line\">31</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">interface</span> <span class=\"title\">IShape</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"function\"><span class=\"built_in\">double</span> <span class=\"title\">Area</span>()</span>;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">struct</span> Square : IShape</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">double</span> Side;</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"built_in\">double</span> <span class=\"title\">Area</span>()</span> =&gt; Side * Side;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">struct</span> Circle : IShape</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">double</span> Radius;</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"built_in\">double</span> <span class=\"title\">Area</span>()</span> =&gt; Math.PI * Radius * Radius;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// Test 方法接收 IShape 引用类型，struct 传入时装箱一次</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">Test</span>(<span class=\"params\">IShape shape</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 方法内部可以多次调用 shape.Area()、shape 的方法等</span></span><br><span class=\"line\">    <span class=\"comment\">// 不会再发生装箱，因为 shape 已经是引用类型</span></span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">$&quot;面积：<span class=\"subst\">&#123;shape.Area()&#125;</span>&quot;</span>);</span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">$&quot;类型：<span class=\"subst\">&#123;shape.GetType().Name&#125;</span>&quot;</span>);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">Square square = <span class=\"keyword\">new</span> Square &#123; Side = <span class=\"number\">5</span> &#125;;</span><br><span class=\"line\">Circle circle = <span class=\"keyword\">new</span> Circle &#123; Radius = <span class=\"number\">3</span> &#125;;</span><br><span class=\"line\"></span><br><span class=\"line\">Test(square);   <span class=\"comment\">// Square → IShape，装箱一次，内部不再重复装箱</span></span><br><span class=\"line\">Test(circle);   <span class=\"comment\">// Circle → IShape，装箱一次，内部不再重复装箱</span></span><br></pre></td></tr></table></figure>\n\n<p>对比一下：如果没有统一的接口，Square 和 Circle 又都需要传入同一个方法处理，就只能退回到用 <code>object</code> 来接收：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 没有统一接口 → 用 object 凑合</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">ProcessWithoutInterface</span>(<span class=\"params\"><span class=\"built_in\">object</span> obj</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 每次访问具体字段/方法，都要先拆箱</span></span><br><span class=\"line\">    <span class=\"keyword\">if</span> (obj <span class=\"keyword\">is</span> Square)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        Square s = (Square)obj;         <span class=\"comment\">// 拆箱！</span></span><br><span class=\"line\">        Console.WriteLine(<span class=\"string\">$&quot;正方形面积：<span class=\"subst\">&#123;s.Side * s.Side&#125;</span>&quot;</span>);</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">    <span class=\"keyword\">else</span> <span class=\"keyword\">if</span> (obj <span class=\"keyword\">is</span> Circle)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        Circle c = (Circle)obj;         <span class=\"comment\">// 拆箱！</span></span><br><span class=\"line\">        Console.WriteLine(<span class=\"string\">$&quot;圆形面积：<span class=\"subst\">&#123;Math.PI * c.Radius * c.Radius&#125;</span>&quot;</span>);</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">    <span class=\"comment\">// 如果方法里还要再做其他操作（比如打印类型名、序列化等），</span></span><br><span class=\"line\">    <span class=\"comment\">// 每次都从 object 里拆出来，拆箱次数 = if 分支数 × 操作次数</span></span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">Square square = <span class=\"keyword\">new</span> Square &#123; Side = <span class=\"number\">5</span> &#125;;</span><br><span class=\"line\">Circle circle = <span class=\"keyword\">new</span> Circle &#123; Radius = <span class=\"number\">3</span> &#125;;</span><br><span class=\"line\"></span><br><span class=\"line\">ProcessWithoutInterface(square);  <span class=\"comment\">// square → object（装箱）</span></span><br><span class=\"line\">ProcessWithoutInterface(circle);  <span class=\"comment\">// circle → object（装箱）</span></span><br><span class=\"line\"><span class=\"comment\">// 更糟的是：方法内部每次 (Square)obj / (Circle)obj 都在拆箱</span></span><br><span class=\"line\"><span class=\"comment\">// 如果用 is 做类型判断再强转，等于拆了两次（is 一次，强转又一次）</span></span><br></pre></td></tr></table></figure>\n\n<p>可以看到，没有统一接口时：传入方法时<strong>装箱一次</strong>，但方法内部每次要访问具体数据就得<strong>拆箱一次</strong>——如果有 N 种 struct 类型、每种有 M 个操作，最坏情况下拆箱次数 &#x3D; N × M。而使用统一接口后，只需在入口处装箱一次，整个方法体内都在跟已装箱的引用类型打交道，零额外开销。</p>\n<p>这种做法的优势在于：装箱发生在<strong>方法入口处</strong>，只做一次。方法内部操作的都是已装箱好的引用类型对象，无需再次拆装箱，从而将性能影响控制在最小范围。</p>\n<div class=\"tip\">\n最后依然要提醒大家：struct 值类型数据结构如果没有真正理解它的原理，用起来可能会引发很多麻烦。切记不要盲目认为使用结构体一定会让性能提升——在没有完全彻底理解之前贸然大量使用 struct，可能会对你的程序性能带来重创。\n</div>\n\n<hr>\n<h2 id=\"参考资料\"><a href=\"#参考资料\" class=\"headerlink\" title=\"参考资料\"></a>参考资料</h2><ul>\n<li><a href=\"http://www.luzexi.com/2019/01/26/Unity3D%E9%AB%98%E7%BA%A7%E7%BC%96%E7%A8%8B%E4%B9%8B%E8%BF%9B%E9%98%B6%E4%B8%BB%E7%A8%8B-CSharp%E8%A6%81%E7%82%B9%E6%8A%80%E6%9C%AF4\">《Unity3D高级编程之进阶主程》第一章，C#要点技术(四) 委托、事件、装箱、拆箱 - 技术人生 - 编程技术 - JESSE人生</a></li>\n</ul>\n","excerpt":"","more":"<hr>\n<h2 id=\"写在前面\"><a href=\"#写在前面\" class=\"headerlink\" title=\"写在前面\"></a>写在前面</h2><p>这个分类下的文章是我对 《Unity3D高级编程之进阶主程》一书的学习笔记。</p>\n<p>这个博客对应的章节是 </p>\n<ul>\n<li>第一章，C#要点技术(四) 委托、事件、装箱、拆箱</li>\n</ul>\n<p>部分内容为ai生成，如有错误恳请指出。</p>\n<hr>\n<h2 id=\"一、值类型和引用类型\"><a href=\"#一、值类型和引用类型\" class=\"headerlink\" title=\"一、值类型和引用类型\"></a>一、值类型和引用类型</h2><p>值类型包括所有整数、浮点数、<code>bool</code>，以及用 <code>struct</code> 声明的结构。这里要特别留意 <code>struct</code>——它是经常容易犯错误的地方。很多人会把 <code>struct</code> 当作类来使用，这是完全错误的。因为它是值类型，在赋值操作时通过<strong>直接拷贝数据</strong>完成，所以常会出现这样的困惑：<code>a</code>、<code>b</code> 同是结构体实例，<code>a</code> 赋值给 <code>b</code>，当 <code>b</code> 更改了数据后却发现 <code>a</code> 的数据没有同步。事实上它们根本就是两块独立的数据空间——当 <code>a</code> 赋值给 <code>b</code> 时并不是引用拷贝，而是<strong>整个数据空间的拷贝</strong>，相当于有了两个不同的西瓜，只是长得比较像而已。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// struct 是值类型，赋值 = 完整拷贝</span></span><br><span class=\"line\"><span class=\"keyword\">struct</span> Point</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> X;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> Y;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">Point a = <span class=\"keyword\">new</span> Point &#123; X = <span class=\"number\">1</span>, Y = <span class=\"number\">2</span> &#125;;</span><br><span class=\"line\">Point b = a;          <span class=\"comment\">// 把 a 的所有数据完整拷贝给 b</span></span><br><span class=\"line\">b.X = <span class=\"number\">10</span>;             <span class=\"comment\">// 修改 b 的 X</span></span><br><span class=\"line\"></span><br><span class=\"line\">Console.WriteLine(a.X);  <span class=\"comment\">// 输出 1 —— a 不受影响</span></span><br><span class=\"line\">Console.WriteLine(b.X);  <span class=\"comment\">// 输出 10</span></span><br></pre></td></tr></table></figure>\n\n<p>引用类型包括类、接口、委托（委托也是类）、数组，以及内置的 <code>object</code> 与 <code>string</code>。在 C# 里万物皆是类——虽然 <code>int</code> 等值类型本质上也是类，只不过它们是特殊的、值类型的类。”类都是引用类型”虽然不算完全精确，但也不妨碍它作为一个好记的口诀。</p>\n<p>值类型的变量会<strong>直接存储数据</strong>，而引用类型的变量持有的是<strong>数据的引用</strong>，其真实数据存储在托管堆中。当声明一个引用类型变量时，只在栈（或堆）中分配一小片内存用于容纳一个地址，此时并没有为其分配堆上的内存空间，因此它为 <code>null</code>；直到使用 <code>new</code> 创建实例时，才在堆上分配空间，并把堆上空间的地址保存给这个引用变量，这时引用变量才真正指向了有效的内存。</p>\n<p>这也解释了为什么 <code>struct</code> 和类的赋值表现完全不同。<code>struct</code> 赋值时，是把整块数据拷贝过去——被赋值的变量已经在一块全新的内存里存储了数据，与原变量再无瓜葛。而类的赋值，只是把数据的<strong>引用</strong>拷贝过去——被赋值的变量和赋给它的变量里存着<strong>同一个地址</strong>，指向堆上的同一块数据空间。所以 <code>b</code> 更改了数据之后，再用 <code>a</code> 访问，看到的也是被改过的那块数据。</p>\n<hr>\n<h2 id=\"二、委托和事件\"><a href=\"#二、委托和事件\" class=\"headerlink\" title=\"二、委托和事件\"></a>二、委托和事件</h2><h3 id=\"委托\"><a href=\"#委托\" class=\"headerlink\" title=\"委托\"></a>委托</h3><p>在 C# 中万物皆是类，大部分时间里都看不到指针的身影，最多也只是引用，因为指针被封装在内部函数当中。但回调函数的需求依然存在，于是 C# 引入了**委托（delegate）**的概念，所有函数指针的功能都以委托的方式来达成。委托可以被视为一个更高级的函数指针——它不仅能将地址指向另一个函数，还能传递参数、获取返回值等。系统还为委托对象自动生成了同步和异步的调用方式，开发人员使用 <code>BeginInvoke</code>、<code>EndInvoke</code> 方法就可以避开 <code>Thread</code> 类，直接实现多线程调用。</p>\n<h4 id=\"委托的常见语法\"><a href=\"#委托的常见语法\" class=\"headerlink\" title=\"委托的常见语法\"></a>委托的常见语法</h4><figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 1. 声明委托类型</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">public</span> <span class=\"built_in\">delegate</span> <span class=\"built_in\">int</span> <span class=\"title\">CalculateDelegate</span>(<span class=\"params\"><span class=\"built_in\">int</span> x, <span class=\"built_in\">int</span> y</span>)</span>;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 2. 创建委托实例</span></span><br><span class=\"line\">CalculateDelegate calc = <span class=\"keyword\">new</span> CalculateDelegate(Add);</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 3. 使用委托——像调用方法一样</span></span><br><span class=\"line\"><span class=\"built_in\">int</span> result = calc(<span class=\"number\">3</span>, <span class=\"number\">5</span>);        <span class=\"comment\">// 输出 8</span></span><br><span class=\"line\">result = calc.Invoke(<span class=\"number\">3</span>, <span class=\"number\">5</span>);     <span class=\"comment\">// 与上一行等价</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 4. 多播委托 —— 一个委托挂载多个方法，Invoke()会按顺序依次调用委托中的所有方法</span></span><br><span class=\"line\">calc += Subtract;               <span class=\"comment\">// 添加方法</span></span><br><span class=\"line\">calc -= Add;                    <span class=\"comment\">// 移除方法</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">static</span> <span class=\"built_in\">int</span> <span class=\"title\">Add</span>(<span class=\"params\"><span class=\"built_in\">int</span> a, <span class=\"built_in\">int</span> b</span>)</span> =&gt; a + b; <span class=\"comment\">// lambda 表达式</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">static</span> <span class=\"built_in\">int</span> <span class=\"title\">Subtract</span>(<span class=\"params\"><span class=\"built_in\">int</span> a, <span class=\"built_in\">int</span> b</span>)</span> =&gt; a - b;</span><br></pre></td></tr></table></figure>\n\n<p>不过在实际开发中，较少直接使用自定义委托，而是使用 C# 为我们准备好的 <code>Action</code> 和 <code>Func</code> 这两个泛型委托。两者的区别在于：</p>\n<table>\n<thead>\n<tr>\n<th>委托</th>\n<th>返回值</th>\n<th>示例</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>Action</code></td>\n<td>无返回值</td>\n<td><code>Action&lt;string&gt; print = Console.WriteLine;</code></td>\n</tr>\n<tr>\n<td><code>Action&lt;T1, T2, ...&gt;</code></td>\n<td>无返回值</td>\n<td><code>Action&lt;int, int&gt; log = (x, y) =&gt; Console.WriteLine(x + y);</code></td>\n</tr>\n<tr>\n<td><code>Func&lt;T, TResult&gt;</code></td>\n<td>有返回值（最后一个泛型参数是返回类型）</td>\n<td><code>Func&lt;int, int, int&gt; add = (a, b) =&gt; a + b;</code></td>\n</tr>\n</tbody></table>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// Action 示例 —— 无返回值</span></span><br><span class=\"line\">Action&lt;<span class=\"built_in\">string</span>&gt; log = msg =&gt; Console.WriteLine(<span class=\"string\">$&quot;[LOG] <span class=\"subst\">&#123;msg&#125;</span>&quot;</span>);</span><br><span class=\"line\">log(<span class=\"string\">&quot;Hello&quot;</span>);   <span class=\"comment\">// 输出：[LOG] Hello</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// Func 示例 —— 有返回值</span></span><br><span class=\"line\">Func&lt;<span class=\"built_in\">int</span>, <span class=\"built_in\">int</span>, <span class=\"built_in\">int</span>&gt; multiply = (a, b) =&gt; a * b;</span><br><span class=\"line\"><span class=\"built_in\">int</span> product = multiply(<span class=\"number\">3</span>, <span class=\"number\">4</span>);   <span class=\"comment\">// 12</span></span><br></pre></td></tr></table></figure>\n\n<h4 id=\"委托的底层实现\"><a href=\"#委托的底层实现\" class=\"headerlink\" title=\"委托的底层实现\"></a>委托的底层实现</h4><p>我们在创建委托时，实际上就是创建了一个委托类实例。这个委托类继承自 <code>System.MulticastDelegate</code>，类实例中包含 <code>BeginInvoke</code>、<code>EndInvoke</code>、<code>Invoke</code> 三个方法，分别对应异步开始调用、结束异步调用和直接调用。</p>\n<p><code>Delegate</code> 类中有一个变量用于存储函数地址。当使用 <code>=</code> 操作时，把函数地址赋值给该变量存起来。不过这个存储函数地址的变量实际上是一个<strong>调用列表（invocation list）</strong>，其内部结构为一个链表：委托实例持有 <code>_prev</code> 字段指向链表中的前一个委托节点。当链表中只有一个方法时，<code>_prev</code> 为 <code>null</code>；当再 <code>+=</code> 一个方法时，实际是创建了一个新的 <code>MulticastDelegate</code> 实例，其 <code>_prev</code> 指向原来的实例，以此串成链表。注意，每 <code>+=</code> 一次都会生成一个新实例并串到链表头部，所以 <code>_prev</code> 链的方向是<strong>新 → 旧</strong>。当委托被调用（<code>Invoke</code>）时，CLR 会先沿着 <code>_prev</code> 链走到最末端（即最早添加的节点），然后<strong>从旧到新</strong>依次调用每个节点的方法，保证回调顺序与注册顺序一致。</p>\n<p><code>MulticastDelegate</code> 还重写了 <code>+=</code> 和 <code>-=</code> 这两个操作符，其内部实现对应的就是 <code>Delegate.Combine</code> 和 <code>Delegate.Remove</code> 静态方法：对委托执行 <code>+=</code> 相当于把新的函数地址加入调用列表；执行 <code>-=</code> 则是从调用列表中移除该函数。</p>\n<p>看到这里我们就彻底明白了：<code>delegate</code> 关键字本质上是一个语法糖，背后由 C# 编译器将其重写为一个继承自 <code>System.MulticastDelegate</code> 的类。</p>\n<h3 id=\"事件\"><a href=\"#事件\" class=\"headerlink\" title=\"事件\"></a>事件</h3><h4 id=\"事件的常见语法\"><a href=\"#事件的常见语法\" class=\"headerlink\" title=\"事件的常见语法\"></a>事件的常见语法</h4><figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 1. 声明事件（基于已有的委托类型）</span></span><br><span class=\"line\"><span class=\"keyword\">public</span> <span class=\"keyword\">event</span> Action&lt;<span class=\"built_in\">string</span>&gt; OnMessageReceived;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 2. 注册事件</span></span><br><span class=\"line\">OnMessageReceived += HandleMessage;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 3. 注销事件</span></span><br><span class=\"line\">OnMessageReceived -= HandleMessage;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 4. 触发事件（只能在声明事件的类内部调用）</span></span><br><span class=\"line\">OnMessageReceived?.Invoke(<span class=\"string\">&quot;Hello World&quot;</span>);</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 事件处理方法</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">HandleMessage</span>(<span class=\"params\"><span class=\"built_in\">string</span> msg</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">$&quot;收到消息：<span class=\"subst\">&#123;msg&#125;</span>&quot;</span>);</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<h4 id=\"事件的底层实现\"><a href=\"#事件的底层实现\" class=\"headerlink\" title=\"事件的底层实现\"></a>事件的底层实现</h4><p><code>event</code> 很简单，它在委托（delegate）的基础上又做了一次封装。这次封装的意义在于：<strong>限制用户直接操作委托实例中变量的权限</strong>。</p>\n<p>如果不使用 <code>event</code>，委托字段就像一个普通的公共变量，外部代码可以直接对它执行 <code>=</code> 赋值，从而清空之前所有订阅的方法。而 <code>event</code> 关键字本质上会生成类似属性（property）的 <code>add</code> 和 <code>remove</code> 访问器，将底层的委托字段设为私有，外部只能通过 <code>+=</code>&#x2F;<code>-=</code> 来增减方法，无法直接赋值覆盖。其原理大致如下：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// event 背后的等价封装（简化示意）</span></span><br><span class=\"line\"><span class=\"keyword\">private</span> Action&lt;<span class=\"built_in\">string</span>&gt; _onMessage;   <span class=\"comment\">// 底层委托字段是私有的</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">public</span> <span class=\"keyword\">event</span> Action&lt;<span class=\"built_in\">string</span>&gt; OnMessage</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">add</span>    &#123; _onMessage += <span class=\"keyword\">value</span>; &#125;   <span class=\"comment\">// 只能通过 += 注册</span></span><br><span class=\"line\">    <span class=\"keyword\">remove</span> &#123; _onMessage -= <span class=\"keyword\">value</span>; &#125;   <span class=\"comment\">// 只能通过 -= 注销</span></span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>封装之后，用户不再能够用赋值操作（即 <code>=</code> 操作符）来直接改变委托变量，只能通过注册或注销委托的方式来增减委托函数的数量。也就是说，被 <code>event</code> 声明的委托不再对外暴露 <code>=</code> 操作，但仍然保留了 <code>+=</code> 和 <code>-=</code> 操作符可供使用。</p>\n<p>为什么要做这样的限制呢？在平时的编程中，项目庞大、经手人员众多，我们常常无法得知其他人在编写的代码意图是什么。如果委托字段直接暴露在外，随时可能被 <code>=</code> 赋值而<strong>清空前面累积起来的委托链表</strong>，操作的权限范围太大，导致 bug 难以排查。而声明为 <code>event</code> 后，编译器在内部重新封装了委托，让暴露在外的委托不再有被随时清空和重置的风险——只能一个个注册、一个个注销，这样就保证了”谁注册谁负责销毁”的秩序，更好地维护了委托链的完整。</p>\n<hr>\n<h2 id=\"三、装箱和拆箱\"><a href=\"#三、装箱和拆箱\" class=\"headerlink\" title=\"三、装箱和拆箱\"></a>三、装箱和拆箱</h2><p>什么是装箱和拆箱？其实很简单：<strong>把值类型实例转换为引用类型实例，就是装箱；反过来，把引用类型实例转换为值类型实例，就是拆箱。</strong></p>\n<p>举个例子：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">int</span> a = <span class=\"number\">5</span>;</span><br><span class=\"line\"><span class=\"built_in\">object</span> obj = a;     <span class=\"comment\">// 装箱</span></span><br></pre></td></tr></table></figure>\n\n<p><code>a</code> 是值类型，是直接持有数据的变量；<code>obj</code> 是引用类型，指针与数据是分离的。把 <code>a</code> 赋值给 <code>obj</code>，实际上就是 <code>obj</code> 在堆上开辟了一块新的数据空间，拷贝了 <code>a</code> 的数据，并为自己创建了一个指针指向这块数据空间。接着上面的代码继续：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"built_in\">int</span> b = (<span class=\"built_in\">int</span>)obj;   <span class=\"comment\">// 拆箱</span></span><br></pre></td></tr></table></figure>\n\n<p>这就是拆箱——把 <code>obj</code> 指向的内存空间复制一份交给 <code>b</code>。因为 <code>b</code> 是值类型，它不允许指向某个内存空间，只能靠复制数据来传递数据。</p>\n<h3 id=\"堆内存和栈内存\"><a href=\"#堆内存和栈内存\" class=\"headerlink\" title=\"堆内存和栈内存\"></a>堆内存和栈内存</h3><p>这里稍微阐述一下堆和栈内存，因为很多人对此有错误的认识。为什么要有栈内存和堆内存之分？用简短的语言来说明：</p>\n<blockquote>\n<p>栈是本着<strong>先进后出（LIFO）<strong>原则的存储机制，它是一段</strong>连续的内存</strong>，所以对栈数据的定位比较快速。而堆则是<strong>随机分配</strong>的空间，处理的数据比较多，无论如何至少要两次定位。堆内存的创建和删除节点的时间复杂度是 O(log n)，栈创建和删除的时间复杂度则是 O(1)，栈速度更快。</p>\n<p>那么既然栈速度这么快，全部用栈不就好了？这又涉及生命周期的问题。栈中的生命周期是必须确定的，销毁时必须按次序从最后分配的块开始销毁，创建后什么时候销毁必须是一个定值，所以在分配和销毁时不灵活，基本只用于函数调用和递归调用这类生命周期比较确定的地方。相反，堆内存可以存放生命周期不确定的内存块，满足”需要时再删除”的需求，所以堆内存更适合全局性质的、需要灵活分配和销毁的内存块。</p>\n</blockquote>\n<p>很多人把”值类型放在栈中、引用类型放在堆中”当作绝对规律，这是错误的。栈内存主要为<strong>确定性生命周期</strong>的内存服务，堆内存则更多服务于<strong>无序的、随时可能释放</strong>的内存。因此值类型既可以在堆内也可以在栈内，引用类型的指针部分同样可以在栈内或堆内——但引用类型<strong>指向的内存块</strong>一定在堆内。</p>\n<h3 id=\"什么时候会用到装箱\"><a href=\"#什么时候会用到装箱\" class=\"headerlink\" title=\"什么时候会用到装箱\"></a>什么时候会用到装箱</h3><p>大部分时候只有当程序、逻辑或接口需要更加通用时，才会需要装箱。比如：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 情况一：调用含 object 类型参数的方法</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">PrintObject</span>(<span class=\"params\"><span class=\"built_in\">object</span> obj</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    Console.WriteLine(obj);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"built_in\">int</span> <span class=\"keyword\">value</span> = <span class=\"number\">42</span>;</span><br><span class=\"line\">PrintObject(<span class=\"keyword\">value</span>);     <span class=\"comment\">// int → object，发生装箱</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 情况二：非泛型容器，元素类型定义为 object</span></span><br><span class=\"line\">ArrayList list = <span class=\"keyword\">new</span> ArrayList();</span><br><span class=\"line\">list.Add(<span class=\"number\">100</span>);          <span class=\"comment\">// int → object，发生装箱</span></span><br><span class=\"line\">list.Add(<span class=\"string\">&quot;hello&quot;</span>);      <span class=\"comment\">// string 本身就是引用类型，不装箱</span></span><br></pre></td></tr></table></figure>\n\n<p>当你需要将一个值类型（如 <code>int</code>）传入参数为 <code>object</code> 的方法时，就需要装箱。又比如一个非泛型的容器（如 <code>ArrayList</code>）为了保证通用，将元素类型定义为 <code>object</code>，值类型数据放入时就会触发装箱。</p>\n<h3 id=\"装箱和拆箱的内部操作\"><a href=\"#装箱和拆箱的内部操作\" class=\"headerlink\" title=\"装箱和拆箱的内部操作\"></a>装箱和拆箱的内部操作</h3><p><strong>装箱</strong>：根据相应的值类型在堆中分配一个内存块，再将数据拷贝给它。具体按以下三步进行：</p>\n<ol>\n<li><strong>第一步</strong>：在堆内存中新分配一个内存块（大小为值类型实例大小加上一个方法表指针和一个 <code>SyncBlockIndex</code>）。</li>\n<li><strong>第二步</strong>：将值类型的实例字段拷贝到新分配的内存块中。</li>\n<li><strong>第三步</strong>：返回堆中新分配对象的地址——这个地址就是一个指向对象的引用了。</li>\n</ol>\n<p><strong>拆箱</strong>则更为简单：先检查对象实例，确保它是给定值类型的一个装箱值，再将该值从实例复制到值类型变量的内存块中。</p>\n<h3 id=\"装箱、拆箱对执行效率的影响及优化\"><a href=\"#装箱、拆箱对执行效率的影响及优化\" class=\"headerlink\" title=\"装箱、拆箱对执行效率的影响及优化\"></a>装箱、拆箱对执行效率的影响及优化</h3><p>由于装箱和拆箱时生成的是全新的对象，不断地分配和销毁内存不仅大量消耗 CPU，也会增加内存碎片，从而降低性能。那该如何优化呢？</p>\n<p>最关键的就是<strong>减少装箱和拆箱操作</strong>——在编程规范中要牢记：装箱和拆箱是消耗较大的操作，写代码时需要特别留意。</p>\n<p>整数、浮点数、布尔等数值型变量变化手段不多，主要靠加强编码规范来减少装拆箱。而 <code>struct</code> 有点不一样——它既是值类型，又可以像类一样实现接口，用途更多样、转换途径也更多，稍不留神就会埋下性能隐患。所以这里重点讲讲 <code>struct</code> 的优化方法。</p>\n<h4 id=\"通过重载函数来避免装箱\"><a href=\"#通过重载函数来避免装箱\" class=\"headerlink\" title=\"通过重载函数来避免装箱\"></a>通过重载函数来避免装箱</h4><p>比如常用的 <code>ToString()</code>、<code>GetHashCode()</code>、<code>Equals()</code> 方法：如果 <code>struct</code> 没有重写这些方法，调用它们时会先装箱再调用，导致内存分配的性能损耗。</p>\n<p>为什么 struct 调用这些方法需要先装箱？因为这些方法的默认实现定义在 <code>System.Object</code> 或 <code>System.ValueType</code> 基类中，都是虚方法（<code>virtual</code>）。当 struct 没有重写它们时，虚方法调用需要查方法表（vtable），而方法表存储在引用类型对象中——值类型实例本身不携带方法表指针。因此必须先将 struct 装箱为一个引用类型对象，才能完成虚方法的派发。例如：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">struct</span> MyStruct</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> Value;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">MyStruct s = <span class=\"keyword\">new</span> MyStruct &#123; Value = <span class=\"number\">10</span> &#125;;</span><br><span class=\"line\">s.ToString();     <span class=\"comment\">// MyStruct 没有重写 ToString → 先装箱再调用 → 产生 GC 垃圾</span></span><br></pre></td></tr></table></figure>\n\n<p>而要避免这一点很简单——在 struct 中手动重写这些方法即可：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">struct</span> MyStruct</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">int</span> Value;</span><br><span class=\"line\"></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">override</span> <span class=\"built_in\">string</span> <span class=\"title\">ToString</span>()</span> =&gt; Value.ToString();  <span class=\"comment\">// 重载后不再装箱</span></span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">override</span> <span class=\"built_in\">int</span> <span class=\"title\">GetHashCode</span>()</span> =&gt; Value.GetHashCode();</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">override</span> <span class=\"built_in\">bool</span> <span class=\"title\">Equals</span>(<span class=\"params\"><span class=\"built_in\">object</span> obj</span>)</span> =&gt; obj <span class=\"keyword\">is</span> MyStruct s &amp;&amp; Value == s.Value;</span><br><span class=\"line\">&#125;</span><br></pre></td></tr></table></figure>\n\n<p>所以，对于那些需要调用的引用类型方法，struct 必须逐一<strong>手动重载</strong>。</p>\n<h4 id=\"通过泛型来避免装箱\"><a href=\"#通过泛型来避免装箱\" class=\"headerlink\" title=\"通过泛型来避免装箱\"></a>通过泛型来避免装箱</h4><p>别忘了 struct 也是可以实现接口的。在不同的、相似的、具有父子关系的 struct 之间，可以用泛型来传递参数，这样就不用装箱后再传递了。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">interface</span> <span class=\"title\">IPrintable</span> &#123; <span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">Print</span>()</span>; &#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">struct</span> Document : IPrintable</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">string</span> Name;</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"keyword\">void</span> <span class=\"title\">Print</span>()</span> =&gt; Console.WriteLine(<span class=\"string\">$&quot;打印：<span class=\"subst\">&#123;Name&#125;</span>&quot;</span>);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 不使用泛型 —— 传参时装箱</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">ProcessByInterface</span>(<span class=\"params\">IPrintable printable</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    printable.Print();</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 使用泛型 —— 不会装箱</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">ProcessByGeneric</span>&lt;<span class=\"title\">T</span>&gt;(<span class=\"params\">T printable</span>) <span class=\"keyword\">where</span> T : IPrintable</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    printable.Print();</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">Document doc = <span class=\"keyword\">new</span> Document &#123; Name = <span class=\"string\">&quot;报告&quot;</span> &#125;;</span><br><span class=\"line\">ProcessByInterface(doc);   <span class=\"comment\">// doc 转为 IPrintable → 装箱</span></span><br><span class=\"line\">ProcessByGeneric(doc);     <span class=\"comment\">// 泛型直接约束为 IPrintable，不装箱</span></span><br></pre></td></tr></table></figure>\n\n<p>为什么传给 <code>IPrintable</code> 类型的参数就需要装箱？原因是 struct 实例本质上只是栈上的一段<strong>纯数据</strong>，没有对象头（Object Header），也没有方法表指针（MethodTable Pointer）。而接口方法调用属于<strong>虚分派（virtual dispatch）</strong>，CLR 必须通过方法表来查找实际要调用的方法——栈上的 struct 不携带这些信息，所以必须先装箱，在堆上生成一个带有完整对象头和接口映射表（Interface Map）的包装对象，才能完成接口分派。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 通过具体类型直接调用 —— 不装箱</span></span><br><span class=\"line\">Document doc = <span class=\"keyword\">new</span> Document();</span><br><span class=\"line\">doc.Print();     <span class=\"comment\">// 编译器知道 doc 就是 Document，直接静态调用</span></span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// 通过接口引用调用 —— 需要装箱</span></span><br><span class=\"line\">IPrintable ip = doc;   <span class=\"comment\">// doc 装箱：在堆上创建带方法表指针的副本</span></span><br><span class=\"line\">ip.Print();            <span class=\"comment\">// 通过接口分派，走方法表查找</span></span><br></pre></td></tr></table></figure>\n\n<p>而泛型则绕过了这个问题。由于 JIT 编译时会对每个值类型生成<strong>专用版本</strong>（代码特化），当 <code>T</code> 是 <code>Document</code> 时，JIT 生成的方法内部等价于直接接收 <code>Document</code> 类型的参数。调用 <code>Print()</code> 时走的是<strong>具体类型的静态分派</strong>，而不是接口虚分派——<code>where T : IPrintable</code> 约束仅用于编译期检查，确保 <code>T</code> 实现了 <code>Print()</code>，运行时完全不经过接口查表，因此全程不需要装箱。</p>\n<h4 id=\"通过继承统一的接口提前装箱，避免多次重复拆装箱\"><a href=\"#通过继承统一的接口提前装箱，避免多次重复拆装箱\" class=\"headerlink\" title=\"通过继承统一的接口提前装箱，避免多次重复拆装箱\"></a>通过继承统一的接口提前装箱，避免多次重复拆装箱</h4><p>很多时候拆装箱不可避免，那么我们就让多种 struct 继承某个统一的接口。这样不同 struct 就可以用相同的接口访问。将 struct 传递到其他方法时，相当于<strong>提前进行了一次装箱操作</strong>——在方法中得到的是引用类型的值，并且具备所需的接口，避免了在方法内部重复多次的拆装箱。</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br><span class=\"line\">26</span><br><span class=\"line\">27</span><br><span class=\"line\">28</span><br><span class=\"line\">29</span><br><span class=\"line\">30</span><br><span class=\"line\">31</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"keyword\">interface</span> <span class=\"title\">IShape</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"function\"><span class=\"built_in\">double</span> <span class=\"title\">Area</span>()</span>;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">struct</span> Square : IShape</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">double</span> Side;</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"built_in\">double</span> <span class=\"title\">Area</span>()</span> =&gt; Side * Side;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"keyword\">struct</span> Circle : IShape</span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"keyword\">public</span> <span class=\"built_in\">double</span> Radius;</span><br><span class=\"line\">    <span class=\"function\"><span class=\"keyword\">public</span> <span class=\"built_in\">double</span> <span class=\"title\">Area</span>()</span> =&gt; Math.PI * Radius * Radius;</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\"><span class=\"comment\">// Test 方法接收 IShape 引用类型，struct 传入时装箱一次</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">Test</span>(<span class=\"params\">IShape shape</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 方法内部可以多次调用 shape.Area()、shape 的方法等</span></span><br><span class=\"line\">    <span class=\"comment\">// 不会再发生装箱，因为 shape 已经是引用类型</span></span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">$&quot;面积：<span class=\"subst\">&#123;shape.Area()&#125;</span>&quot;</span>);</span><br><span class=\"line\">    Console.WriteLine(<span class=\"string\">$&quot;类型：<span class=\"subst\">&#123;shape.GetType().Name&#125;</span>&quot;</span>);</span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">Square square = <span class=\"keyword\">new</span> Square &#123; Side = <span class=\"number\">5</span> &#125;;</span><br><span class=\"line\">Circle circle = <span class=\"keyword\">new</span> Circle &#123; Radius = <span class=\"number\">3</span> &#125;;</span><br><span class=\"line\"></span><br><span class=\"line\">Test(square);   <span class=\"comment\">// Square → IShape，装箱一次，内部不再重复装箱</span></span><br><span class=\"line\">Test(circle);   <span class=\"comment\">// Circle → IShape，装箱一次，内部不再重复装箱</span></span><br></pre></td></tr></table></figure>\n\n<p>对比一下：如果没有统一的接口，Square 和 Circle 又都需要传入同一个方法处理，就只能退回到用 <code>object</code> 来接收：</p>\n<figure class=\"highlight csharp\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br><span class=\"line\">7</span><br><span class=\"line\">8</span><br><span class=\"line\">9</span><br><span class=\"line\">10</span><br><span class=\"line\">11</span><br><span class=\"line\">12</span><br><span class=\"line\">13</span><br><span class=\"line\">14</span><br><span class=\"line\">15</span><br><span class=\"line\">16</span><br><span class=\"line\">17</span><br><span class=\"line\">18</span><br><span class=\"line\">19</span><br><span class=\"line\">20</span><br><span class=\"line\">21</span><br><span class=\"line\">22</span><br><span class=\"line\">23</span><br><span class=\"line\">24</span><br><span class=\"line\">25</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\">// 没有统一接口 → 用 object 凑合</span></span><br><span class=\"line\"><span class=\"function\"><span class=\"keyword\">void</span> <span class=\"title\">ProcessWithoutInterface</span>(<span class=\"params\"><span class=\"built_in\">object</span> obj</span>)</span></span><br><span class=\"line\">&#123;</span><br><span class=\"line\">    <span class=\"comment\">// 每次访问具体字段/方法，都要先拆箱</span></span><br><span class=\"line\">    <span class=\"keyword\">if</span> (obj <span class=\"keyword\">is</span> Square)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        Square s = (Square)obj;         <span class=\"comment\">// 拆箱！</span></span><br><span class=\"line\">        Console.WriteLine(<span class=\"string\">$&quot;正方形面积：<span class=\"subst\">&#123;s.Side * s.Side&#125;</span>&quot;</span>);</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">    <span class=\"keyword\">else</span> <span class=\"keyword\">if</span> (obj <span class=\"keyword\">is</span> Circle)</span><br><span class=\"line\">    &#123;</span><br><span class=\"line\">        Circle c = (Circle)obj;         <span class=\"comment\">// 拆箱！</span></span><br><span class=\"line\">        Console.WriteLine(<span class=\"string\">$&quot;圆形面积：<span class=\"subst\">&#123;Math.PI * c.Radius * c.Radius&#125;</span>&quot;</span>);</span><br><span class=\"line\">    &#125;</span><br><span class=\"line\">    <span class=\"comment\">// 如果方法里还要再做其他操作（比如打印类型名、序列化等），</span></span><br><span class=\"line\">    <span class=\"comment\">// 每次都从 object 里拆出来，拆箱次数 = if 分支数 × 操作次数</span></span><br><span class=\"line\">&#125;</span><br><span class=\"line\"></span><br><span class=\"line\">Square square = <span class=\"keyword\">new</span> Square &#123; Side = <span class=\"number\">5</span> &#125;;</span><br><span class=\"line\">Circle circle = <span class=\"keyword\">new</span> Circle &#123; Radius = <span class=\"number\">3</span> &#125;;</span><br><span class=\"line\"></span><br><span class=\"line\">ProcessWithoutInterface(square);  <span class=\"comment\">// square → object（装箱）</span></span><br><span class=\"line\">ProcessWithoutInterface(circle);  <span class=\"comment\">// circle → object（装箱）</span></span><br><span class=\"line\"><span class=\"comment\">// 更糟的是：方法内部每次 (Square)obj / (Circle)obj 都在拆箱</span></span><br><span class=\"line\"><span class=\"comment\">// 如果用 is 做类型判断再强转，等于拆了两次（is 一次，强转又一次）</span></span><br></pre></td></tr></table></figure>\n\n<p>可以看到，没有统一接口时：传入方法时<strong>装箱一次</strong>，但方法内部每次要访问具体数据就得<strong>拆箱一次</strong>——如果有 N 种 struct 类型、每种有 M 个操作，最坏情况下拆箱次数 &#x3D; N × M。而使用统一接口后，只需在入口处装箱一次，整个方法体内都在跟已装箱的引用类型打交道，零额外开销。</p>\n<p>这种做法的优势在于：装箱发生在<strong>方法入口处</strong>，只做一次。方法内部操作的都是已装箱好的引用类型对象，无需再次拆装箱，从而将性能影响控制在最小范围。</p>\n<div class=\"tip\">\n最后依然要提醒大家：struct 值类型数据结构如果没有真正理解它的原理，用起来可能会引发很多麻烦。切记不要盲目认为使用结构体一定会让性能提升——在没有完全彻底理解之前贸然大量使用 struct，可能会对你的程序性能带来重创。\n</div>\n\n<hr>\n<h2 id=\"参考资料\"><a href=\"#参考资料\" class=\"headerlink\" title=\"参考资料\"></a>参考资料</h2><ul>\n<li><a href=\"http://www.luzexi.com/2019/01/26/Unity3D%E9%AB%98%E7%BA%A7%E7%BC%96%E7%A8%8B%E4%B9%8B%E8%BF%9B%E9%98%B6%E4%B8%BB%E7%A8%8B-CSharp%E8%A6%81%E7%82%B9%E6%8A%80%E6%9C%AF4\">《Unity3D高级编程之进阶主程》第一章，C#要点技术(四) 委托、事件、装箱、拆箱 - 技术人生 - 编程技术 - JESSE人生</a></li>\n</ul>\n","path":"posts/4235838758/","permalink":"https://littlefish04.github.io/blog/posts/4235838758/","tags":[{"name":"C#基础","_id":"cuidnWyuY1jAqcfiXHZQfsJs3","slug":"C-基础","path":"tags/C-基础/","permalink":"https://littlefish04.github.io/blog/tags/C-%E5%9F%BA%E7%A1%80/","length":2}],"categories":[{"name":"Unity 基础学习","_id":"cuidTzjCqx2ZSY0nK-cKuIOWc","slug":"Unity-基础学习","path":"categories/Unity-基础学习/","permalink":"https://littlefish04.github.io/blog/categories/Unity-%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0/","length":2}],"prev":null,"next":{"title":"C#常用组件底层代码分析","date":"2026-07-20T07:40:02.000Z","summary":"剖析 List 和 Dictionary 的底层源码实现，包括扩容机制、哈希拉链法等核心原理。","slug":"C-常用组件底层代码分析","published":true,"updated":"2026-07-22T15:30:00.525Z","_id":"cuidGN_Xbj_KXghJ3enxsj890","layout":"post","photos":[],"excerpt":"","path":"posts/1942871416/","permalink":"https://littlefish04.github.io/blog/posts/1942871416/","__post":true},"__post":true}